# TableNine 领域词汇

本词汇表统一 TableNine 产品与表现设计中的领域语言，避免把内容、载体和全局布局都笼统称作“面板”。

## 灵动界面

**面板载体**：
界面中长期存在、可移动和改变占位尺寸，并能轮换视觉用途的像素化视觉块；它不包含必须逐帧追随运动的文字。
_Avoid_: 面板、框、UI 块

**功能面**：
某一时刻共同表达一项功能的视觉投影与内容投影，如玩家信息、卡组名称或设置操作；两种投影可以处在不同渲染空间。
_Avoid_: 页面、状态、面板

**内容槽**：
位于固定点位或固定范围内、用于承载文字等非运动内容的界面区域；构型变化时主要切换内容和显隐，不追随面板载体逐帧位移。
_Avoid_: 跟随文字、面板子文本

**随行内容**：
被明确指定在转场中跟随面板载体的内容；其包裹范围构成该载体转场尺寸的额外下限。
_Avoid_: 所有文字默认跟随、反向追踪背板

**流动下限**：
面板载体在转场中维持自身可辨认形态所允许的最小宽高；即使内容已隐藏，也不得缩小到失去布局流动意义。
_Avoid_: 零面积、缩没、强制切换

**布局构型**：
某一时刻全部面板载体之间的位置、占位尺寸、遮盖顺序与组合关系。
_Avoid_: 布局状态、面板状态

**大盘构型**：
人工编排并验证过的全局终态构图，规定主要面板载体、功能面与内容槽之间的宏观关系。
_Avoid_: 自由布局、页面预制体、大盘布局 UI

**构型样板**：
在编辑器中手工搭建的大盘构型权威终态；它既供人和 AI 观察，也用于烘焙驱动唯一一套运行时载体的构型数据。
_Avoid_: 仅供参考的截图、整套运行时替换 Prefab

**局部变体**：
在不改变大盘构型宏观关系的前提下，由悬停或轻量操作触发的受限内部变化。
_Avoid_: 任意变形、自由组合、小面板状态

**构型许可表**：
规定每个大盘构型允许启用哪些局部变体的产品约束；未列入许可表的组合不可到达。
_Avoid_: 动态布局兼容猜测、全组合支持

**碰撞校验对**：
一次转场中必须接受连续面积相交检查的一对面板载体；有意重叠的载体对由作者显式排除并通过预览验收。
_Avoid_: 全对象碰撞、自动理解表现语义

**转场计划**：
从当前实况过渡到目标构型的一组载体变化、启动时差与切换时刻；意图中途改变时可以从最新实况重新规划，但不得改变权威终态。
_Avoid_: 固定 A-B 动画、无条件直接重定向

**构型预演**：
由悬停触发、可撤销且尚未提交的临时布局构型；移出稳定触发区后回到最近一次已提交构型。
_Avoid_: Hover 动画、临时面板状态

**已提交构型**：
由明确操作确认、在新的提交或流程切换发生前持续生效的布局构型。
_Avoid_: 锁定 Hover、当前页面

**稳定触发区**：
不随构型预演中的面板载体运动而改变的交互命中区域，用于避免预演自身造成反复进入与离开。
_Avoid_: 面板碰撞体、动态命中框

**停泊位**：
布局构型中供面板载体占据的逻辑区域；载体进入另一载体的内部停泊位可形成视觉吞并，但不改变任一载体的身份或物理父级。
_Avoid_: 子物体槽、动态父节点

**视觉吞并**：
两个固定身份的面板载体通过停泊、裁切与边框衔接表现为一个组合体，其中一个可承担另一个的补充信息。
_Avoid_: SetParent、合并物体、真的吞掉

**受保护场地**：
局内九宫格、场上牌、手牌与卡组构成的独立表现域；灵动界面可以遮盖它，但不接管、重排或重置其内部状态。
_Avoid_: 中央面板、可重排战场

**场地覆层**：
用于暂时遮住受保护场地并承载替代功能的界面表面；移除后直接显露场地的当前真实状态。
_Avoid_: 战场背面、场地替换、战场重建

**活性遮盖**：
场地覆层存在期间，受保护场地仍按正常时间推进表现与流程，但不接收被覆层占有的玩家输入。
_Avoid_: 暂停菜单、冻结场地、背景快照

## 战斗表演编排

**表演导演（PresentationDirector）**：
战斗内表演时间线的唯一所有者与唯一出口；输入意图在此排队，Core 解算批次与表演步骤在此交替推进。
_Avoid_: 多个并行编排器、散落的 async 链、God Object 直接驱动

**编排时间线（BattleTimeline）**：
唯一的串行主线；玩家输入互斥只认它是否在跑。
_Avoid_: 多条竞争时间线、全局 PresentationLocked 布尔当唯一门禁

**编排步骤（Step）**：
时间线上的原子单元，类型含"请 Core 解算下一批""播一段表演""Delay 等待""触发脉冲"；表现侧一律称 Step。
_Avoid_: 用 Action 指代表演原子（Action 专属 Core 逻辑侧）

**解算批次（Batch）**：
一拍被表演就位回执驱动的 Core 解算，配 OpenBatch/FinishBatch；Core 一次只前进一批。
_Avoid_: RunToCompletion 一口气把整局逻辑算在表演之前、核先行

**就位回执（ack）**：
一个批次的表演播完后、通知时间线可前进的信号。
_Avoid_: 靠阈值/预算/超时间接判定完成

**输入意图（InputIntent）**：
玩家输入转成的、可在忙时缓冲的请求；不直接改状态。
_Avoid_: 输入回调直接写 Core/改 Transform、忙时直接吞掉点击

**触发脉冲（Trigger）**：
特效/音效的一次性脉冲，发即完成、不持有所有权、可降级；需卡时序时在时间线显式插 Delay。
_Avoid_: 特效占用长期控制权、await 特效把 FX 卡进关键路径

**旁路装饰道（bypass lane）**：
纯装饰性表演（受击反馈、额外弹道等）的并行道，与主线并行推进但不占输入互斥锁。
_Avoid_: 装饰表演阻塞主交互、装饰表演抢主线时间轴

**占格权威 vs 几何注册**：
逻辑占格真相永在 Core（BoardModel）；Cards 只持"格位→卡视图/Transform"的几何注册用于命中测试；目标合法性由 Flow 在 idle 时对 Core 裁决。
_Avoid_: Cards 维护独立占格真相、事后 force sync 对账、双真相打架

## 输入门禁与意图收口

**意图收口（IntentIntake）**：
玩家输入进入表演编排的唯一入口；门禁、合法性与互斥/缓冲在此一处裁决，任何输入路径都不得绕过它。
_Avoid_: 各 Controller 各自判定、Explore 直发 Command 绕过门禁、advisory 与 authoritative 两层门禁并存

**时序互斥（Mainline busy）**：
门禁第一轴：唯一以"主线是否在跑"为准的临时输入互斥真相；凡须阻塞输入的表演都必须持有主线（作为 Step 或外部租约）。
_Avoid_: BattleBusy/FieldBusy 各自为政的并行 busy 源、全局 PresentationLocked 布尔当唯一门禁

**输入所有权（Input ownership）**：
门禁第二轴：某一时刻拥有玩家输入的表面（受保护场地 / 场地覆层 / 棋盘选择模式）；意图被接纳须满足"目标表面 == 当前所有者"。与时序互斥正交。
_Avoid_: Opening/Overlay/BoardSelect 摊平成一袋布尔、用 busy 表达所有权、活性遮盖当成"忙"

**最新覆盖缓冲（latest-wins buffer）**：
主线忙时至多缓冲一条意图，后来的点击覆盖先前缓冲；主线转 idle 时对 Core 重校合法性再放行，非法即丢。
_Avoid_: 最早保留后来者拒、缓冲多条 FIFO 连动、flush 时不重校合法性

**冲动轻点（impatience tap）**：
主线忙时落下、并不改变缓冲的额外点击；作为独立脉冲交给（当前 no-op 的）加速通道，为未来"多点即加速"预留缝。
_Avoid_: 忙时额外点击直接静默吞掉、把加速逻辑塞进缓冲语义

## 卡牌表现实体

**卡牌底盘**：
运行时卡牌实体；持有运动塔、排序组、命中代理与效果挂点，不表达某一类对象的专属卡面维度。
_Avoid_: Standard Card、万能卡、一张顶所有

**卡面模板**：
按对象种类区分的卡面与卡背视觉构型；挂在底盘约定的视觉层下，只负责该类对象的可见维度。
_Avoid_: 用完整运行时 Prefab 替代底盘、Sample Card 当生产实体

**遗物卡**：
用于选择、获取，以及后续可能的场地展示的遗物视觉对象；不进入手牌与卡组运动区。对应表现种 `Relic`。
_Avoid_: 遗物进手牌/抽牌堆、把遗物当帮助卡组牌

**玩家卡**：
标准玩家对象的卡面模板；对应表现种 `Avatar`。
_Avoid_: 用玩家技能卡、PlayerSkill 表现链路顶替玩家卡

**怪物卡**：
标准怪物对象的卡面模板；对应表现种 `Monster`。
_Avoid_: 与玩家卡共用数值栏位布局

**道具卡**：
帮助卡与道具对象的统称卡面模板；对应表现种 `HelpCard`、`Item`，以及语义为组牌内容的 `PlayerCard`。
_Avoid_: 把 Avatar 叫成道具卡、把遗物并进道具卡

**玩家技能（已移除 / 已并入遗物）**：
历史上的 Player Skill 卡面与被动持有路径；产品上已正式去除，被动效果已吸收为遗物（`relic.*`，默认 Blue；开局初始遗物 `relic.easy_road`）。怪物技能仍用 `skill.*`。
_Avoid_: 为玩家技能单独做卡面/持有栏/授予原子、在 mapper/Catalog 里继续按 PlayerSkill 分支做新接线

**卡牌表现投影**：
从 Content/结算指令汇成的、面向表现的一整包可读状态；**视觉**与**数值**分属两条路径——视觉可由 Mapper 刷新，数值只经表演锚点上的结算指令赋值。不按卡种裁剪真相。
_Avoid_: 每种卡面各自拉一套独立数据源、卡面自己当状态权威、用 JSON stats 覆盖战中显示值

**卡面消费**：
卡面模板只消费表现投影里自己需要的字段；不需要的不暴露、不绑定，特殊诉求时可另从后台读取而不扩张默认投影。
_Avoid_: 卡面回写 Core、为未使用字段强行占位 UI

**权威值**：
规则内核此刻的真相（含跨战斗持久的运行级修正）；表演开始时一批解算往往已是终值。
_Avoid_: 把权威值直接当成玩家此刻应看到的卡面数字

**卡面显示值**：
玩家在卡面上看到的攻/甲/血等，由结算指令在表演锚点驱动，**有意滞后于权威值**。
_Avoid_: 表演中随时直读 Core 拉齐卡面、强制对账抹平滞后

**结算指令**：
一次解算批次产出的、按结算顺序排列的事实条目（`PresentationBatch.Instructions`）；数值类携带结算后绝对值，卡面赋值不累加。
_Avoid_: 表现层自行推算增量、用 EventLog 旁路代替指令消费驱动 FX/飘字/金币

**表演锚点**：
表演编排在时间轴上声明的语义时刻（命中 / 收尾 / 无表演消费）；剧本报点时排期器把归属该锚点的结算指令交给**第一个认领的处理器**（卡面数值或旁路装饰）。
_Avoid_: 各调用点私自选同步时机、默认沉默「不上卡面」、为装饰另建第二张锚点表

**战斗锚点排期器**：
一批结算指令的唯一分发出口；装载非 `None` 归属指令，按报点分发给多个 `IBattleBeatHandler`；Settled 后未消费只报不改。
_Avoid_: 每类表现各建一套排期、装饰 await 进主线 ack

**投影提交（Commit）**：
把表现投影中某一字段或某一对象的可见值推到卡面消费端的动作；卡面**数值**只由战斗锚点排期器经卡面数值处理器触发，不得在队列外直读 Core 刷数值。装饰类表演经同一排期器的其它 `IBattleBeatHandler` 消费（见 ADR-0007），不另开提交出口。
_Avoid_: Sync 直读 Core 写卡面数值、任意 SetAttack/SetHealth 旁路、用 digit animate 代替提交时机、强制对账静默拉回

**牌面朝向**：
卡牌实例在逻辑上的明/暗（正面/背面）状态；可参与攻击许可与效果可否触发等规则裁决，权威在 Core，不在显示模式。
_Avoid_: 用 Hand/Deck/Ground 显示模式代替朝向真相、仅把翻牌当 L4 动画

**卡面朝向表现**：
卡面模板上正面/背面节点的可见构型；只消费已提交的牌面朝向，不参与规则裁决。
_Avoid_: 卡面本地私自翻面当逻辑状态

**卡面挂载点**：
卡面模板在卡牌底盘变换塔中的固定接入位置（L4 CardVisual 之下）；不得改写 L0–L3 职责或另起与塔并行的视觉根。
_Avoid_: 用卡面模板替换底盘、front/back 拆到不同塔层、为翻牌另建与塔冲突的父级

**卡级排序黑盒**：
卡牌实例之间的前后关系仍由底盘根 SortingGroup 与既有 DisplayMode/飞行抬序逻辑独占；卡面模板只配置组内相对 order。
_Avoid_: 卡面自挂第二套 SortingGroup、卡面改写卡级 sortingOrder、取消卡级 SG 改绝对序

**通层排序值**：
同一卡面排序作用域内（同一 SortingLayer、同一卡级 SG 子树）出现重复的 sortingOrder；属装配错误，编辑器校验须告警。
_Avoid_: 靠不稳定的同序叠加碰运气、前后端各写一套绝对序表

**卡面可装配槽**：
卡面模板上允许被内容/编辑器替换的视觉挂点（图标、背景、边框、横幅等）；接入后原则上模板内全部内容槽均可装配替换。
_Avoid_: 写死在预制体里不可替换的关键立绘、只改运行时改不了编辑器

**默认装配优先级**：
在未另行指定时，内容装配先替换各模板的主图标（Main Icon），其次替换卡面背景（若该模板有背景槽）。
_Avoid_: 先改边框/装饰再改主识别物、无优先级的随意覆盖

**卡面最终态显示**：
预览与运行时展示的是对象卡牌装配完成后的完整可见状态；不因编辑器收纳次要槽而隐藏最终画面。
_Avoid_: 预览只画主图标、收纳槽等于不渲染

**直接暴露装配项**：
编辑器默认展开、供高频替换的装配入口；与卡面装配槽为同一槽表中的标记子集。初版直接暴露含主图标、卡面背景与卡背相关槽；其余默认收纳；图标呈现属性（大小/颜色等）进收纳子栏。
_Avoid_: 每张卡默认摊开全部槽位、把卡背排除在直接管理之外、贴图装配与描述图标分两套系统

**卡背装配**：
卡背归**卡组**所有：同一卡组的卡共用一套卡背，个别特殊卡（Boss、诅咒卡等）可在卡上覆写。卡组未配置时回退到该卡面模板预制体上的兜底卡背，不设全局统一卡背源。预览为分层重叠后的最终态。直接暴露字段对齐模板三件套：背框、背纹、logo。
_Avoid_: 逐卡各配一套卡背、四套模板共用一个全局卡背、缺省时找全局默认背图、另建平行卡背 Catalog

**卡面模板表**：
由表现种（Kind）映射到卡面模板预制体的显式表；Spawn 时先实例化卡牌底盘，再按表将对应卡面挂入 L4。DefId 级预制体注册仅作特例覆盖。
_Avoid_: 为每种卡复制完整底盘、仅靠 DefId 前缀猜测模板

**卡牌底盘宿主**：
挂在卡牌底盘根上的薄视图组件（现名可仍为 `StandardCardView`）；负责 SortingGroup、卡面挂载与把已提交投影分发给当前卡面 Binder，不再充当全能数值/贴图 API。类型名可暂留以降低爆破面，职责按底盘宿主理解。
_Avoid_: 继续用上帝 SetAttack/SetCardFace 当正式通路、为每种卡面各挂一套根 View

**卡面基础描述**：
写在卡面上的**人手写卡级概括**；一句话说清整张卡，多效果时由人负责精炼，不由模板拼接。可插 `{参数}` 占位符，由装配实参填充——填的是初始配置值，不接入战斗表现数值管线，文案本身不随战中数值跳变。图标占位引用该卡面装配系统中已暴露的图标语义槽，运行时按装配结果插入真实游戏图标。
_Avoid_: 用效果描述拼接充当卡级概括、恢复悬停 Card Info Text 当卡面描述权威（该动态 HUD 管道已退役）、把基础描述当动态数值句、用打字表情代替图标占位、描述里写死资源路径

**详细描述**：
供右键（或等价）打开的详述文案，由两段构成：人手写的卡面基础描述 + 从装配结果**自动展开**的词条解释。参数插值后与实际逻辑不脱节，走独立排版系统。规划中尚未落地。
_Avoid_: 用悬停描述顶替详细描述面板、把详述长文塞进卡面基础描述、详述文案与实际装配各写一份

**卡面装配槽**：
卡面装配系统中的唯一槽表条目；以代码内部代号标识（如 `Action_Icon`、`Main_Icon`），编辑器悬停显示中文注释（如行动图标/攻击图标）。图标槽可装配 Sprite，缺省回退到该卡面源模板自带图标；数值槽缺省统一为 0。
_Avoid_: 中文当主键、描述写资源路径、缺省找全局图、缺省数值用 -1 或隐藏当「无」

**描述方括号引用**：
描述中用方括号包裹代号。两类来源单向分层：① 装配 Insertable 槽（如 `[Action_Icon]` / `[Main_Icon]`）只由高层装配/模板缺省下行解析，描述侧**不可**改其 Sprite；② 项目级**词条表**登记与装配无关的自定义码（如 `[灼烧]`），全局共用、不写回预制体。输入/JSON 始终保留代号；卡面渲染为 TMP 内联 sprite，详情描述展开为词条解释——同一份数据两种展开。内联布局（em 相对 Offset / Base Scale）仍由项目级 `CardFaceDescriptionInlineIconStyle` 按代号管理。编辑入口：「NineGrid/表现层配置 → 描述富文本」。
_Avoid_: 用词条表覆盖 `Main_Icon`/`Action_Icon`、占用装配槽保留代号、`{icon:…}` 与槽表脱节、表情字符、裸路径、把内联布局或词条映射写进单卡 JSON、字号一变就重调绝对像素偏移

**装配槽注册表**：
项目级 ScriptableObject，登记全部卡面装配槽代号、中文注释与槽角色（直接暴露/收纳/可插入描述/数值等）；卡面模板选用并绑定节点，内容条目按代号填值。
_Avoid_: 每模板私自发明代号、仅靠预制体字段名当全局契约、初版不用注册表

**卡面首波回填**：
新卡面系统接入后，运行时优先从既有装配/内容数据回填主图标、名字与数值；描述、行动计数等未接线槽口走模板默认（数值默认 0），其余槽待后续装配。目标是可玩且能从卡面区分不同卡牌效果。
_Avoid_: 首波强求描述/回合计数接线、首波清空老素材重做图

## 卡牌内容装配

**效果模板**：
不绑定任何具体卡的一类效果诉求；描述「做什么」与「需要哪些参数」，可被帮助卡、遗物、怪物技能共同引用。
_Avoid_: 按卡命名的一次性效果（`help.fireball.use` 式）、逻辑相同却各写一条、把容器种类写进模板身份

**装配引用**：
卡上的一条条目，指明引用哪个效果模板并给出实参；一张卡可有多条。卡的全部信息（身份、表现引用、数值、装配引用、分类）在同一个文件里看得全。
_Avoid_: 参数外置到数值表再靠 id 间接、模板带默认值让卡文件看不出实际数值、把 DSL 内联进卡以致复用退化成复制粘贴

**效果责任自陈**：
效果自己声明适用的场景、对象与条件；享有多大权利（产生多大影响），就承担多大责任（声明多严的适用范围）。不存在按场域拦截的外部门禁；同一效果不得因上下文呈现两种形态，若有则拆成两个效果。
_Avoid_: `EffectOwnerScopeGate` 式外部风控、`ownerOnly` 这类上下文开关参数、靠默认值隐式表达适用范围、装配非法却静默失效

**适用声明（requires）**：
自陈中「我适用于什么场景」的部分——需场上实体 / 需被使用 / 需处于某区域。不成立属**装配错误**，诊断级别高于玩法条件。
_Avoid_: 与玩法条件混写一块、不成立时静默跳过不留痕

**玩法条件（conditions）**：
自陈中「此刻是否满足」的部分——血量、格位、计数等。不成立属**正常玩法**，不是错误。
_Avoid_: 用它表达适用范围、让探查手段无法与装配错误分诊

**词条**：
项目级共享的玩家可读机制语汇（约百条量级），如「灼烧」「护甲」「穿透」；一个词条可被多个效果模板引用，解释只写一遍。按认知分区归类供百科浏览，单一分区 + `Others` 兜底；分区是产品编排，不是代码模块。
_Avoid_: 一模板一词条导致共享机制解释重复、把 75 个 DSL 原子名当玩家词条、中文当主键

**卡组**：
卡的视觉归属单位，决定卡背；怪物的卡组兼任遭遇编排单位，回答「什么时候会遇到你」。玩家一组、帮助卡一组、遗物一组，怪物按所属阵营分组；不存在统称的「怪物卡组」。
_Avoid_: 把功能角色塞进卡组、为帮助卡编造阵营归属、逐卡各配卡背

**功能角色**：
帮助卡与遗物的粗粒度分类轴（攻击 / 防御 / 功能），回答「你在玩家 build 里干什么活」；桶数少而稳定，用于投放均衡规则。
_Avoid_: 用细标签做均衡（规则退化成一长串枚举，加卡就改规则）、与卡组混为一轴

**细标签**：
可增长的多值风味/检索标签（直伤、恢复、经济、位移…），供百科检索、词条关联与未来的协同 build；不承担投放均衡职责。
_Avoid_: 中文当主键、把它当粗轴用
