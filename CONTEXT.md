# TableNine 领域词汇

本词汇表统一 TableNine 产品与表现设计中的领域语言，避免把内容、载体和全局布局都笼统称作“面板”。

## 灵动界面

**面板载体**：
界面中长期存在、可移动和改变占位尺寸，并能轮换视觉用途的像素化视觉块；它不包含必须逐帧追随运动的文字。
_Avoid_: 面板、框、UI 块

**功能面**：
某一时刻共同表达一项功能的视觉投影与内容投影，如玩家信息、卡组名称或设置操作；两种投影可以处在不同渲染空间。
_Avoid_: 页面、状态、面板

**内容槽**：
位于固定点位或固定范围内、用于承载文字等非运动内容的界面区域；构型变化时主要切换内容和显隐，不追随面板载体逐帧位移。
_Avoid_: 跟随文字、面板子文本

**随行内容**：
被明确指定在转场中跟随面板载体的内容；其包裹范围构成该载体转场尺寸的额外下限。
_Avoid_: 所有文字默认跟随、反向追踪背板

**流动下限**：
面板载体在转场中维持自身可辨认形态所允许的最小宽高；即使内容已隐藏，也不得缩小到失去布局流动意义。
_Avoid_: 零面积、缩没、强制切换

**布局构型**：
某一时刻全部面板载体之间的位置、占位尺寸、遮盖顺序与组合关系。
_Avoid_: 布局状态、面板状态

**大盘构型**：
人工编排并验证过的全局终态构图，规定主要面板载体、功能面与内容槽之间的宏观关系。
_Avoid_: 自由布局、页面预制体、大盘布局 UI

**构型样板**：
在编辑器中手工搭建的大盘构型权威终态；它既供人和 AI 观察，也用于烘焙驱动唯一一套运行时载体的构型数据。
_Avoid_: 仅供参考的截图、整套运行时替换 Prefab

**局部变体**：
在不改变大盘构型宏观关系的前提下，由悬停或轻量操作触发的受限内部变化。
_Avoid_: 任意变形、自由组合、小面板状态

**构型许可表**：
规定每个大盘构型允许启用哪些局部变体的产品约束；未列入许可表的组合不可到达。
_Avoid_: 动态布局兼容猜测、全组合支持

**碰撞校验对**：
一次转场中必须接受连续面积相交检查的一对面板载体；有意重叠的载体对由作者显式排除并通过预览验收。
_Avoid_: 全对象碰撞、自动理解表现语义

**转场计划**：
从当前实况过渡到目标构型的一组载体变化、启动时差与切换时刻；意图中途改变时可以从最新实况重新规划，但不得改变权威终态。
_Avoid_: 固定 A-B 动画、无条件直接重定向

**构型预演**：
由悬停触发、可撤销且尚未提交的临时布局构型；移出稳定触发区后回到最近一次已提交构型。
_Avoid_: Hover 动画、临时面板状态

**已提交构型**：
由明确操作确认、在新的提交或流程切换发生前持续生效的布局构型。
_Avoid_: 锁定 Hover、当前页面

**稳定触发区**：
不随构型预演中的面板载体运动而改变的交互命中区域，用于避免预演自身造成反复进入与离开。
_Avoid_: 面板碰撞体、动态命中框

**停泊位**：
布局构型中供面板载体占据的逻辑区域；载体进入另一载体的内部停泊位可形成视觉吞并，但不改变任一载体的身份或物理父级。
_Avoid_: 子物体槽、动态父节点

**视觉吞并**：
两个固定身份的面板载体通过停泊、裁切与边框衔接表现为一个组合体，其中一个可承担另一个的补充信息。
_Avoid_: SetParent、合并物体、真的吞掉

**受保护场地**：
局内九宫格、场上牌、手牌与卡组构成的独立表现域；灵动界面可以遮盖它，但不接管、重排或重置其内部状态。
_Avoid_: 中央面板、可重排战场

**场地覆层**：
用于暂时遮住受保护场地并承载替代功能的界面表面；移除后直接显露场地的当前真实状态。
_Avoid_: 战场背面、场地替换、战场重建

**活性遮盖**：
场地覆层存在期间，受保护场地仍按正常时间推进表现与流程，但不接收被覆层占有的玩家输入。
_Avoid_: 暂停菜单、冻结场地、背景快照

## 战斗表演编排

**表演导演（PresentationDirector）**：
战斗内表演时间线的唯一所有者与唯一出口；输入意图在此排队，Core 解算批次与表演步骤在此交替推进。
_Avoid_: 多个并行编排器、散落的 async 链、God Object 直接驱动

**编排时间线（BattleTimeline）**：
唯一的串行主线；玩家输入互斥只认它是否在跑。
_Avoid_: 多条竞争时间线、全局 PresentationLocked 布尔当唯一门禁

**编排步骤（Step）**：
时间线上的原子单元，类型含"请 Core 解算下一批""播一段表演""Delay 等待""触发脉冲"；表现侧一律称 Step。
_Avoid_: 用 Action 指代表演原子（Action 专属 Core 逻辑侧）

**解算批次（Batch）**：
一拍被表演就位回执驱动的 Core 解算，配 OpenBatch/FinishBatch；Core 一次只前进一批。
_Avoid_: RunToCompletion 一口气把整局逻辑算在表演之前、核先行

**就位回执（ack）**：
一个批次的表演播完后、通知时间线可前进的信号。
_Avoid_: 靠阈值/预算/超时间接判定完成

**盘面稳定化（Board stabilization）**：
交互相位的稳定边界由 Core `IBoardStabilizationSystem` 权威判断：存在非 Avatar 空格且抽牌堆仍有牌时才欠补位；每次 `ResolveNextSlice` 最多执行一轮 `FillEmptySlotsAction`。表现侧 `BoardStabilizationScheduler` 只在上一轮 Present ack 后再次检查，逐轮推进 `Resolve → Present → ack` 直到稳定。融合/重组结果的延后抽牌排除由 Core 管理，表现层不得临时改牌堆。
_Avoid_: 每条剧本自带补牌布尔、固定 Tick 数推断完成、Present 未 ack 先解算下一轮、表现层维护融合牌堆例外

**输入意图（InputIntent）**：
玩家输入转成的、经 IntentIntake 裁决的请求；不直接改状态。
_Avoid_: 输入回调直接写 Core/改 Transform、忙时缓冲意图待主线空闲后自动执行

**触发脉冲（Trigger）**：
特效/音效的一次性脉冲，发即完成、不持有业务所有权；需卡时序时在时间线显式插 Delay。音效可按既有策略降级；视觉特效失败不播放回退表现，必须留下可归因错误，但不得阻断规则或主线。
_Avoid_: 特效占用长期控制权、await 特效把 FX 卡进关键路径、用占位特效掩盖 VFX 配置或播放错误

**视觉特效提示（VFX Cue）**：
玩家可感知动作中“这里可能需要视觉附加表演”的稳定语义标识；它描述发生了什么，不包含序列帧路径、运行时实例 ID、空间坐标或具体播放实现。它与声音提示身份独立，同一动作可以分别发射其中任意一种或两种提示。
_Avoid_: 播放器调用点、素材路径、运行时 UID、把 Audio Cue 与 VFX Cue 强制合成同一身份

**视觉特效绑定（VFX Binding）**：
视觉特效提示或持续视觉状态到“是否播放、调用哪个稳定播放器”的持久化单一真源，并可按稳定内容标识提供专属覆盖。素材型播放器的真实公共调参可以由绑定提供默认并按白名单覆盖；程序化播放器自治其算法、内部参数、资源与完成条件，绑定不伪造一套通用参数去约束它。
_Avoid_: 从素材目录运行时合并播放决策、在提示发射处选择播放器、总控假装理解所有程序化参数、允许调用方覆盖生命周期或视觉域契约、把一次播放实例当绑定、要求每个声音绑定都有对应视觉绑定

**视觉空间上下文（VFX Spatial Context）**：
一次视觉特效请求携带的运行时定位依据，包含语义角色、可选控制主体或格位引用以及发射时位置快照；视觉特效绑定或播放器据此选择位置快照、跟随对象或受控视觉域。运行时身份只用于本次定位与生命周期，不进入绑定主键。
_Avoid_: 只传裸世界坐标、把 Transform 或运行时 UID 写进内容绑定、由素材反查业务目标

**视觉域宿主（VFX Domain Host）**：
向特效实例提供父级、遮罩、跟随、坐标转换和排序边界的外部视觉控制主体；特效可以管理宿主内部视觉，但不得越界改写共享相机、Canvas、卡级排序或卡牌底盘变换塔。
_Avoid_: 每个特效任意创建全局视觉根、播放器自行篡改外部排序、把所有空间强制摊平成世界坐标

**附着型特效（Attached VFX）**：
依赖视觉域宿主存续的特效；宿主在开始前丢失则不创建，播放中丢失则按已声明的正常生命周期结束。持续视觉状态通常属于此类。
_Avoid_: 宿主消失后猜测世界坐标继续跟随、把正常宿主结束误报为播放器故障

**独立型特效（Independent VFX）**：
在请求被接纳时取得确定视觉空间、此后不再依赖业务宿主的一次性特效；开始后按播放器完成契约自然播完，停用绑定只抑制后续请求。爆炸、飞行金币等属于此类。
_Avoid_: 宿主死亡时中断已开始的独立表演、用跟随失败回退快照来伪装空间类型、由工作台强杀已接纳实例

**金币表现时间窗（Gold presentation window）**：
一批独立飞行金币从第一枚抵达并消失到最后一枚抵达并消失的计划时段；金币播放器一次性给出该时段，金币 HUD 在其中独立推进数字并在末端精确收敛，不与每个金币实例逐一回调绑定。
_Avoid_: 每粒金币驱动一次数字事件、VFX 直接写金币文本、让主线等待飞币完成、HUD 复制飞行算法

**持续视觉状态（Persistent Visual State）**：
由稳定运行时所有者在命名视觉槽中持有的长期视觉投影，以 `owner + slot + state` 标识并通过幂等期望状态显式开始、替换和结束；它与一次性触发脉冲共享素材目录和播放器能力，但不属于发即完成的 VFX Cue。
_Avoid_: 用无限循环 Cue 冒充持续状态、只持易丢失的实例句柄、照搬音乐播放代数与幽灵 BGM 防护体系

**期望视觉槽状态（Desired VFX slot state）**：
某个运行时所有者的命名视觉槽此刻应该投影的唯一持续视觉状态；同值重复提交无操作，新值原子替换旧值，空值清除。较早流程只能在槽仍持有其预期状态时条件式清除，不得误停后来者。
_Avoid_: Start/Stop 失配、只靠易丢失句柄停止、旧异步流程无条件清空新状态、由特效反推规则真相

**声音提示（Audio Cue）**：
玩家可感知动作中“这里可能需要声音”的稳定语义标识；它描述发生了什么，不包含素材路径、运行时实例 ID 或具体播放实现。
_Avoid_: PlaySound 调用点、方法名、Clip 路径、运行时 UID

**声音绑定（Audio Binding）**：
声音提示到实际播放决策的持久化映射，包含素材、音量、音频起播点、绑定延迟和最短播放间隔；可有稳定内容标识限定的专属覆盖。
_Avoid_: 在声音提示发射处同时决定具体声音、把一次播放实例当绑定

**声音总线（Audio Bus）**：
玩家可独立调音和静音的声音类别；首版为 Master、BGM、SFX，其中 Master 统一控制后两者且静音不破坏原音量。
_Avoid_: 每个模块各建音量开关、用绑定音量代替玩家总控

**期望音乐状态（Desired music state）**：
游戏此刻希望呈现的唯一 BGM 语义状态；同状态或解析到同一音乐的重复提交不得重启音乐。
_Avoid_: 不同游戏表面各自拥有当前 BGM、用正在发声的素材反推游戏状态

**音乐播放代数（Music playback generation）**：
每次实际 BGM 切换产生的递增标识，用于关联状态请求、播放、淡化、停止和异常重叠诊断；旧代数不得再成为当前音乐。
_Avoid_: 只记 Clip 名无法还原切歌因果、无来源的幽灵 BGM

**音频起播点（Clip start offset）**：
一次声音从音频素材内部哪个时间点开始读取；它用于跳过素材自身的前导空白或迟滞，不表示相对游戏事件提前播放，也不允许早于素材开头。
_Avoid_: 负延迟、提前触发、事件时间偏移

**随机音频池（Audio variation pool）**：
声音绑定内部可选的多个音频素材集合；调用方仍只发一个声音提示，运行时独立随机选出素材，默认避免立即重复。随机结果不要求跨跑图复现，但每条播放记录必须写明实际选中的素材。
_Avoid_: 调用方自行随机 Clip、消费 Core RNG、为每个变体新增 Cue、把素材池暴露为业务概念

**绑定延迟（Binding delay）**：
为解决声音与既有表现“对不上”而配置的播放延迟；声音提示一旦被接纳便默认必播，不因发射宿主随后死亡或销毁而自动取消。
_Avoid_: 宿主生命周期延迟、对象销毁即取消、用时间线 Delay 阻塞主线

**可取消排期提示（Cancellable scheduled cue）**：
只用于蓄力、预备、持续施法等“动作尚未真正成立”的声音提示；只有该动作的明确取消语义可以撤销它，发射宿主单纯死亡或消失不构成通用取消规则。
_Avoid_: 所有延迟都跟随宿主生命周期、把普通对齐延迟当可取消动作

**音效说明（Cue note）**：
声音提示或最终绑定的强语义中文说明，供人一眼判断它对应什么声音，如“爆裂决斗者技能触发”。
_Avoid_: 只有 cueId 无说明、以代码方法名为说明

**旁路装饰道（bypass lane）**：
纯装饰性表演（受击反馈、额外弹道等）的并行道，与主线并行推进但不占输入互斥锁。
_Avoid_: 装饰表演阻塞主交互、装饰表演抢主线时间轴

**占格权威 vs 几何注册**：
逻辑占格真相永在 Core（BoardModel）；表现侧只持一张"格位→认领者"的几何注册，用于命中与悬停派发；目标合法性由 Flow 在 idle 时对 Core 裁决。
_Avoid_: 表现侧维护独立占格真相、事后 force sync 对账、双真相打架、为图标/货架另建第二张注册表

## 输入门禁与意图收口

**意图收口（IntentIntake）**：
玩家输入进入表演编排的唯一入口；门禁、合法性与互斥在此一处裁决，任何输入路径都不得绕过它。
_Avoid_: 各 Controller 各自判定、Explore 直发 Command 绕过门禁、advisory 与 authoritative 两层门禁并存

**时序互斥（Mainline busy）**：
门禁第一轴：唯一以"主线是否在跑"为准的临时输入互斥真相；凡须阻塞输入的表演都必须持有主线（作为 Step 或外部租约）。主线忙时棋盘动作一律 `Reject`，不缓冲。
_Avoid_: BattleBusy/FieldBusy 各自为政的并行 busy 源、全局 PresentationLocked 布尔当唯一门禁、忙时 latest-wins 缓冲待 flush

**输入所有权（Input ownership）**：
门禁第二轴：某一时刻拥有玩家输入的表面（受保护场地 / 场地覆层 / 棋盘选择模式）；意图被接纳须满足"目标表面 == 当前所有者"。与时序互斥正交。
_Avoid_: Opening/Overlay/BoardSelect 摊平成一袋布尔、用 busy 表达所有权、活性遮盖当成"忙"

**冲动轻点（impatience tap）**：
主线忙时落下的额外点击；作为独立脉冲交给（当前 no-op 的）加速通道，为未来"多点即加速"预留缝。
_Avoid_: 忙时额外点击直接静默吞掉、把加速逻辑塞进缓冲语义

## 场地命中与认领

**表面**：
指针命中仲裁的最小单位；同一时刻屏幕上的每个点至多归属一个表面。表面之间的先后由显式且互不相同的优先级决定，同分属装配错误。表面只回答"点落在哪"，不回答"谁有权收输入"——后者归输入所有权。
_Avoid_: 每个可点对象各算一个表面、靠注册顺序决出平局、用表面优先级表达权限

**场地面**：
九宫格整体作为单一表面；命中它之后再按格位查出认领者。
_Avoid_: 每张卡/每个图标各自注册进命中路由、场地与手牌共用一套逐对象注册表

**格位命中框**：
长在格位锚点上、世界尺寸恒定的命中区域；它属于棋盘几何，与落在其上的任何美术、缩放、卡种无关，且全局常开。
_Avoid_: 卡牌/图标自带碰撞盒、按美术尺寸推导命中区、用启停命中框表达规则

**认领者**：
某一格此刻静止的可点击实体；它向该格声明"点我提交什么意图、悬停我给什么文案"。一格同时只有一个认领者；Avatar 是行动者而非目标，永不认领；飞行、跳跃、拖拽中的对象也不认领。
_Avoid_: 一格多认领者靠优先级打架、后到静默覆盖先到、把认领当成占格真相

**命中恒成功**：
棋盘范围内的点击永远命中某一格并取得认领语义；"这一下不生效"必须表达为一条显式拒绝，而不是没命中。
_Avoid_: 规则不满足时让点击落空、静默吞掉、与手滑不可区分

## Avatar 占格与中心格

**中心格**：
九宫拓扑上的格 5：不参与外圈旋转，也不作为补牌空位。是否被占用与 Avatar 是否站在此处无关。
_Avoid_: Avatar 保留格、用格 5 表示玩家位置、把中心格当成 Avatar 永远所在格

**离巢**：
Avatar 当前占格不是中心格。
_Avoid_: 换位中、玩家在外圈（口语可说，规则与文档用离巢）

**换位**：
Avatar 与一张场上卡互换所在格。专属道具发动时目标仅限正面真怪。
_Avoid_: 与「交换」混称、把换位当成两张非玩家卡互换

**交换**：
两张非 Avatar 的场上卡互换所在格。
_Avoid_: 换位、把玩家当成可被交换选中的卡

**归位倒计时**：
离巢后从换位落地起算、只认 Avatar 占格实际变化的三次计数；到 3 即回到中心格。再打出换位则从新的落地重新起算。
_Avoid_: 卡级移动计数、给 Avatar 挂节奏源、把发动换位的那一次算进这 3 次

## 跑图与房间流程

**跑图（Run）**：
从选定角色到通关或死亡的一整趟游戏；由 3 个层、每层 8 个地图节点构成。进度真相在 Core `RunModel`，表现层壳只读。
_Avoid_: 局（与「战斗节点」混淆）、关卡（过窄）、把壳层 NodeIndex 当进度真相

**层（Floor）**：
跑图的一个大段落，含 8 个地图节点，末节点固定为层主房。每层开局随机绑定一套未用过的主题怪物卡组。
_Avoid_: 关、章节、`ModifierLayer`（那是属性修饰层，同名不同义）

**地图节点（MapNode）**：
层内的一个落脚点，序号 1–8。节点自身不含内容，内容由落在它上面的房间决定；节点序号决定「清关后放出哪一类房间选项」。
_Avoid_: 关卡、房间（房间是节点的具体化）、把节点等同于一场战斗

**房间**：
地图节点的具体化，分战斗房 / 消费房 / 特殊房三族。战斗房要打怪并清关；节点 4（离开）与节点 7（层主房图标战前缓冲）**不进 `InteractionLoop`**，没有发牌与清关判定——节点 4 靠离开图标推进，节点 7 走格进入层主房图标后推进至节点 8 开战。
_Avoid_: 关卡类型、事件、把消费房当成「一种战斗」、层主战前无图标直接开战

**清关**：
完成本战斗节点、放出后续房间图标 / 导航的规则事件。现以击破带「离开」技能的机关卡为**唯一**清关手段；真怪物清零不再自动清关。击破离开机关的那一拍：剩余怪 / 其它机关一并清场且不兑金；场上未拾取道具卡若本节点开局编入的真怪**已全部击破**则按回收价自动兑金移除，否则仅移除不兑金；发牌侧未使用道具仍只移除不兑金；道具卡格内容保留。玩家可在主动击破之前继续留在场地交互（拾取更替、回收丢弃、击杀赚钱等）——这不是另开的拣选相位。
_Avoid_: 场上无怪即清关、有怪残留仍自动卖场上道具、清关自动兑金清空道具卡格、为拣选另开独立相位

**离开机关卡**：
本战斗中打通对局的关键机关卡；持有「门」「魔免」与「离开」等技能。击破后直接完成本次战斗（清关）。
_Avoid_: 靠清光真怪物通关、把离开做成与击破无关的纯 UI 按钮

**门（机关技能）**：
离开机关卡技能：持有者只承受**交战**中玩家出手造成的伤害；道具直伤、遗物/效果伤、其它来源伤害，以及直接移除/放逐类效果一律无效。不阻挡：洗回卡组、翻面；被翻面后玩家仍可通过交互翻回，不构成死局。总原则：交互不得制造死局。
_Avoid_: 玩家侧一切伤害都能拆门、无差别无敌到连交战也打不动、把门+背面惰性叠成无法清关

**魔免（机关技能）**：
离开机关卡技能：持有者不受任何外来「效果」影响（机关卡 / 怪物技能 / 道具效果等）。可被选作目标，但效果管线直接失效（不改 targeting 合法性）。仍可被玩家交战打死；持有者自身效果（门 / 离开 / 魔免）不受影响。
_Avoid_: 在选目标层把离开机关标成非法、挡交战伤害、拆掉自身门/离开挂载

**离开（机关技能）**：
离开机关卡技能：持有者被击破时，直接完成本次战斗（清关）。
_Avoid_: 击破后还要再点离开图标才清关

**离开机关插入**：
默认战斗房：在 `RewardSystem.BuildNodeDeckOptions` 阶段将离开机关卡编入敌侧发牌池，经正常开局发牌/补牌上场；开局洗牌后必落在抽牌堆后半段（下标 ≥ ⌈N/2⌉，0 为下一张）。
层主房（`RoomKind.Boss`）：击破本节点开局编入的层主后洗入，且置顶为下一张补牌。
_Avoid_: 普通房仍按过半击杀才洗入、离开机关随机落在前半段、层主房洗入后再全堆重洗、按已出场怪计数、下取整、无怪即出门、跳过牌堆直接瞬占格、把机关计入 N、为防软锁强制置场、层主房仍按过半出门、局中新生层主冒充开局层主

**常规机关装填**：
正式战斗卡组生成阶段（`RewardSystem.BuildNodeDeckOptions`）开局随机注入三张**常规机关**进敌侧发牌池（无放回、同种子可复现；池不足按池量）；常规机关池只含稀有度 White 的 Trap（滚石/攻甲恢复图腾/倒刺/捕熊陷阱六张），离开机关与技能/遗物专用特殊机关（烈焰/治疗泉/复活石）绝不出现在初始随机三张内——离开机关由同路径的 `AppendOpeningLeaveTrapCard` 单独编入（普通房）。QuickTest `\1–\9` 定向注入叠加于此，`\0` 与正式镜像一致。常规机关仍不算真怪物：无击杀赏金。
_Avoid_: 借用 QuickTest `trapContentIds` 通道注入正式内容、把离开机关/特殊机关放进随机池、同场放回抽到重复三张、把常规机关当真怪计入阈值或赏金

**场地图标**：
落在九宫格上、走上去即触发的非卡牌实体，分**房间图标**（下一步进哪种房）与**导航图标**（离开 / 下楼 / 上楼）。导航图标不进玩法 Catalog。
_Avoid_: 房间按钮、房间卡、把导航图标当 `RoomKind`

**驻留提交**：
Avatar 踩上场地图标后停留满 1 秒即自动提交该图标的动作；期间跳走即取消。计时器在表现侧，到点向 IntentIntake 提交意图——门禁与收口内不得出现壁钟。
_Avoid_: 落地即提交、二次点击确认、把计时写进 IntentIntake 或两轴门禁

**就地选项卡**：
房间内落在格子上、**任意距离点击**即生效的选项实体（主要是卡店服务）；点击它不算九宫格互动，不推进互动计数、不补牌、不旋转。
_Avoid_: 选项 UI、把就地点击当成九宫格互动、要求正交相邻才能点

**简要解释**：
悬停当前格的认领者时，在场景对象 `Panels/简要解释文字框` 里显示的一句话说明；无认领者即空。悬停与点击**同源**——同一个认领者既给文案也给意图。它与**卡面基础描述**、**详细描述**是三样东西——后两者只服务战斗内卡牌检视。
_Avoid_: 复用右键详述面板讲房间、把简要解释写进卡面槽、恢复已退役的动态描述 HUD 管道、悬停与点击各走一套命中

**简要解释内容槽的停泊位互斥（已废止）**：
旧约束曾规定简要解释内容槽与道具卡格占用同一停泊位、不可同时在场。描述已迁到不冲突位置；二者可共存。本条仅作废止记录，勿再当现行规则。
_Avoid_: 恢复「描述区与道具卡格互斥」、战斗内隐藏简要解释仅因占位冲突

**主题怪物卡组**：
一套 5 张、按序列 1–5 排列的怪物卡集合；**每层开局**从**本层难度档**随机绑定一套未用过的（普通→第 1 层、中等→第 2 层、困难→第 3 层；`monster_decks.json` 的 `deck_kind`）。`deck.*` 主题英文是历史残留不透明主键（[ADR-0014](docs/adr/0014-theme-ids-are-legacy-opaque.md)），不是玩家叙事阵营。七套正式卡组的稳定槽位契约见 `ThemeDeckStableMapping`（#127 / [ADR-0029](docs/adr/0029-content-guardrail-stable-theme-deck-mapping.md)）；难度档契约见 `ThemeDeckFloorTierMapping`（[ADR-0022](docs/adr/0022-node-loadout-model.md)）。
_Avoid_: 把 `Unknown` 卡组放进正式层池、同一套卡组在多层复用（用尽后同层档内才允许回退复用）

**怪物层数数值叠加**：
相对第 1 层 JSON 基准：**每层中段（节点 4 起，含 Boss 战）** 攻击 +1、血量 +2；**每打完一层 Boss 进新层** 再叠攻击 +1、血量 +2（即每层包含中段与打完 Boss 两档，`MonsterFloorStatScaling` 档数 `(floor-1)*2 + (nodeIndex>=3?1:0)`）。困难选人档（`RunModel.DifficultyId=hard`）将档位加成翻倍为 +2/+4；普通/进阶不变。在 `ContentSystem.CreateDraft` 按 `RunModel.Floor` / `NodeIndex` / 难度注入，对任意主题卡组生效。
_Avoid_: 在卡面 JSON 里手填层数加成、仅部分卡组享受叠加、把主题 deck 静态 `faceBackground` 当环境权威

**地下城环境变体**：
跑图内怪物卡面背景与大楼层文案由 **层号 + 层内房间 1–8 + 困难档** 权威（[ADR-0053](docs/adr/0053-dungeon-environment-from-run-progress.md)）：前四/后四节点切前段/后段环境名；困难整层血色。`GroundPanel ` / MainBG / 九宫格面**暂用场景静态配色**，运行时动态接线已断开（[ADR-0055](docs/adr/0055-venue-board-visuals-static-fallback.md)）。作者真源 `tables/dungeon_environments.json`（表现层配置器侧栏「地下城虚构」）；`GetCurrentDungeonEnvironmentQuery` / `DungeonEnvironmentCatalog.Resolve` 为读侧。怪物 JSON `faceBackground` 运行时由 `CoreCardPresentationMapper` 覆盖，非内容权威。
_Avoid_: 用全局节点号或主题 deckId 切环境、用 deck 批量脚本写怪物 `faceBackground`、把未入切换表的 PNG 变体硬接进表

**序列**：
怪物卡在其主题卡组内的梯队位次（1–5），序列 5 即该套的层主。节点规则表按序列规定各抽几张。七套每套五个序列槽位的稳定 contentId 契约见 `ThemeDeckStableMapping`（#127 / ADR-0029）。
_Avoid_: 等级（`level` 只区分普通 / 层主）、稀有度、用 `level1/2/3_min/max` 表达 5 档

**玩家侧卡组**：
一个战斗节点开局按规则**重新生成**、用于发进场地的道具卡集合：**固定卡占容量预算格** + 剩余容量从来源池随机 + 房间注入（不占容量）；与**道具卡格**（跑图内持续持有）是两个容器。卡店「固定」与「扩容」互斥权衡：多固定每关可控但总量少，多扩容总量多但随机。随机来源池只含**常规稀有度三档**（白=高/蓝=中/金=低，[ADR-0033](docs/adr/0033-help-card-rarity-distribution-control.md) 修订）道具卡，随机装填按**高60/中30/低10**加权、同档内均匀；**特殊**（红）不进随机抽取，只经商店固定货架、战斗房注入（宝箱房→宝箱卡、金币房→金币卡、属性房→属性卡）、精英击杀奖池、层主击杀固定注入等定向渠道进入玩家侧卡组。存档/跨层恢复时来源池与固定卡按常规档过滤、道具卡格只放行 live 道具卡（归档卡一律拦截）。关卡结束时，未入手、仍在场地/发牌侧的未使用道具如何处理见清关规则——道具卡格内的卡不因清关被清。属性房与其它战斗房相同：开局按 `OpeningInjects` 加权自动注入属性卡（如 2 张 40/40/20，可重复），进房直接开战（[ADR-0031](docs/adr/0031-attribute-room-pick-two.md) 三选二会话已废止）。
_Avoid_: 持久帮助卡栈（`HelpCardStacks` 已退役）、把玩家侧卡组当成跨关背包、与道具卡格混为一谈、属性房仍走进房三选二会话

**携带卡包（已退役）**：
旧模型里商店/奖励产出的暂存容器，下战斗节点开局再倒入。现已退役：购入与奖励产出一律直写**道具卡格**，满则拒买/拒领。
_Avoid_: 恢复携带卡包、买到却不在道具卡格的第二持有真相

**道具卡格**：
存放已拾取 / 已购入道具卡的格位区。卡一旦进入即进入**单趟跑图内**的持续持有域——不因清关、节点切换或自动结算被清掉；玩家主动丢弃、使用（含**非战斗可用**卡在非战斗相位使用，见 ADR-0032）是主要离手路径。跑图结束（通关/死亡）后不跨局保留。规范词是「道具卡格」；「手牌」仅为表现侧口语别名。不另设「留存卡」或跨局 meta 背包。
_Avoid_: 把手牌当另一套持有区、另立留存卡/跨局 meta 背包、清关自动兑金清空道具卡格、与携带卡包混为一谈

**道具卡格容量**：
道具卡格可同时持有的格位数；初始 2，最高 5。与**玩家侧卡组容量**（每战斗重生成张数）是两套独立数字。
_Avoid_: 与玩家侧卡组容量共用一个字段、扩容超过 5

**道具牌格升级**：
商店房内的就地选项：花 **50** 金币将**道具卡格容量** +1（每级同价）；达到上限 5 后该选项不再显示。与卡店「提升玩家侧卡组容量」无关。
_Avoid_: 靠货架 RNG 刷到才升级、与玩家侧卡组扩容共用同一服务、满级后仍占格可点

**道具卡回收**：
玩家将道具卡格中的卡拖入回收区（`HandcardRecycleZone`；拖动时激活 `CardRecycleNotice`）并确认离手；每张固定 +10 金币。道具卡格可见的流程段均可发起；主线 busy 时拒收。
_Avoid_: 忙碌时打回导致「一会能收一会不能」、清关自动兑金替代主动回收、把遗物丢弃并进 `RecycleItemSlot` 命令

**遗物丢弃**：
玩家将装备栏遗物图标拖入**同一**回收区并确认离手；每件固定 +20 金币（`DiscardRelic`）。拖动时槽位图标隐藏但不改布局；右键只开详述，不丢弃。
_Avoid_: 右键直接丢弃、另做一套回收 UI、与道具回收共用 Core 命令

**道具卡格满拒入**：
道具卡格已无空位时，场上拾取、商店购买、奖励房领取等一切「写入道具卡格」的动作直接拒绝；本阶段不依赖简要解释提示（后续可另做满格表述）。玩家须先回收腾位。
_Avoid_: 自动挤掉旧卡、满格仍写入、把满格提示绑死在简要解释上

**玩家侧卡组容量**：
每个战斗节点开局按规则重生成、发进场地用的道具卡张数上限（现行默认 6，可另有扩容）。不决定道具卡格能持几张。
_Avoid_: 与道具卡格容量混用、用它限制商店购买

**手牌（口语别名）**：
表现侧对「道具卡格」的口语叫法；领域规则与文档一律写「道具卡格」。
_Avoid_: 在 ADR / CONTEXT / Spec 里用「手牌」当规范词

**非战斗可用**：
道具卡 JSON 上的卡级声明（`usableOutsideBattle`，缺省 false=战斗限定）；标 true 的卡在非战斗相位（选房 RoomChoice / 房内会话 RewardItemChoice）可拖到棋盘空位使用，与战斗内同一链路、同样消耗；RoomEvent 瞬态相位不放行。与**效果责任自陈**正交：requires 声明「效果适用场景」，本声明管辖「卡的使用相位」。护甲类效果因当前护甲每节点重置，跨不进下一战，v1 不标注。
_Avoid_: 全道具卡一刀切、把声明粒度放模板、把非战斗使用当成新交互相位、护甲重置语义未定前把护甲卡标 true

## 怪物行动与交战

**九宫格互动**：
玩家对棋盘的一次操作——与正交相邻怪物交战、拾取正交相邻帮助卡、点击正交相邻空格；每次互动推进**行动计数**通道（仅卡级节奏源为行动计数的卡扣减共享倒计时）。道具使用不是互动。
_Avoid_: 回合、一步、点击（过宽）、把道具使用算作互动、默认推进全场所有怪的倒计时（须看卡级节奏源）

**行动计数**：
内核级节奏通道之一：由九宫格互动推进。卡只能把卡级节奏源绑到本通道或移动计数二者之一。详见 [ADR-0038](docs/adr/0038-card-rhythm-dual-channel.md)。
_Avoid_: 行动技术、回合、把遗物「用了几张道具」等非节奏累计并进本通道

**移动计数**：
内核级节奏通道之一：本卡在场地格之间任意换位一次推进一次（手牌/卡组移动不计；不限旋转，任意盘面换格皆算）。只服务于有卡级节奏源的场上卡（怪物/机关等），不是 Avatar 离巢后的回家倒数。详见 [ADR-0038](docs/adr/0038-card-rhythm-dual-channel.md)。
_Avoid_: 移动技术、仅旋转移动、场上全局移动总和、把手牌/卡组位移算进来、给 Avatar 挂卡级节奏源来倒数回家

**卡级节奏源**：
该卡出生时二选一绑定的节奏通道（行动计数或移动计数）；同卡攻击模式与技能同步触发共用此源。
_Avoid_: 技能各自选源、模式与技能分绑不同通道

**节奏周期**：
卡级配置的正整数 X；进场时卡级共享倒计时置为 X。频率不再写死在攻击模式枚举或同步技能模板的 `every` 上。
_Avoid_: 模式表内建 3/5、技能模板硬编码每 N 次、静默沿用旧频率

**卡级共享倒计时**：
该卡距下一次开火窗口还需经历的通道推进次数（「还差几次」）；攻击模式与技能同步触发共用同一个剩余值。卡面「行动计数」数值槽显示的就是它。
_Avoid_: 模式与技能各一套计数值互相偷拍、累计次数、冷却（CD）、用频率减累计再换算

**技能同步触发**：
绑定卡级节奏、在开火窗口与攻击模式同拍触发的技能；模板不再自带节奏 `every`。
_Avoid_: 技能内硬编码每 N 次互动/移动、与攻击模式分轨独立倒计时

**交战**：
玩家发起的、与一张正交相邻怪物之间的互相伤害结算；双方按先手还击裁决出手顺序，死者不再造成伤害。`OnBattle` 仅在交战作用域（Begin…End）内触发。
_Avoid_: 战斗（过宽）、把单向打击当成交战、用「攻击」指代双方的整次结算

**单向打击**：
怪物按自己的攻击模式主动对玩家卡造成的一次伤害；玩家不反击。它与交战是两个概念，不是交战的一半。
_Avoid_: 怪物反击、怪物回合、先攻、把它当成一次交战

**攻击模式**：
每张怪物卡必填的内生几何属性，规定「从哪些格位打得到玩家」（普通近战 / 斜角近战 / 全向近战 / 无）；远程射击由 [[远程武器]] 等技能表达，不是独立攻击模式。出手节奏由卡级节奏源与节奏周期决定，不进效果装配、不写入卡面描述，也不可被运行时效果改写。详见 [ADR-0011](docs/adr/0011-monster-attack-pattern-intrinsic.md)、[ADR-0038](docs/adr/0038-card-rhythm-dual-channel.md)。
_Avoid_: 怪物技能、主动技能、把频率当成模式的一部分、缺省即「无」

**开火窗口**：
卡级共享倒计时归零的那一拍；同拍可触发攻击模式单向打击与全部技能同步触发。窗口是一次性的——攻击模式位置条件不满足即作废本次单向打击并把倒计时重置为节奏周期，不保留蓄力。
_Avoid_: 蓄力、充能满待发、错过后保持归零下拍再试

**敌方行动阶段**：
节奏结算链中执行开火窗口的报名、逐条结算、收尾三段；行动源卡在九宫格互动后报名，移动源卡在盘面换格推进归零后进入等价开火路径。
_Avoid_: 敌方回合、怪物 AI、敌方 tick、把它当成若干条效果的自然触发

**行动名单**：
敌方行动阶段报名时冻结的、本拍开火窗口打开的怪物序列，按进场先后排列；一经冻结不再增删。
_Avoid_: 结算时实时遍历场上怪物、允许中途插队或补录

**盘面冻结**：
敌方行动阶段的报名与逐条结算期间不补牌、不旋转；本阶段累积的全部死亡统一在收尾段处理。它是位置条件在阶段内可预测的前提。
_Avoid_: 每次打击后立即补牌旋转、一次齐射打死三只就连转三格

**位移挂起**：
交战窗与敌方行动阶段（位移锁定窗口）内由效果 DSL 触发的盘面位移（旋转/换位/移格）不即时结算，挂起到收尾锚点（互动计数推进 / 齐射收尾 / 道具链收尾）就地复验后统一落地——交战按玩家点击时的盘面完整结算（先打再转）。详见 [ADR-0044](docs/adr/0044-battle-window-deferred-board-motion.md)。
_Avoid_: 交战中途真实转格、给反击加几何复核、逐内容特判旋转时机

**先手还击**：
交战内部的出手顺序裁决——仅一方持有时该方先出手；它不产生额外攻击，与攻击模式正交。
_Avoid_: 先攻（会被读成「怪物会主动先动手」，那是攻击模式）、把它当成怪物的主动攻击手段

**是否有技能同步触发（图标）**：
怪物卡面子图标：有技能同步触发且攻击模式≠「无」时激活于攻击模式图标下方；仅有同步技能、攻击模式为「无」时，将该图标升格装进攻击模式槽；仅攻击无同步技能时不显示。与行动计数数值槽分离。
_Avoid_: 与行动计数数值混为一谈、无同步技能却常亮、把升格当成第二套数值

## 卡牌表现实体

**卡牌底盘**：
运行时卡牌实体；持有运动塔、排序组、命中代理与效果挂点，不表达某一类对象的专属卡面维度。
_Avoid_: Standard Card、万能卡、一张顶所有

**卡面模板**：
按对象种类区分的卡面与卡背视觉构型；挂在底盘约定的视觉层下，只负责该类对象的可见维度。
_Avoid_: 用完整运行时 Prefab 替代底盘、Sample Card 当生产实体

**遗物卡**：
用于选择、获取，以及后续可能的场地展示的遗物视觉对象；不进入手牌与卡组运动区。对应表现种 `Relic`。
_Avoid_: 遗物进手牌/抽牌堆、把遗物当帮助卡组牌

**玩家卡**：
标准玩家对象的卡面模板；对应表现种 `Avatar`。
_Avoid_: 用玩家技能卡、PlayerSkill 表现链路顶替玩家卡

**怪物卡**：
标准怪物对象的卡面模板；对应表现种 `Monster`。
_Avoid_: 与玩家卡共用数值栏位布局

**机关卡**：
场上可交战、可受伤的非怪物卡（`CardKind.Trap` / 表现种 `Trap`）；进敌侧发牌与占格，但不计真怪物、无击杀赏金，激活时静默挂 `CounterAttackBanned`（[ADR-0017](docs/adr/0017-trap-card-kind-and-dual-bucket.md)）。内容 id `trap.*`，卡组 `deck.trap`。含普通机关与**离开机关卡**等特化。
_Avoid_: 用 `Monster`+`trap.*` 前缀伪装、把机关当帮助卡拾取、清关把残留机关当敌

**道具卡**：
帮助卡与道具对象的统称卡面模板；对应表现种 `HelpCard`、`Item`，以及语义为组牌内容的 `PlayerCard`。
_Avoid_: 把 Avatar 叫成道具卡、把遗物并进道具卡

**玩家技能（已移除 / 已并入遗物）**：
历史上的 Player Skill 卡面与被动持有路径；产品上已正式去除，被动效果已吸收为遗物（`relic.*`，默认 Blue）。开局 Profession 初始遗物为 `relic.rotten_cleave_axe`（腐朽顺劈斧，Red），战士基础护甲 0（#116）。错位旧卡在 `deck.relic_archive`（#115）。怪物技能仍用 `skill.*`。
_Avoid_: 为玩家技能单独做卡面/持有栏/授予原子、在 mapper/Catalog 里继续按 PlayerSkill 分支做新接线

**卡牌表现投影**：
从 Content/结算指令汇成的、面向表现的一整包可读状态；**视觉**与**数值**分属两条路径——视觉可由 Mapper 刷新，数值只经表演锚点上的结算指令赋值。不按卡种裁剪真相。
_Avoid_: 每种卡面各自拉一套独立数据源、卡面自己当状态权威、用 JSON stats 覆盖战中显示值

**卡面消费**：
卡面模板只消费表现投影里自己需要的字段；不需要的不暴露、不绑定，特殊诉求时可另从后台读取而不扩张默认投影。
_Avoid_: 卡面回写 Core、为未使用字段强行占位 UI

**权威值**：
规则内核此刻的真相（含跨战斗持久的运行级修正）；表演开始时一批解算往往已是终值。
_Avoid_: 把权威值直接当成玩家此刻应看到的卡面数字

**卡面显示值**：
玩家在卡面上看到的攻/甲/血等，由结算指令在表演锚点驱动，**有意滞后于权威值**。卡面「甲」= **当前护甲**（本关可消耗；只认当前甲指令，不消费基础甲 `ResultValue`）；玩家信息 HUD「甲」= **有效护甲**（用于读成长上限）。文案用「护甲」，不用「防御」指战斗属性。指令的**发射完整性**由 Core 统一对账缝自动保证（每个动作边界 diff-emit 卡面绝对值提交，[ADR-0045](docs/adr/0045-unified-card-face-stat-projection.md)）；「不对账」纪律只约束表现层。
_Avoid_: 表演中随时直读 Core 拉齐卡面、表现层强制对账抹平滞后、新增数值源时手工补发卡面提交事件（旧 `Append*FaceCommit` 已废除）、HUD/卡面混用「防御」指护甲、用 `ModifyBaseStat(Armor)` 基础值直接写卡面甲

**基础护甲**：
角色/卡牌的静态护甲属性（`StatId.Armor` base）；永久成长与出生值落在这一层。`ModifyBaseStat(Armor)` 在改基础的同时按实际变化量同步 `CurrentArmor` 并发 `ArmorChanged`（详见 [ADR-0028](docs/adr/0028-damage-formula-armor-and-reduction.md)）。
_Avoid_: 把局内临时叠甲写进基础层、用「防御」代替护甲

**有效护甲**：
基础护甲经 Modifier 管线后的值；关卡开始时用于初始化当前护甲。
_Avoid_: 把有效护甲当成可被单次受伤直接扣减的资源

**当前护甲**：
本关可消耗的护甲资源（`StatId.CurrentArmor`）；受伤优先扣除，归零后伤害扣血。关卡开始重置为有效护甲（开战 `AvatarAppeared` 之前）；**无**回合结束清甲。
_Avoid_: 回合清甲、关内把当前甲与基础甲混写、HUD 用当前甲冒充有效甲（或反之）

**伤害减免**：
受击方累加的减伤属性语义：标准公式在护甲吸收前从伤害中减去（多源累加，无来源为 0）。实现为 `RuleId.DamageReduction`，不上卡面三围、不上 HUD。详见 [ADR-0028](docs/adr/0028-damage-formula-armor-and-reduction.md)。
_Avoid_: 与 `DamageFlatDelta` / `DamageMultiplier` 混称、当成护甲的别名、用「防御」指减免

**无视护甲**：
单次伤害实例标记：跳过当前护甲吸收与金甲抵甲伤，全额打血。不是受击方常驻状态。详见 [ADR-0028](docs/adr/0028-damage-formula-armor-and-reduction.md)。
_Avoid_: 做成常驻 Rule、与「穿透」词条未定案机制混写

**结算指令**：
一次解算批次产出的、按结算顺序排列的事实条目（`PresentationBatch.Instructions`）；数值类携带结算后绝对值，卡面赋值不累加。
_Avoid_: 表现层自行推算增量、用 EventLog 旁路代替指令消费驱动 FX/飘字/金币

**表演锚点**：
表演编排在时间轴上声明的语义时刻（命中 / 收尾 / 无表演消费）；剧本报点时排期器把归属该锚点的结算指令交给**第一个认领的处理器**（卡面数值或旁路装饰）。
_Avoid_: 各调用点私自选同步时机、默认沉默「不上卡面」、为装饰另建第二张锚点表

**触发可见因果**：
效果后果上屏之前，其触发条件必须已在场上成为玩家可见事实；同批内核已算完 ≠ 可提前播触发。详见 [ADR-0018](docs/adr/0018-trigger-visible-causality.md)。
_Avoid_: 旋转/位移前抢播脉冲或扣血、把 Batch 指令序当成可见时序

**基础触发表现**：
一次经 `ExecuteEffectAction` 的卡牌触发，在 Impact 时对仍可视在场的效果持有者播的最小可见反馈（v1=缩放）；进阶定向特效另票。详见 [ADR-0018](docs/adr/0018-trigger-visible-causality.md)。
_Avoid_: 只有后果无持有者反馈、用命中帧交战动画冒充非交战触发、遗物/光环/OnActivate 强行套脉冲

**战斗锚点排期器**：
一批结算指令的唯一分发出口；装载非 `None` 归属指令，按报点分发给多个 `IBattleBeatHandler`；Settled 后未消费只报不改。
_Avoid_: 每类表现各建一套排期、装饰 await 进主线 ack

**投影提交（Commit）**：
把表现投影中某一字段或某一对象的可见值推到卡面消费端的动作；卡面**数值**只由战斗锚点排期器经卡面数值处理器触发，不得在队列外直读 Core 刷数值。装饰类表演经同一排期器的其它 `IBattleBeatHandler` 消费（见 ADR-0007），不另开提交出口。
_Avoid_: Sync 直读 Core 写卡面数值、任意 SetAttack/SetHealth 旁路、用 digit animate 代替提交时机、强制对账静默拉回

**牌面朝向**：
卡牌实例在逻辑上的明/暗（正面/背面）状态；可参与攻击许可、受击许可、敌方行动与效果可否触发等规则裁决，权威在 Core，不在显示模式。背面默认**双向惰性**：不可被伤害、背面怪不推进/不开火（`AttackPatternCountdown` 冻结），被动默认不生效；需背面计时的效果走独立 `faceDownTick.*`（`FaceDownTickCounters`）。`ActiveWhileFaceDown` 仅豁免被动惰性。详见 ADR-0016。
_Avoid_: 用 Hand/Deck/Ground 显示模式代替朝向真相、仅把翻牌当 L4 动画、用攻击倒计时兼做翻回计时

**卡面朝向表现**：
卡面模板上正面/背面节点的可见构型；只消费已提交的牌面朝向，不参与规则裁决；翻牌动画由 `CardFaceFlipPresenter` 驱动。
_Avoid_: 卡面本地私自翻面当逻辑状态

**卡面挂载点**：
卡面模板在卡牌底盘变换塔中的固定接入位置（L4 CardVisual 之下）；不得改写 L0–L3 职责或另起与塔并行的视觉根。
_Avoid_: 用卡面模板替换底盘、front/back 拆到不同塔层、为翻牌另建与塔冲突的父级

**卡级排序黑盒**：
卡牌实例之间的前后关系仍由底盘根 SortingGroup 与既有 DisplayMode/飞行抬序逻辑独占；卡面模板只配置组内相对 order。
_Avoid_: 卡面自挂第二套 SortingGroup、卡面改写卡级 sortingOrder、取消卡级 SG 改绝对序

**通层排序值**：
同一卡面排序作用域内（同一 SortingLayer、同一卡级 SG 子树）出现重复的 sortingOrder；属装配错误，编辑器校验须告警。
_Avoid_: 靠不稳定的同序叠加碰运气、前后端各写一套绝对序表

**卡面可装配槽**：
卡面模板上允许被内容/编辑器替换的视觉挂点（图标、背景、边框、横幅等）；接入后原则上模板内全部内容槽均可装配替换。
_Avoid_: 写死在预制体里不可替换的关键立绘、只改运行时改不了编辑器

**默认装配优先级**：
在未另行指定时，内容装配先替换各模板的主图标（Main Icon），其次替换卡面背景（若该模板有背景槽）。
_Avoid_: 先改边框/装饰再改主识别物、无优先级的随意覆盖

**卡面最终态显示**：
预览与运行时展示的是对象卡牌装配完成后的完整可见状态；不因编辑器收纳次要槽而隐藏最终画面。
_Avoid_: 预览只画主图标、收纳槽等于不渲染

**直接暴露装配项**：
编辑器默认展开、供高频替换的装配入口；与卡面装配槽为同一槽表中的标记子集。初版直接暴露含主图标、卡面背景与卡背相关槽；其余默认收纳；图标呈现属性（大小/颜色等）进收纳子栏。
_Avoid_: 每张卡默认摊开全部槽位、把卡背排除在直接管理之外、贴图装配与描述图标分两套系统

**卡背装配**：
卡背归**卡组**所有：同一卡组的卡共用一套卡背，个别特殊卡（Boss、诅咒卡等）可在卡上覆写。卡组未配置时回退到该卡面模板预制体上的兜底卡背，不设全局统一卡背源。预览为分层重叠后的最终态。直接暴露字段对齐模板三件套：背框、背纹、logo。
_Avoid_: 逐卡各配一套卡背、四套模板共用一个全局卡背、缺省时找全局默认背图、另建平行卡背 Catalog

**卡面模板表**：
由表现种（Kind）映射到卡面模板预制体的显式表；Spawn 时先实例化卡牌底盘，再按表将对应卡面挂入 L4。DefId 级预制体注册仅作特例覆盖。
_Avoid_: 为每种卡复制完整底盘、仅靠 DefId 前缀猜测模板

**卡牌底盘宿主**：
挂在卡牌底盘根上的薄视图组件（现名可仍为 `StandardCardView`）；负责 SortingGroup、卡面挂载与把已提交投影分发给当前卡面 Binder，不再充当全能数值/贴图 API。类型名可暂留以降低爆破面，职责按底盘宿主理解。
_Avoid_: 继续用上帝 SetAttack/SetCardFace 当正式通路、为每种卡面各挂一套根 View

**卡面基础描述（检查描述）**：
写在卡面 JSON（`description`）上的**人手写卡级概括**；一句话说清整张卡，多效果时由人负责精炼。节奏类统一「每〈事件〉N〈单位〉 / …次后」句式（单位可保留次/张/只/点）。数值只许 `{装配id.键}` 装配参数引用（初始配置插值）；文案本身不随战中剩余次数跳变。右键检查永远展示这一套。硬上限见「描述格」。
_Avoid_: 简单式 `{value}`/`{amount}`、`{卡defId.键}` 前缀式、把基础描述当局内剩余次数句、用效果描述拼接充当卡级概括、恢复已退役悬停动态 HUD、描述里写死资源路径

**卡面介绍**：
写在卡面 JSON（`faceIntro`）上的人手风味短句；纯跟卡面绑定，不进玩法 Catalog；以简洁有趣为先，不承担规则精确说明。硬上限同「描述格」（26）。与卡面基础描述、局内描述投影三者分立，不得互相顶替。
_Avoid_: 把规则数值句塞进介绍、用介绍顶替检查描述或局内投影、按效果模板另写一份与装配脱节的长文

**局内描述投影**：
已接线卡在实例可见表面及店/奖/鉴预览上的描述层；自阶段一改造起与**检查描述恒同文**（`CardFaceDescriptionProjector` 只填 `description` + 装配初始实参，永不消费已提交倒计时剩余）。**例外**：HelpCard 的 `{装配id.amount}` 印刷数值允许直读 run 级 `PlayerModel.ItemStatBonus`（`HelpCardMagnitudeOverlay`，与结算同口径）。效果倒计时剩余改走专用 UI：**机关** → 卡面 `ActionCount` 槽（`ShowActionCount`）；**遗物** → 遗物栏图标左下角「计数」TMP（`RelicHudHook`）。`projectKey` + Settled 提交纪律不变（键为完整 `装配id.键`），禁止 View 直读内核计数器。JSON 字段 `liveTemplate` 已退役。
_Avoid_: 在检查描述上加「（还剩 X）」括号、描述随剩余跳变、直读 Core 计数器、恢复 `liveTemplate` 契约

**装配参数引用**：
描述中指向「某条装配实例 × 某个参数键」的唯一写法 `{装配id.键}`；每条装配须有稳定 `id`。凡玩家可见数字（含叙事永不改的数）都须进入装配实参栏；叙事参数值可与文案预期相同，但不得系统外写死。`templateId` 仅作者辅助显示，不作解析权威。
_Avoid_: 简单式同键首个装配、defId 前缀歧义匹配、用 templateId 当描述解析主键、参数栏缺失却在文案写死数字

**描述格**：
卡面基础描述、卡面介绍共用的长度计量：硬上限 26 格；普通字符各算 1 格；每个数值代码块（`{…}`）算 1 格；每个图标富文本块（`[…]`）与每个文字词条块（`[[…]]`）各算 1 格。
_Avoid_: 按 UTF-16/字节数另算、代码块按展开后数字位数计价、把 `[[…]]` 拆成两个 `[…]` 计格

**详细描述**：
供右键（或等价）打开的详述面板内容；检查侧以**卡面基础描述**为人手概括源（静态）。效果信息区只展示**词条行**（多行 ScrollView）：基础填充 = 检查描述里 `[[展示名]]` 抽出的词条；追加 = 卡级词条配置栏位（JSON `extraGlossaryTerms`）搜索词条表额外选入的**隐藏词条**——仅右键详情多展，不占卡面描述，与描述词条去重，删描述词条后保存刷新自动同步消失。图标词条默认隐藏，仅在详述卡面预览上 hover 时填入**首行**临时解释槽——覆盖描述内联 `[code]` 图标与卡面机制图标（攻击 / 护甲 / 血量、倒计时、攻击模式、技能同步触发）。不拼接效果 `design_text`；不把局内剩余次数写进检查面板。（[ADR-0037](docs/adr/0037-inspect-detail-is-glossary-rows.md)）
_Avoid_: 用悬停描述顶替详细描述面板、把详述长文塞进卡面基础描述、详述再堆效果原子文案、检查面板展示局内倒计时剩余、把图标词条常驻堆进词条栏、hover 解释槽挂在列表末尾、把「常规怪物」这类隐藏词条堆进每张卡的检查描述

**卡面装配槽**：
卡面装配系统中的唯一槽表条目；以代码内部代号标识（如 `Action_Icon`、`Main_Icon`），编辑器悬停显示中文注释（如行动图标/攻击图标）。图标槽可装配 Sprite，缺省回退到该卡面源模板自带图标；数值槽缺省统一为 0。
_Avoid_: 中文当主键、描述写资源路径、缺省找全局图、缺省数值用 -1 或隐藏当「无」

**描述方括号引用**：
描述中两种括号语法：① **`[[展示名]]`** 显式文字词条——按词条表名字精确匹配，卡面去括号显示（可着色），右键详情默认展开介绍；② **`[code]`** 内联图标——装配 Insertable 槽（如 `[Action_Icon]`）只由高层装配下行，其余走项目级词条表代号 → sprite；卡面渲染 TMP sprite，详情默认不展开，仅 Inspect 预览 hover 解释（卡面机制图标同样 hover 解释，靶表见 `CardFaceIconGlossaryTargets`）。词条表（`CardFaceDescriptionIconCatalog`：名字 + 介绍 + 可选颜色 + 可选代号/图标 + 分区）全局共用。内联布局仍由 `CardFaceDescriptionInlineIconStyle` 按代号管理。编辑入口：「NineGrid/表现层配置 → 词条」。（[ADR-0037](docs/adr/0037-inspect-detail-is-glossary-rows.md)）
_Avoid_: 用词条表覆盖 `Main_Icon`/`Action_Icon`、占用装配槽保留代号、把 `[[…]]` 当图标、详情再堆 `design_text`、表情字符、裸路径、把内联布局或词条映射写进单卡 JSON

**装配槽注册表**：
项目级 ScriptableObject，登记全部卡面装配槽代号、中文注释与槽角色（直接暴露/收纳/可插入描述/数值等）；卡面模板选用并绑定节点，内容条目按代号填值。
_Avoid_: 每模板私自发明代号、仅靠预制体字段名当全局契约、初版不用注册表

**卡面首波回填**：
新卡面系统接入后，运行时优先从既有装配/内容数据回填主图标、名字与数值；描述、行动计数等未接线槽口走模板默认（数值默认 0），其余槽待后续装配。目标是可玩且能从卡面区分不同卡牌效果。
_Avoid_: 首波强求描述/回合计数接线、首波清空老素材重做图

## 卡牌内容装配

**效果模板**：
不绑定任何具体卡的一类效果诉求；描述「做什么」与「需要哪些参数」；JSON 同构，可被帮助卡、遗物、怪物技能、机关技能共同引用。编辑器默认按卡种只展示/多载同类模板（道具效果 / 遗物效果 / 怪物技能效果 / 机关技能效果）。
_Avoid_: 按卡命名的一次性效果（`help.fireball.use` 式）、逻辑相同却各写一条、把容器种类写进模板身份、编辑器跨类乱挂当默认流程

**装配引用**：
卡上的一条条目，指明引用哪个效果模板并给出实参；一张卡可有多条。卡的全部信息（身份、表现引用、数值、装配引用、分类）在同一个文件里看得全。
_Avoid_: 参数外置到数值表再靠 id 间接、模板带默认值让卡文件看不出实际数值、把 DSL 内联进卡以致复用退化成复制粘贴

**衍生卡**：
一张卡的效果装配中 `Spawn` 动作（模板 `body` 里 `action.atom == "Spawn"` 的 `defId`）指向的另一张卡，即「这张卡会涉及到的卡」（亡语链：死亡召唤/死亡之主 → 复活石、复活石 → 巨剑骷髅；遗物教学：血液冲击波 → 撞击等）。解析权威在 Content 层 `DerivedCardResolver`（静态投影 + 缓存，随内容重载失效，[ADR-0052](docs/adr/0052-derived-card-inspect-navigation.md)）。右键详述面板两态 BG 各有「衍生卡查看按钮」：无衍生卡文案「衍生卡：无」，有则动态填卡名；点击入栈切入衍生卡详情，关闭/半黑屏退出时栈式返回母卡；衍生链可能成环（历史上复活石 ↔ 巨斧骷髅 互相衍生）由导航栈查重防环。
_Avoid_: 用 `effectIds` 字段推导（它只是装配 id 的冗余声明）、把 `skillIds` 递归展开（当前正式内容无此链）、在表现层硬编码某几张卡的关系表

**效果责任自陈**：
效果自己声明适用的场景、对象与条件；享有多大权利（产生多大影响），就承担多大责任（声明多严的适用范围）。不存在按场域拦截的外部门禁；同一效果不得因上下文呈现两种形态，若有则拆成两个效果。
_Avoid_: `EffectOwnerScopeGate` 式外部风控、`ownerOnly` 这类上下文开关参数、靠默认值隐式表达适用范围、装配非法却静默失效

**适用声明（requires）**：
自陈中「我适用于什么场景」的部分——需场上实体 / 需被使用 / 需处于某区域。不成立属**装配错误**，诊断级别高于玩法条件。
_Avoid_: 与玩法条件混写一块、不成立时静默跳过不留痕

**玩法条件（conditions）**：
自陈中「此刻是否满足」的部分——血量、格位、计数等。不成立属**正常玩法**，不是错误。
_Avoid_: 用它表达适用范围、让探查手段无法与装配错误分诊

**词条**：
项目级共享的玩家可读机制语汇（约百条量级），如「灼烧」「护甲」「伤害减免」；名字 + 详细介绍 + 可选颜色；可选代号/图标供 `[code]`。检查描述用 `[[名字]]` 点名后右键详情默认展开；纯图标词条默认隐藏、Inspect hover 才解释。按认知分区归类，单一分区 + `Others` 兜底。「穿透」若作词条须先有机制定案，勿与「无视护甲」未加区分地混用。
_Avoid_: 一模板一词条导致共享机制解释重复、把 75 个 DSL 原子名当玩家词条、用代号当 `[[…]]` 匹配键、用「防御」当护甲词条名

**卡组**：
卡的视觉归属单位，决定卡背；怪物的卡组兼任遭遇编排单位，回答「什么时候会遇到你」。玩家一组、帮助卡一组、遗物一组（live `deck.relic`；错位旧卡归档 `deck.relic_archive`，不进奖池/授予接线，#115）、**机关一组（`deck.trap`）**，怪物按所属阵营分组；不存在统称的「怪物卡组」。`deckId` 主题英文（如 `deck.dragon`）是内部渠道代号与历史残留命名，不是玩家叙事阵营；玩家可见称呼用 `displayName` / 另行映射（[ADR-0014](docs/adr/0014-theme-ids-are-legacy-opaque.md)）。
_Avoid_: 把功能角色塞进卡组、为帮助卡编造阵营归属、逐卡各配卡背、把 `deck.*` 主题英文当成玩家叙事阵营或现行世界观

**归档卡组 / 非正式接线（表现层编辑器 + 运行时门禁）**：
表现层卡牌配置器侧栏按所属卡组分组时，**卡组显示名含「归档」二字**的组（如「遗物归档卡组」「道具卡归档卡组」「怪物归档卡组」）及其成员**不属于正式接线**——保留 JSON 供参考或效果直生，不进奖池/授予/正式遭遇抽选。代码侧对应 `deck.relic_archive` / `deck.help_archive` / `deck.transition`、**`deck.ai_expansion`（AI 拓展设计卡组）**与怪物 `isReserve`。运行时统一门禁 `FormalContentWiring`（[ADR-0054](docs/adr/0054-unofficial-content-never-enters-live-pools.md)）：随机池排除非正式+Reserve，具名 Spawn/授予排除非正式。批量导出/卫生校验与编辑器过滤与此对齐（`CardPresentationPrimaryCardRules`）。
_Avoid_: 把归档组成员当 live 内容批量改写、按归档卡组做正式投放或奖池展开、只在单个装填入口补排除而让 `ShuffleRandomContent` 继续扫全 Catalog

**主要内容卡牌**：
表现层一卡一文件 JSON 中承担玩法与卡面主体、适合批量填充描述的四类：`Monster`（怪物卡）、`Relic`（遗物卡）、`HelpCard`（道具卡）、`Trap`（机关卡）。与之相对：Skill / Deck / Room / ChoiceOption / Avatar 等为技能容器、卡组元数据、房间选项等，**不算**主要内容卡牌。批量描述导出默认只收上述四类且排除归档成员。
_Avoid_: 把 Skill JSON 或房间选项混进「全卡描述」导出、把归档卡组成员当主要内容批量改写

**功能角色**：
帮助卡与遗物的粗粒度分类轴（攻击 / 防御 / 功能；代码 `ContentRole`），回答「你在玩家 build 里干什么活」；桶数少而稳定，用于投放均衡规则（如 `BalanceMinDefense`）。**「防御」在此仅指投放粗桶，不是战斗属性，也不等于护甲或伤害减免**（战斗语义见上文护甲词条与 [ADR-0028](docs/adr/0028-damage-formula-armor-and-reduction.md)）。
_Avoid_: 用细标签做均衡（规则退化成一长串枚举，加卡就改规则）、与卡组混为一轴、把 `ContentRole.Defense` 读成「有护甲/有减伤」

**细标签**：
可增长的多值风味/检索标签（直伤、恢复、经济、位移…），供百科检索、词条关联与未来的协同 build；不承担投放均衡职责。
_Avoid_: 中文当主键、把它当粗轴用
