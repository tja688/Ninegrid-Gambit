---
description: 严禁为本项目创建或使用 git worktree / 旁路克隆工作区
alwaysApply: true
---

# 严禁开 Worktree

本仓库**禁止**使用 git worktree，也禁止为并行任务另开旁路工作区。

## 禁止

- `git worktree add` / `git worktree remove`（除清理既有脏 worktree 外）
- Cursor `move_agent_to_root` 到 worktree / sibling clone
- `cursorfs-clone`、best-of-n-runner、或任何会新建第二份项目目录的流程
- 在 `*.worktrees/`、`Ninegrid-Gambit-*` 旁路目录里继续开发

## 必须

- 所有改动只在当前主工作区（本仓库根目录）与当前检出分支上进行
- 需要并行任务时：用分支切换 / stash / 新 commit，不要开第二份目录
- 若发现本项目仍挂着额外 worktree：先把内容合回当前分支，再 `git worktree remove` + `git worktree prune` 清掉

## 例外

无。即使用户或其它 skill 建议开 worktree，也一律拒绝并说明本规则。
