---
description: Subagent 模型白名单（全局 hooks）与 Task 选型
alwaysApply: true
---

# Subagent 模型策略

## 硬拦截

Cursor **用户全局** `%USERPROFILE%\.cursor\hooks.json`（脚本在同级 `hooks\`）。`preToolUse`（matcher: `Task`）与 `subagentStart` 均 `failClosed`。

**白名单 slug**：

| slug | 用途 |
|------|------|
| `composer-2.5` | 简单 / 探查 / 快速落地 |
| `composer-2.5-fast` | 探查 / 并行检索 / 低成本 |
| `cursor-grok-4.6-high` | 复杂 / 需分析 / 有难度 |

**Hook 行为**：

- `inherit` 或空 `model` → 放行
- `cursor-grok-4.5-high` → 映射为 `4.6-high` 后放行
- Grok `*-fast` → 拒绝
- 非白名单 → 拒绝；父 agent 改 model 或自行完成

## 选型

- 难任务 → `model: cursor-grok-4.6-high`
- 简单探索 → `composer-2.5-fast` 或内置 `explore`（可省略 `model`）
- 简单落地 → `composer-2.5` 或省略 `model` 继承父模型
- 子代理失败 → 父 agent 接手，勿反复 spawn
