---
description: TableNine 项目全局约束（文件保护、提交、Core 分层、Inspector 装配注释）
alwaysApply: true
---

# TableNine 项目规则

## 文件保护规则

- **严禁改动 .unity 文件**：在任何情况、以任何理由直接修改 `.unity` 后缀的场景文件。相关改动需求应通过 Unity MCP 进行落地，不得手动或通过文本方式编辑 `.unity` 文件。

## 工程提交规则

- 每次开发任务完成后，必须全量进行 **git 提交**；提交信息简洁概括目的。
- 提交 git **前**，必须先执行一次 Unity MCP 的 `refresh_unity` 刷新动作，并检查 Console 是否有新增错误。若 Unity MCP 未连接或不可用，则跳过此步。

## 代码维护规则

- 所有内核 core 的代码维护和开发必须使用 **框架**：QFramework。
- 所有非内核 core 的代码维护和开发不可使用 **框架**：QFramework。
- **框架**：QFramework 为内核专属，整个内核被视为黑盒，开发、维护有专门的技能支持；除非明确要求，不随手改 core。在开发、维护其他层代码时，除非明确要求，否则不关注 core 实现，节省上下文。

## Inspector 装配注释约定

凡 `MonoBehaviour` 上**暴露在外部、需在 Inspector 手动拖拽/装配**的序列化字段（`[SerializeField]`、`public` 字段等），**必须**加 `[Tooltip("…")]`，使鼠标悬停即可看到说明。

- **手动装配**：说明用途、装配对象/路径、留空时的行为（若会报错也要写清）。
- **运行时自动装配**：注释中**必须注明**「运行时自动查找/装配」，并简要说明查找方式（如 `Awake` 中 `GetComponent`、`Find`、`transform.Find` 等）及留空是否可接受。