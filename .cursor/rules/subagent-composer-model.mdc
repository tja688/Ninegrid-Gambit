---
description: 子代理（Task/subagent）固定使用 Composer 2.5，禁止擅自切换为 GPT/Claude/Grok 等模型
alwaysApply: true
---

# 子代理模型约束

在本项目中，所有子代理必须使用 **Composer 2.5**（非 fast 变体）。

## 必须遵守

- 调用 `Task` 工具 spawn 子代理时：
  - **优先**：不传 `model` 参数，继承父 Agent 的 Composer 2.5。
  - **或**：显式传 `model: "composer-2.5"`。
- **禁止**在 `Task` 的 `model` 参数中使用 `gpt-*`、`claude-*`、`grok-*` 或其他非 Composer 模型，除非用户在当前对话中明确要求。
- 并行子代理（如 code-review 的 Standards / Spec 双轴）同样适用，不得为「省钱」或「分工」擅自换模型。
- 自定义子代理（`.cursor/agents/*.md`）的 frontmatter 应写 `model: composer-2.5[fast=false]`。

## 示例

```text
# ✅ 正确：继承父模型
Task({ subagent_type: "generalPurpose", prompt: "..." })

# ✅ 正确：显式指定
Task({ subagent_type: "generalPurpose", model: "composer-2.5", prompt: "..." })

# ❌ 错误：未经用户要求使用其他模型
Task({ subagent_type: "generalPurpose", model: "gpt-5.6-terra-medium", prompt: "..." })
```
