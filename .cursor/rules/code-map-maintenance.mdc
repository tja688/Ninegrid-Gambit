---
description: Code Map 与 ADR 维护义务；改结构/验证时必读
alwaysApply: true
---

# Code Map 维护规范

权威导航：[`docs/code-map/`](../../docs/code-map/) + [`docs/adr/`](../../docs/adr/) + [`CONTEXT.md`](../../CONTEXT.md)。入口 [`docs/code-map/README.md`](../../docs/code-map/README.md)。

## 权威层级

| 层级 | 路径 | 角色 |
|------|------|------|
| 代码现状 | `docs/code-map/` | 程序集、目录、装配、验证——**只写当下事实** |
| 长期决策 | `docs/adr/`、`CONTEXT.md` | 行为不变量、领域词汇 |
| 过程笔记 | `Assets/Notes/` | 复盘与 Spec 草稿，**非权威** |

## 须同步 code-map 的改动

改下列内容时，**先改或同 PR 改** `docs/code-map/`，再改业务代码：

- `NineGrid.Presentation` 目录/程序集/场景装配（`Setup/`、`PresentationCompositionRoot`、`PresentationSceneBindings`）
- 新增/删改 Controller、System、Command、Query，或变更 Command / Query / Event / IntentIntake 范式
- 静态 Hook 矩阵、`*ManagerSingleton` 壳、效果扩展点

| 改什么 | 改哪个文件 |
|--------|------------|
| 程序集、场景入口、维护约定 | `docs/code-map/README.md` |
| Presentation 目录、装配、扩展点 | `docs/code-map/presentation.md` |
| 验证门槛 | `docs/code-map/tests.md` |

## 须写 ADR 的改动

Batch-ack、占格权威、卡面 Commit、IntentIntake 两轴、跨层通信禁令等**长期不变量**——新增/修订 `docs/adr/` 并在 code-map 引用编号。

## 验证与两击放弃

硬要求：`recompile` 后 Console 无**本改动导致**的新增 Error / Exception / Assert；其余按需 Play / QuickTest。完整门槛见 [`docs/code-map/tests.md`](../../docs/code-map/tests.md)。

**两击放弃**：同一验证动作（recompile / 查 Console）2 次仍无果即停，不换命令绕路；汇报改动并建议人手验证。

## 禁止

- 把未落地 Spec 目标树写入 code-map 当既成事实
- 新增业务静态 Sink，或 View 直接改 Core 规则状态（见 `presentation.md`）
- 仅改 `Assets/Notes/` 却声称架构已变更
- 从 git 历史恢复已清空的旧测试套件（除非用户明确要求）
