---
description: Unity 协作（MCP 首选、CLI 回退）
alwaysApply: true
---

# Unity 协作规范

与 Editor 协作**优先 Unity MCP**（`mcpforunity://` 资源 + `manage_*` 等 MCP 工具）。**仅在** MCP 不可用、缺能力或调用卡死时，回退 `unity` CLI + `com.unity.pipeline`（`unity command …`）。

## MCP 首选

- 读资源、查场景 / 组件、改 GameObject、执行 Editor 脚本等，优先 MCP。
- MCP 正常时不必为了「统一口径」强行改走 CLI。

## CLI 回退（必须）

- Editor 已打开本工程，且启动带 **`-automated`**（Hub 双击不够）。开/重开用 [`.cursor/skills/unity-automated-launch/`](../../.cursor/skills/unity-automated-launch/)。
- 改脚本后：`unity command recompile`（或等自动编译），`unity command console` 读 Console。
- 改场景 / GameObject / 组件：Pipeline 命令（`create_gameobject`、`find_gameobjects`、`add_component`、`set_component_properties` 等）。
- 调用一律带 `--project-path "<本仓库绝对路径>"`，建议 `--format json`。
- `recompile` 后轮询 `recompile_status` 至 `completed`，再发下一命令。
- 命令超时后先查 `recompile_status` / `api/status`，再决定是否重试，避免重复启动同一命令。
- **两击放弃**：同一验证动作（recompile / 查 Console）2 次仍无果（超时 / 卡死 / 状态不明）即停；汇报改动并建议人手验证。

## 场景中文名检索

`.unity` / `.prefab` YAML 里中文 `m_Name` 常写成 `\uXXXX` 转义（如 `作弊工具BG` → `"\u4F5C\u5F0A\u5DE5\u5177BG"`）。明文中文 `grep` 会误报「不存在」——应搜 Unicode 转义、用 MCP / Editor `FindObjectsOfTypeAll` / Pipeline `find_gameobjects`，或先解码再比。
