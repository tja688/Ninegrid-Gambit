---
description: 项目级 Unity CLI / Pipeline 协作规范；禁止以 Safe Mode 启动或在 Safe Mode 下操作
alwaysApply: true
---

# Unity CLI 使用规范

本仓库与 Editor 协作**一律走 Unity CLI + `com.unity.pipeline`**，不要依赖 Unity MCP。细则见 `docs/agents/unity-cli.md`。

## 必须

- Editor 已打开本工程，且启动带 **`-automated`**（Hub 双击不够）。开/重开用 `.cursor/skills/unity-automated-launch/`。
- 改脚本后用 `unity command recompile`（或等自动编译），用 `unity command console` 读 Console。
- 改场景 / GameObject / 组件优先用 Pipeline 命令（`create_gameobject`、`find_gameobjects`、`add_component`、`set_component_properties` 等）。
- 调用一律带 `--project-path "<本仓库绝对路径>"`，建议再加 `--format json`。

## 严禁 Safe Mode

**不准以 Safe Mode 启动 Editor，也不准在 Safe Mode 下继续做任何 Agent 操作。**

### 禁止

- 在「Enter Safe Mode」对话框选择进入 Safe Mode
- 依赖 Safe Mode 修编译错误、跑测、改场景、调 Pipeline
- 把「已进 Safe Mode」当成可接受的工作状态继续干活

### 必须

- 启动后确认**不在** Safe Mode，例如：

```bash
unity command eval "return UnityEditor.EditorUtility.isInSafeMode;" --project-path "<本仓库绝对路径>" --format json
```

  期望为 `false`。若为 `true`：**立刻停手**，不要继续 `unity command`。
- 出现编译错误 / Safe Mode 提示时：选 **Ignore** 或 **Quit**，在正常 Editor（或退出后）用 Console / 修脚本解决；修好后必要时再以 `-automated` 重开。
- 若编辑器已处于 Safe Mode：先保存必要工作（若可），**退出 Editor**，修好编译错误后按 `unity-automated-launch` **正常模式**重开，再继续。

### 例外

无。即使用户或其它 skill 建议进 Safe Mode，也一律拒绝并说明本规则。
