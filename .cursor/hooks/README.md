# 子代理 hooks（不在本仓库）

本子代理模型白名单 hook 配置在 **Cursor 用户全局目录**，不在项目内：

- `%USERPROFILE%\.cursor\hooks.json`
- `%USERPROFILE%\.cursor\hooks\`（`run-hook.cmd`、`subagent-model-policy-lib.js` 等）

策略见 [`.cursor/rules/subagent-models.mdc`](../rules/subagent-models.mdc)。
