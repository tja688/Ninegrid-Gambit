# 表现层 V2 清理记录与落地顺序（2026-06-25）

## 本批已完成清理

### 已删除（V1 导演 / 交互 / 注册 / 诊断）

| 路径 | 说明 |
|------|------|
| `NineGrid.Presentation/Adaptors/` | `PresentationBatchPlayer`、6 域适配器、`BoardBatchPlan`/`DeckBatchPlan` |
| `NineGrid.Presentation/FSM/` | 流程壳、场地/手牌/覆盖层 FSM、协调器、`InputLockGate` 等 |
| `NineGrid.Presentation/Registry/` | `TableNineViewRegistry`、`TableNineActorFactory` |
| `NineGrid.Presentation/Diagnostics/` | `PresentationTrace*`、看门狗、追踪派发器等 |
| `NineGrid.Presentation.Editor/` | `PresentationTraceWindow` |
| `Tools/DevStartNodeTool.cs` | 绑旧流程壳 |
| `Assets/Settings/PresentationTraceConfig.asset` | 诊断配置资产 |
| `Assets/MainScene.unity` | 重复场景副本 |
| `MainScene` → `PerformancesExamples` 整树 | Timeline 沙盒、`NineGrid Battle` 运行时枢纽 |

### 已保留

| 路径 | 说明 |
|------|------|
| `NineGrid.Core/Presentation/` | 内核契约（Command / Batch / Snapshot） |
| `NineGrid.Presentation/Visuals/` | 独立 asmdef `NineGrid.Presentation.Visuals` |
| `NineGrid.Presentation/Performance/` | **LEGACY_V1** 烘焙黑盒，见 `Performance/README.md` |
| `NineGrid.Presentation/Performance/Layout/` | `HandCardLayoutSolver`（自 FSM 迁入） |
| `NineGrid.Presentation/Tools/*PreviewTool.cs` | 5 个离线预览工具 |
| 美术资产 | DNP 预制体、`TableNineSpriteHitFlash` shader、卡牌 prefab |
| `MainScene` 锚点/UI 壳 | `Anchors/*`、`Panels/*`、Visuals 组件 |

### 场景现状

- `Assets/Scenes/MainScene.unity`：仅锚点 + UI 骨架 + Visuals；无适配器/FSM/Performance 场景挂载
- ` Directors` 空占位根保留

---

## V2 落地顺序（对照第二版蓝图）

1. **脊柱**：`Registry/ViewRegistry`、`Registry/ActorFactory`、`InputLockGate` + 场景锚点接线
2. **导演**：`Director/PerformancePlanBuilder`、`PlanStep`、`PerformanceSO`、新版 `PresentationBatchPlayer`
3. **表演接线**：PlanBuilder 对接 **既有** `Performance/`（改 `PlanStep` 入口，不重写 tween）
4. **效果池**：`Effects/` — `CardHitFlashEffects`、`DamageNumbersEffects`、`AttackEffects`（events 解耦）
5. **局内 FSM**：场地 → 手牌 → 选择 → **牌组 FSM（新增）** → Command 出口
6. **主流程 FSM**：`RunSession` / `NodePlaying` + `GamePhase` 弱投影

权威蓝图：`九宫牌局表现层（第二版）.canvas`  
契约清单：`Core表现层Command与Event消费清单-2026-06-20.md`

---

## 验收（本批）

- [x] `Adaptors` / `FSM` / `Registry` / `Diagnostics` 代码已移除
- [x] `Performance/` + `Visuals/` 保留且可编译
- [x] `MainScene` 无 `PerformancesExamples`、无 Missing Script
- [x] 删除 `Assets/MainScene.unity` 副本
- [x] Unity 编译通过、Play Mode 0 Error（`read_console` 无 error）
- [x] `NineGrid.Core.Tests` — 内核测试与表现层清理无代码耦合；EditMode 全量可在 Test Runner 复跑（本批 MCP 测试任务因 runner 占用未拿到终态）
