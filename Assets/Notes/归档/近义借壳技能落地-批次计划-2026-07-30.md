# 近义借壳技能落地 · 批次计划（2026-07-30）

> 过程笔记，非权威。权威：`02-技能字典.md`、本仓 ADR/code-map、项目 skill `.cursor/skills/quick-test-effect-channels/`。

## 背景

- 新设计：九宫登神；本仓以九宫牌局为底增量对齐，可侵入式改；删旧拖后。
- 本轮范围：`技能缺口.md` **第三节「近义可借壳，但语义不等价」**。
- Grill 共识已收口；**下一位 AI 按两批次实施**，并遵循 `quick-test-effect-channels` skill。

## 已定原则（勿再问）

1. 同义 → 改成新 id/名；只留新技能 + 完全不同的老技能（储备）。
2. 清空所有 `monster_*.json` 的 `effectAssemblies`/`skillIds`；技能权威只在 `skill_*.json`。
3. 正式开局（点「开始」）= 正常卡组 + 白板怪；与 `\` QuickTest **从根分离**。
4. `\0`–`\9` 全是可挂技能通道；仅主菜单长按 `\` 选码；**局内无 `\`（含删调速）**；换测退回主菜单。
5. 动态挂到场上怪；**卡面描述 = 技能效果描述**；道具正常发。
6. 落地后推荐通道 → 用户点头 → 写入预设；独立可混、体系单开。
7. 神圣决斗本轮不做，文档进缺原子。
8. 盗取 = 邻接帮助卡**随机移除一张**。
9. 旧献祭 → `skill.purge_followers`；新献身占用 `skill.sacrifice`。
10. Alpha5 = 鼠标指向卡 **Core Flip**（与 Alpha4 表现 POC 并存）。

## 批次 A（优先）

**目标**：改名包 + 献身 + 白板化 + QuickTest 通道骨架。

### A1. 内容白板化

- [x] 全部 `Assets/Arts/ContentVisual/cards/monster_*.json` 清空 `effectAssemblies`、`skillIds`
- [x] 同步 StreamingAssets
- [x] 确认正式开局不再从 JSON 激活怪物技能

### A2. 同义改名（Arts 双写 + 全引用替换）

| from | to | displayName |
|------|-----|-------------|
| `skill.absorb_random` | `skill.absorb` | 吸收 |
| `skill.invoke` | `skill.call_melee6` | 呼唤 |
| `skill.devotion` | `skill.offer_fire` | 献火 |
| `skill.thief_claims` | `skill.link_prep` | 链接准备 |
| 旧 `skill.sacrifice`（清龙信徒） | `skill.purge_followers` | 献祭 |

含：`skill_*.json` 文件名/contentId、模板 `tpl.skill.*` id、`effect_templates.json`、测试字符串、夹具。

### A3. 献身（同义改名，非新建）

- [x] 仓库已有 `skill.martyr`（献身，模板已等同）→ 改名为 `skill.sacrifice` / `tpl.skill.sacrifice.remove`
- [x] `OnSelfRemoved` → `FilteredCards Monster Board exclude Self` → `ModifyBaseStat Attack +1`
- [x] design_text / 卡面描述用字典全文（沿用原 martyr design_text）

### A4. QuickTest 通道骨架

- [x] `MaxPickerCode = 9`；`\0`–`\9` 映射预设（样板 `\1` = `skill.sacrifice`）
- [x] `QuickTestRunOptions` 带本局 `skillIds`
- [x] Orchestrator QuickTest 钩子：挂到场上怪 + 刷新卡面技能描述
- [x] **删除** `InBattleDebugQuickModeInputHandler` 局内 `\` 调速
- [x] HP99/ATK5 保留
- [x] 旧钉死军团牌组逻辑从 `\1`–`\6` 退役为预设可选字段

### A5. 文档

- [ ] Obsidian `技能缺口.md`：第三节处置状态；神圣决斗进第四节缺原子（若本 Notes 同步时尚未改完）
- [x] 两通道预算已写入：`\1` 移除向（sacrifice/absorb/offer_fire）；`\2` 互动向（call_melee6/link_prep）；`purge_followers` 不占码

### A6. 验收

- [ ] 点「开始」：正常卡组、怪无技能
- [ ] 主菜单 `\1`（或样板码）：场上怪有动态技能且卡面可读效果
- [ ] 改名 id 无残留引用；EditMode 相关绿
- [ ] `unity command recompile` + console 无新增 Error

## 批次 B

**目标**：翻面技能接线 + Alpha5。

### B1. 跳杀 / 盗取

- [x] `skill.leap_kill`：`OnFlip` + `IsFaceUp` + 正交邻玩家 → `DealDamage` amount=Self.Attack
- [x] `skill.steal`：`OnFlip` + `IsFaceUp` → 邻接 `HelpCard` **random count 1** → `RemoveCard`
- [x] 卡面描述=字典全文

### B2. Alpha5

- [x] `GroundFieldManagerDevKeys`：Alpha5 → 鼠标下场地卡走 Core `Flip`/`RevealFace`（触发 `OnFlip`）
- [x] 无命中：log + no-op

### B3. 通道与验收

- [x] 推荐翻面相关 `\N`（可含帮助卡联动说明）
- [x] 用户点头后写入预设
- [ ] 验收：`\N` 开局 → Alpha5 翻宿主 → 跳杀/盗取触发；正式开局仍白板

## 明确不做（本两批）

- `skill.holy_duel` 实现（等 `SetCardCounter` 或等价）
- 按 `03-怪物一一对应配置` 全量重挂正式怪
- 恢复怪物 JSON 永久 `effectAssemblies`（正式接线另票）
- 局内 `\` 换通道

## 给下一 AI 的入口

1. 读 `.cursor/skills/quick-test-effect-channels/SKILL.md` + `reference.md`
2. `ai-workspace claim` 后做批次 A，再 B
3. 每批结束用 skill 的「推荐通道」流程问用户，再改预设表
