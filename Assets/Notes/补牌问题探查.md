## 调查完成，临时脚本已清理。下面是我找到的东西。

结论：这不是一个 bug，是一条缺失的不变量
你的设计意图（「有空地 + 卡组有卡 → 补牌」）在代码里根本不存在。它不是一条被某处实现错了的规则，而是从来没有被表达成规则——它被拆成了 10 条剧本各自手工排队的补牌批次。所以现象必然「时好时坏」：补不补取决于你恰好走了哪条剧本分支。

三份证据（两路 opencode 子代理 + 我自己的日志取证）指向同一个根：

Core 侧 FillEmptySlotsAction 全仓只有 3 个生产入队点（PhaseSystem.cs:172 开局、:1813 击杀后、:1845 drain/fusion），TriggerSystem 上没有任何 OnKill→Fill 的系统级触发器，pipeline 收尾没有自动 Fill。「场上有空地就补牌」在 Core 里不是不变量，只是表现层调度器的一个选择——连「只在 InteractionLoop 补牌」这条都写在 DrainRefillScheduler.cs:90，而不在 Core。

你抓到的三个现象，各自的机制
现象 1（节点三开局有空缺不补）——已在你的日志里坐实，而且比你想的更严重

corelog 里 18 次 StartNode 的 boardOccupantCount：17 次是 8，只有节点 3 是 7，而同时 deckCount=14（抽牌堆有 14 张）。逐格快照 1:O55|2:O63|3:O53|4:O0|6:O50|7:O43|8:O44|9:O42 —— 格 4 空，且 coreCount=7。

注意 coreCount=7：Core 自己就只有 7 张。这不是表现层丢视图，是 Core 的补牌只填了 7 格。机制在 BoardDeckActions.cs:371-397：FillEmptySlotsAction 按顺时针只遍历一圈，遍历结束后才派发 OnDeal/OnEnter 触发（:410-413）。而节点 3 格 2 是 trap.rolling_stone——补牌批内被触发的效果腾出的格，本批不会再补，而 StartNode 之后没有任何收尾 Fill 命令。于是空格一直挂到你第一次交互。这也正好解释了你说的「点一下那个空格就正常补牌+旋转」：explore 剧本带 Fill 批。

现象 3（捕熊陷阱 / 恐怖面具 时好时坏）——同一个根，不同触发点

捕熊陷阱 tpl.trap.bear_trap.fill 的触发条件就是 ActionSource(action=FillEmptySlots)——它专门在补牌批里开火，打完还 RemoveCard(Self) 自移除。也就是说它必然落在「补牌循环已跑完」之后。战斗中靠敌方行动收尾（ResolveEnemyActionFinale）兜底补一次，所以「有时补」；一旦场上没有参与敌方行动的怪，EnemyActionPhaseScheduler.cs:51 就早退，收尾补牌也没了，于是「有时不补」。

你说的遗物是恐怖面具 relic.terror_mask（击杀 6 只 → 随机移除 1 只非 Boss 怪）。日志里三次触发（节点 19/21/24）我都追了后续事件——这三次都补上了，因为它挂在 OnCumulative，触发时机在玩家交战批内，晚于它的 ResolvePostKillFill 会补。所以它单独不是漏点，但它和捕熊陷阱叠在一起就是：捕熊在 Fill 批里打死一只怪 → OnKill 链拉起恐怖面具 → 又拆走一只怪 → 两个洞都在 Fill 批内，都不补。这正是你感觉「触发很随机」的来源。

另外还有一个判定口径不一致的独立漏点：攻击路径用 ContainsCardKilled(targetUid) 只看目标死没死，用牌路径用 ContainsAnyCardKilled 看任意卡。所以「攻击没杀死目标、但同批效果杀死了别的怪」时，攻击剧本走非击杀分支，没有 DrainRefill（AttackIntentScriptFactory 里根本没注入 DrainRefillScheduler，与 UseItemIntentScriptFactory 不对称）。

现象 2（节点三战斗末尾红色错误）——日志里没找到 Error/Exception，但找到了两类被降级成 Anomaly 的东西

registrylog 里有 163 次 FieldVisualGap 和 51 次 ChoreoIncompleteAtBeatClose。我按 coreCount != occupiedCount 过滤掉中间态噪声后，剩 27 次真实 Core/表现占格分叉。另外全局 3 次发牌失败 ok=false reason=nullView（节点 3 idx2166、节点 10、节点 21）——Core 已经补了牌，表现层因为 View 为空画不出来，然后回滚清占格。这是「Core 有牌、屏幕没牌」的第二类空缺，与补牌漏排是两回事。你看到的红色警告很可能是这批 Debug.LogWarning（CardDeckManagerSingleton.cs:1189），不是 Exception。

现在开始拷问
要从一个「统一的根」上解决，有几个决定只有你能做。这些是当前前沿上、不依赖其他未定问题的：

❓ Q1 - 「补牌」到底是谁的不变量：现在它是表现层剧本的编排选择。要收口，有三种根本不同的做法：(a) Core 不变量——在 ActionPipelineSystem 每次 RunToCompletion 收尾处，若「相位=InteractionLoop 且未清关 且有空格 且抽牌堆非空」则自动补到不动点，表现层剧本里所有 Fill/Drain/Fusion 排队全部删掉；(b) 导演层单一兜底——保留 Core 现状，但在 PresentationDirector.Tick 里当主线跑空且无缓冲时检查空格，缺则自动追加一个补牌批；(c) 维持逐剧本排队，只把三个漏点各自补上。

➡️ 我建议 (a)。理由：你的痛点正是「太多、太杂、触发很随机」，而 (b) 和 (c) 都保留了「每新增一条产生空格的路径就要记得手工挂 Fill」这个结构，漏点会持续再生。(a) 把它变成一条 Core 层不可绕过的事实：批次结束时盘面不得同时存在空格和可补的牌。代价是补牌时序从「剧本显式声明」变成「隐式收敛」，ADR-0001 Batch-ack 的批次边界语义需要重新表述。

❓ Q2 - 补牌应该补到「一圈」还是「不动点」：现行 FillEmptySlotsAction 只遍历一圈就收工，然后才派发 OnDeal/OnEnter——这正是节点 3 空格的直接机制。要么改成循环直到「无空格 或 牌堆空 或 达到迭代上限」，要么保持一圈但在触发链之后再排一次 Fill。前者要面对无限循环风险：捕熊陷阱补一张就杀一只再自移除，理论上能把整个牌堆吃光（这在规则上其实是合理的——陷阱确实该一张张吃）。

➡️ 我建议补到不动点，但设迭代上限（例如 16 轮，超限记 Anomaly 而不是抛异常）。理由：「有空地就补」本来就是不动点语义，一圈是实现细节泄漏。上限只是防御 ActionPipelineSystem 那种 depth>64 式的自我保护，正常内容不该碰到。

❓ Q3 - 捕熊陷阱在「开局补牌」里该不该开火：它的触发条件写死 ActionSource=FillEmptySlots，所以开局铺满 8 格的过程中它必然对邻格开火（10 点伤害，一层弱怪普遍撑不住），玩家还没操作就已经掉了一只怪 + 陷阱自爆。这是设计意图，还是「补牌」这个词在开局和战中被当成同一件事的意外后果？

➡️ 我建议开局 Fill 静默机关触发，即区分「开局铺场」与「战中补位」两种 Fill 语义。理由：开局盘面是玩家的初始信息，在他有任何输入之前就自行连锁反应，既不可预期也不可回应；而且这正好是节点 3 现象的源头。但这是策划口径，我不替你定。

❓ Q4 - FieldVisualGap / ChoreoIncompleteAtBeatClose / nullView 该不该升级为硬失败：现在它们是 Anomaly + LogWarning，一次游玩攒了 163 + 51 + 3 条，没人看。你报告的「红色错误」大概就在这里面。

➡️ 我建议：把 coreCount != occupiedCount 的真分叉（27 次）升级为开发期 Assert，nullView 发牌失败升级为必须重试或显式 Anomaly 上屏；coreCount == occupiedCount 的中间态噪声（136 次）则应当从 Gap 判定里剔除——它们把真信号淹了。理由：占格分叉是 ADR-0001 明令禁止的双真相，静默 warning 等于放弃这条不变量。

❓ Q5 - 修复的验证门槛：这类「随机复现」的问题，靠再玩几局验证不可靠。可选：(a) 写 EditMode 契约测试，用固定种子构造「Fill 批内触发移除」场景，断言批次结束后无空格；(b) 加一条运行时不变量检查（每次 idle 时断言「无空格 或 牌堆空」），跑几局看 Anomaly 是否归零；(c) 两者都做。

➡️ 我建议 (c)，且先写 (b)。理由：(b) 能立刻把「还有多少我没抓到的漏点」变成可数的数字——你现在最痛的就是不知道漏点有多少。有了它，(a) 的测试用例可以直接从实测捕获的场景反推，而不是靠想象枚举。

两份完整探查报告在 .opencode/reports/（Refill-Triggers-report.md 的补牌触发矩阵、Refill-EffectAndOpening-report.md 的效果原子清单）。第三路日志取证的子代理进程退出时没落盘，我自己接手做完了，结论都在上面。

后续研究：

## 请以 very thorough 级别研究当前项目中“场地有空位且牌堆有牌则补牌”的 Core 规则实现。重点：FillEmptySlotsAction、ActionPipelineSystem、PhaseSystem、TriggerSystem、所有生产入队点、开局/交互/敌方行动/Drain/Fusion/击杀/效果连锁的批次边界；确认补一圈 vs 补到不动点的真实语义、潜在无限循环及现有防护；列出现有测试。只研究不改动。输出精确文件路径/行号、事实、候选模型利弊与需要用户决策的问题。

研究结论
当前项目中，“场地有可补空位且抽牌堆有牌则补牌”不是 Core 的全局稳定态不变量，而是由开局、击杀、探索、翻牌、敌方阶段、Drain、Fusion 等流程分别显式安排的一次 FillEmptySlotsAction。

真实语义是：

FillEmptySlotsAction 对固定外圈路径做一次扫描，填完扫描开始时可见的空位；随后统一结算 AfterAction → OnDeal → OnEnter 效果。效果在本次 Fill 中再次制造的空位不会被重新检查。

因此当前是“补一轮”，不是“补到不动点”。某些流程碰巧再补第二轮，但这些是手工批次，不构成统一规则。

另外发现两个重要的额外问题：

Fill 的目标集合并非“所有非 Avatar 格”，而是固定外圈 1,2,3,6,9,8,7,4，永远不包含格 5。如果 Avatar 在战斗开始时不在格 5，格 5 即使为空也无法被 Fill 填充。
开局 Fill 会先写入 AvatarAppeared，而很多效果的 EventCard 定义为“本批第一个带 CardUid 的事件”。因此开局 Fill 中 EventCard 实际可能是 Avatar，不是第一张补出的牌；捕熊陷阱存在错误作用到 Avatar 的静态可达路径。
本次只读取和分析，没有修改文件，也没有运行测试。以下测试状态是“源码中存在”，不是本次执行结果。

一、FillEmptySlotsAction 的确切语义
核心实现：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Domain\Actions\BoardDeckActions.cs:339-413

1. 单次固定路径扫描
扫描路径来自：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Domain\Actions\BoardDeckActions.cs:418-441

顺序固定为：

1 → 2 → 3 → 6 → 9 → 8 → 7 → 4

事实：

一次 Action 最多检查 8 个固定外圈格。
格 5从不在扫描列表中。
当前 Avatar 格会再次被排除。
每遇到一个空格，就取抽牌堆首张、移出牌堆并放入该格。
牌堆空时立即终止本轮。
这不是随机选格；发牌位置严格按照上述顺时针顺序。
CardDealt.Amount 是本轮累计补牌数 1...N。
结束时总会发出一个 SlotsFilled，即使 Amount=0。
如果 Apply 结束时抽牌堆为 0，总会发出 DrawPileExhausted，包括调用前本来就空的情况。
相关行：

取固定路径：BoardDeckActions.cs:357-358
单次循环：BoardDeckActions.cs:371-397
SlotsFilled：BoardDeckActions.cs:399-400
DrawPileExhausted：BoardDeckActions.cs:402-405
2. Fill 本身没有相位、清关或稳定态门禁
FillEmptySlotsAction.Apply 不检查：

当前是否为 InteractionLoop
是否已经清关
是否为终局相位
是否真的存在可补空位
这些门禁只存在于部分 PhaseSystem 包装方法中：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:1838-1886

因此：

ResolvePostKillFillInternal 会拒绝终局和已清关状态。
Fusion/Drain 共用的 ResolveFillEmptySlotsBatchInternal 会拒绝 skipFill、终局和已清关状态。
直接执行 BoardSystem.FillEmptySlots() 则没有这些门禁。
二、固定外圈与“所有非 Avatar 空格”之间存在口径冲突
1. Avatar 初始位为格 5，但之后可以移动
初始创建：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Setup\InitialGameFactory.cs:90-94

Avatar 移动：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Domain\Actions\PhaseActions.cs:460-487

允许移动的相位与门禁：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:724-771

新节点重置只清场上卡，不把 Avatar 重置到格 5：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Domain\Actions\BoardDeckActions.cs:20-28

BoardModel.ClearBoardCards() 也不修改 Avatar：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Models\BoardModel.cs:156-165

2. 由此得到的实际后果
如果更高层流程未保证战斗开始前 Avatar 回到格 5：

Avatar 例如位于格 2。
Fill 会跳过格 2。
固定路径# 结论
当前项目没有在 Core 中实现一条全局不变量：

场地存在可补空位，并且抽牌堆有牌时，必须自动补牌。

现有真实语义是：

在少数手工指定的流程边界，执行一次 FillEmptySlotsAction；该 Action 扫描一次外围八格，填完当时可见的空位，然后统一结算 AfterAction → OnDeal → OnEnter。这些触发后新产生的空位不会由本次 Action 再补。

因此：

当前是“一次扫描/补一圈”，不是“补到不动点”。
是否补牌依赖开局、攻击、Explore、Reveal、用牌、敌方行动、Drain、Fusion 等调用方有没有显式安排补牌批次。
ActionPipelineSystem 收尾不检查空位，也不自动补牌。
TriggerSystem 中不存在 OnKill/OnRemove → FillEmptySlotsAction 的系统规则。
Fill 触发链内发生击杀、移除、融合、自毁后，空位是否稍后补上，取决于后面是否恰好还有敌方行动收尾或 Fusion/Drain 调度。
当前没有“无限补牌循环”，主要是因为根本没有不动点循环；若改成不动点，现有通用防护不足。
以下结论来自静态代码审计；未修改文件，也未执行测试。

一、FillEmptySlotsAction 的精确真实语义
核心实现位于：

（339–413 行）

1. 一次 Action 扫描一次固定外围路径
填充路径来自：

（418–441 行）

顺序固定为：

1 → 2 → 3 → 6 → 9 → 8 → 7 → 4

也就是外围八格，不包含格 5。

每个位置的规则是：

等于当前 AvatarSlot：跳过。
已有卡：跳过。
抽牌堆无牌：终止扫描。
否则取抽牌堆首牌，移出牌堆并放入该格。
关键行：

371–377：扫描与跳过。
379–387：取牌、移牌、放牌。
390–396：每张牌产生 CardDealt。
399–400：无论实际补了几张，都产生 SlotsFilled。
402–405：Action 结束时牌堆为空就产生 DrawPileExhausted。
2. SlotsFilled 不代表真的补到了牌
SlotsFilled 永远产生，即使 filled == 0：

（399–400 行）

所以：

“事件日志含 SlotsFilled”只能证明跑过 Fill Action。
不能证明空位被填上。
不能证明牌堆原本有牌。
不能证明盘面最终稳定。
这会影响现有测试可信度，详见测试章节。

3. 开局时还会先把 Avatar 事件塞进同一结果列表
当相位是 DealOpeningCards 时，Fill 在所有 CardDealt 前先产生 AvatarAppeared：

（360–369 行）

因此开局 Fill 的事件顺序通常是：

AvatarAppeared
CardDealt
CardDealt
...
SlotsFilled
DrawPileExhausted（可选）

这会和当前效果系统的 EventCard = 本 Action 第一个带 CardUid 的事件 语义发生潜在冲突：

FirstEventCardUid：
（122–133 行）
EventCard 解析：
（4034–4059 行）
开局 Fill 中，第一个 CardUid 可能是 Avatar，而不是第一张补出的牌。

这意味着类似捕熊陷阱这种同时使用：

OnDeal
EventFilter(CardDealt)
EventCard
ActionSource(FillEmptySlots)
的效果，在开局多事件批中存在目标含义歧义。当前捕熊测试只覆盖单张补牌，没有覆盖开局 AvatarAppeared + 多张 CardDealt。

4. 所有牌先放完，之后才触发效果
Fill 声明的 Post Trigger 顺序是：

AfterAction
OnDeal
OnEnter
见：

（341–346、410–413 行）

但是这些 Trigger 并不是每放一张牌就触发。Apply 会先完成全部放牌并返回，Pipeline 才统一派发触发器：

（145–154 行）

因此真实顺序是：

扫描外围八格
→ 一次性放完本轮所有牌
→ 统一追加全部 CardDealt/SlotsFilled 事件
→ 完整结算 AfterAction 触发链
→ 完整结算 OnDeal 触发链
→ 完整结算 OnEnter 触发链
→ 处理 Fill 自身 FollowUp
→ Fill ActionFinished

结论：

不是逐张“放牌 → 触发 → 再找下一个空位”。
一个效果实例在同一 Fill Action 的 OnDeal 上通常只 Dispatch 一次。
Trigger 收到的是整批事件，不是单张事件。
EventCard 通常只取整批中的第一个事件卡。
触发链移除卡、自毁、击杀、融合后，本次扫描不会重新开始。
5. “可补空位”的定义在各层并不一致
Fill 只扫描外围八格；但 Flow 的空位判断扫描全部九格并只排除当前 Avatar 格：

（90–119 行）

同时：

Avatar 初始位只在新建 Run 时设为格 5：
（90–94 行）
非战斗阶段 Avatar 可以移动：
（460–487 行）
StartNode 没有把 Avatar 强制归位到格 5：
（148–178 行）
如果 Avatar 开战时不在格 5：

Flow 会把空格 5 视为可补空位。
Fill 永远不扫描格 5。
ShouldRefill 可能判断“需要补”，但实际 Fill 补 0 张。
若未来直接写“不动点 while 有空位就 Fill”，这里会构成无进展死循环。
当前没有 Core 断言保证“进入战斗时 Avatar 必在格 5”。

二、ActionPipelineSystem 与触发链顺序
实现位于：


1. 两类队列
普通 Action：FIFO Queue<GameAction>，61–69 行。
Reaction：LIFO Stack<GameAction>，71–79 行。
Reaction 永远优先于普通队列，98–102 行。
RunToCompletion 的稳定条件只是：

ReactionStack 为空
并且
Queue 为空

没有检查：

盘面空位。
抽牌堆。
当前相位。
节点清关状态。
是否需要继续 Fill。
见 87–109 行。

2. 单个 Action 的顺序
ResolveAction：

（135–155 行）

顺序为：

ActionStarted
Pre Triggers
Apply
追加事件
Post Triggers（按 Action 声明顺序）
FollowUp Actions
ActionFinished

因此一次 FillEmptySlotsAction 的全部效果连锁仍属于同一个 RunToCompletion，而且在 Fill 的 ActionFinished 之前完成。

3. 不同 TriggerPoint 之间完全串行
`# 审计结论

本次只做静态研究，未修改文件，也未执行测试。

当前项目中，“场地有可补空位且抽牌堆有牌则补牌”不是 Core 的全局稳定态规则。它实际由多个入口手工安排一次 FillEmptySlotsAction。真实语义是：

对固定外围八格做一次扫描，补完当时看见的所有空位；随后统一结算 AfterAction → OnDeal → OnEnter 效果。效果链中新制造的空位不会由本次 Fill 再检查。

所以当前是“补一轮”，不是“补到不动点”。不同剧本可能随后再安排敌方收尾、FusionRefill 或 DrainRefill，偶尔看起来像收敛，但没有统一保证。

## 请以 very thorough 级别研究当前项目的 Core→表现层批次/ack/导演/补牌表演架构。重点查 ADR-0001、PresentationDirector、DrainRefillScheduler、EnemyActionPhaseScheduler、CardDeckManagerSingleton、FieldVisualGap、ChoreoIncompleteAtBeatClose、nullView、Core/表现双真相规则，以及 Fill/Drain/Fusion 的动画排序和主线空闲判定。只研究不改动。输出精确文件路径/行号、当前契约、若把补牌做成 Core 不变量对表演层的影响与安全落地边界。

结论先行
ADR-0001 的主干已经落地，但不是全项目彻底闭合。 Attack、Explore、RevealFace、UseItem 的主要路径，以及敌方齐射，已经按 Resolve → Present → ack → 下一批 运行；但 Opening、Pickup 和若干兼容入口仍会在一次 Core 调用里先完成 Fill/Rotate/EnemyAction，再由表现层事后播放。
“有可补空位且抽牌堆有牌就必须补牌”目前不是 Core 不变量。 Core 只提供 FillEmptySlotsAction 与几个显式入口；是否追加 Fill、DrainRefill、FusionRefill，仍大量由 Flow 剧本判断。
Fill、DrainRefill、FusionRefill 在 Core 中其实都是同一个 FillEmptySlotsAction。 三者的实质差异主要在表现层调度时机；Fusion 还在表现层临时改写 Core 牌堆顺序并排除融合结果 UID，这已经越过“表现只播放事实”的理想边界。
当前 MainlineBusy == false 只表示导演主队列已空。 它不直接证明盘面已达到“无可补空位”的 Core 稳定态，也不检查 bypass、Core 待稳定义务、FieldVisualGap、open choreo/open motion。
如果把补牌升级为 Core 不变量，不能塞进每次 RunToCompletion 尾部。 必须做成明确的 Core 稳定化事务/状态机，并由导演用一个持续占住主线的 settlement step 逐批推进；每个稳定化切片仍须等上一个 Present ack。
安全落地前必须先加固发牌失败的 ack 契约。 当前最终 nullView/发牌失败可以只告警，然后通道仍完成并 ack，导致 Core 已有卡、表现层无卡、主线却已 idle。
一、当前权威契约
1. ADR-0001：统一时间线、批次锁步、Core 占格权威
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\docs\adr\0001-battle-presentation-unified-timeline-batch-ack.md:9-24

当前 accepted 决策明确规定：

唯一串行 BattleTimeline；
PresentationDirector 是唯一所有者和出口；
Core 一拍一 Batch；
表演完成后通过 FinishBatch ack 才能继续；
逻辑占格只归 Core；
禁止 RunToCompletion 先算完整局；
禁止表现层维护第二套规则占格并 force-sync。
核心原文位置：

决策：:9
“核先行/双真相”的根病灶：:13
被否决的纯表现层方案：:17
_uidBySlot 规则镜像与强制对账退场：:22-24
2. 领域词汇的现行定义
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\CONTEXT.md:89-123

Director 是唯一出口：:89-91
Timeline 是唯一串行主线：:93-95
Batch 是 ack 驱动的 Core 一拍：:101-103
ack 是“表演已就位”的信号：:105-107
Core BoardModel 是逻辑占格真相；表现侧只有几何注册：:121-123
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\CONTEXT.md:131-140

临时输入互斥只认主线是否在跑：:131-133
主线 idle 时才 flush 缓冲，并重新向 Core 校验合法性：:139-140
3. 敌方行动阶段的特殊不变量
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\docs\adr\0012-enemy-action-phase-volley.md:9-27

报名、逐条结算、收尾三段；
报名和逐条结算期间盘面冻结；
只在收尾补牌；
收尾不旋转；
每只怪一批、串行 ack。
关键位置：

收尾补牌、不旋转：:19
阶段内冻结：:21
每怪一批：:25
互动计数、补牌、旋转必须解耦：:27
这意味着任何“每个 Core action 结束就自动补牌”的实现都会直接破坏 ADR-0012。

4. 清关的硬例外
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\docs\adr\0026-leave-trap-sole-clear-condition.md:17-18

击破离开机关的当拍：

禁止再次 FillEmptySlots；
禁止进入敌方行动；
清场扫描不触发常规死亡 Post 链。
所以未来 Core 补牌不变量必须包含“节点已清关/正在清场时关闭稳定化补牌”的显式条件。

5. “Core 补牌不变量”目前只是建议，不是 accepted ADR
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Notes\补牌问题建议.md:13-17

建议文档支持 Core 不变量，但明确反对把 Fill 粗暴塞入每次 RunToCompletion。

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Notes\补牌问题建议.md:64-87

建议的不变量是“稳定边界不允许同时存在可补空位和可用牌”，不是“任何瞬间不许有空格”。

因此，若正式采用，需要新增 ADR 或修订 ADR-0001/0012，而不能把 Notes 当成当前正式契约。

二、导演与 Batch-ack 的实际实现
1. PresentationDirector

当前契约：

IsMainlineBusy 只读 mMainline.IsBusy：:41-45
bypass 不计入主线 busy：:53-56
idle 时立即 BuildScript；忙时 latest-wins 缓冲：:68-95
外部阻塞演出以 ExternalMainlineHoldStep 挂到同一主线：:132-169
每帧先推进 mainline/bypass：:180-184
主线跑空后才 flush 缓冲意图：:185-204
主线空且无缓冲时清 chainId：:205-209
这里的关键事实是：

Director idle 的判定没有查询 Core 是否仍需补牌，也没有检查 Core 是否达到稳定态。

2. BattleTimeline
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\BattleTimeline.cs:25-32

IsBusy 只是：

当前有 Step；
或队列中仍有 Step。
推进规则位于 :60-93：一个 Step 完成后才进入下一个，Aborted 会清空剩余队列。

因此未来若要让“Core 尚未稳定”阻止输入，安全做法不是再造 CoreBusy，而是让一个 settlement step 一直留在主线，直到 Core 报告稳定。

3. Resolve、Present、ack 的顺序

Resolve
有未关闭批次时驻留：:63-68
解算/开批失败时中止整条剧本：:70-78
Present
严格顺序是：

等 Batch 打开：:147-156
默认先冲刷 FaceUp，等翻牌 idle：:158-174
channel.Begin：:174-177
等 channel.IsComplete：:185-191
统一 FlushBeats：:193-198
再次等翻牌 idle：:200-204
TryAcknowledge：:206-216
因此现行 ack 的合同不是“动画启动过”，而是：

Channel 报完成、统一表现锚点已冲刷、翻牌已 idle，然后才 FinishBatch。

4. Batch Gate 与 Sync
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\PresentationSyncBatchGate.cs:83-125

ActiveBatchId > 0 时绝不解算下一批；
Core 命令被拒或没有打开 Batch，剧本失败；
ack 走 FinishBatch(batchId)。
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Presentation\PresentationSyncSystem.cs:39-73

特别重要：

ActiveBatchId 才是“存在打开批次”的真相；
IsInputLocked 只表示本批是否含阻塞指令；
即使空批、非阻塞批，也必须 FinishBatch。
对应测试：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\BehaviorBaseline\BatchAckBaselineTests.cs:15-45

第二批 Resolve 必须等待第一批 Present ack。

空批也必须关闭：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\BehaviorBaseline\BatchAckBaselineTests.cs:49-82

5. 一个隐藏的结构性前提
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Presentation\CoreCommandDispatcher.cs:55-74

Dispatcher 在决定是否开新批时检查的是：

sync.IsInputLocked

而不是：

sync.ActiveBatchId > 0

同时 PresentationSyncSystem.OpenBatch 自身不会拒绝覆盖已有 Batch：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Presentation\PresentationSyncSystem.cs:28-36

标准导演路径由 PresentationSyncBatchGate 保证不会覆盖，因此当前正常工作。但安全边界是：

所有未来稳定化命令必须继续通过 Batch Gate；禁止在有空的非阻塞 Batch 打开时，从旁路直接 dispatcher.Send。

三、补牌目前由谁决定
1. Core 的实际 Fill 语义
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Domain\Actions\BoardDeckActions.cs:339-413

FillEmptySlotsAction：

按固定顺时针路径扫描：:357-359
每个空位从牌堆顶取一张：:371-397
最后发 SlotsFilled：:399-400
牌堆空时发 DrawPileExhausted：:402-405
Post Trigger 包含 AfterAction / OnDeal / OnEnter：:410-413
但 ActionPipelineSystem 是先完成 Apply，再派发 Post Trigger：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\ActionPipelineSystem.cs:145-154

所以当前一轮行为是：

扫描并填完当时的空位
→ 产生 CardDealt / SlotsFilled
→ 才执行 OnDeal / OnEnter 反应
→ 反应若再制造空位，不会自动再 Fill

这是当前不动点缺失的机械根因。

2. 三种“补牌”最终都走同一 Core action
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:505-542

PostKill Fill：ResolvePostKillFill
Fusion Refill：ResolveFusionRefill
Drain Refill：ResolveDrainRefill
Fusion 与 Drain 都转到同一个：

ResolveFillEmptySlotsBatchInternal

其实现：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:1877-1886

只检查：

skipFill
terminal phase
node cleared
然后直接执行 FillEmptySlotsAction。

它本身没有强制 CurrentPhase == InteractionLoop。InteractionLoop 条件主要由表现层 Scheduler 代为维护。

3. DrainRefill 的决定仍在表现层
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\DrainRefillScheduler.cs:82-102

ShouldRefill 由 Flow 判断：

当前必须是 InteractionLoop：:89-93
牌堆非空：:95-99
BoardModel 有空位：:101
若需要，则追加独立 Resolve/Present：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\DrainRefillScheduler.cs:41-80

4. FusionRefill 还包含规则层牌序策略
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\FusionRefillScheduler.cs:185-241

Flow 在解算前判断：

phase；
空位；
抽牌堆；
是否有不在 exclude 集中的候选。
然后：

临时重排 Core 抽牌堆：:243-257
调 ResolveFusionRefillCommand：:258-272
finally 恢复剩余牌的原始顺序：:273-277
规则策略来自：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\FusionRefillPlanner.cs:121-175

它把融合结果 UID 推到牌堆尾部，避免融合结果立即被再次补出。

这不是纯动画策略，因为它决定“哪张牌实际被 Core 抽出”。如果采用 Core 补牌不变量，这部分必须迁入 Core；不能只删 Scheduler。

四、各生产路径的当前批次序列
1. Attack 击杀路径
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\AttackIntentScriptFactory.cs:206-259

现行顺序：

CombatHit Resolve
→ Hit Present/ack
→ InteractionCount Resolve
→ Board Present/ack
→ PostKill Fill Resolve
→ Board Present/ack
→ Rotate Resolve
→ Board Present/ack
→ 若融合：FusionRefill Resolve/Present
→ Enemy Action Phase

Fill 在 Rotate 前，这是已有 Core 合同：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core.Tests\CoreOperationContractTests.cs:83-105

以及表现锁步测试：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Flow\AttackVerticalSliceTests.cs:84-119

2. Attack 非击杀路径
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\AttackIntentScriptFactory.cs:138-151

只根据：

mLastHitKilledTarget

判断是否进入 Fill/Rotate。

非击杀路径：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\AttackIntentScriptFactory.cs:261-264

只推进 interaction，再追加敌方行动。

因此：

目标未死，但同一个 CombatHit 反应链移除了其他卡时，Attack 剧本不会主动追加 DrainRefill。

如果此时场上没有参与敌方行动的怪，敌方 Scheduler 会直接返回：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\EnemyActionPhaseScheduler.cs:51-54

也不会走 Finale Fill。这个漏点由代码结构直接成立。

3. Explore / RevealFace
Explore：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\ExploreIntentScriptFactory.cs:81-125

顺序为：

ClickEmpty
→ InteractionCount
→ Fill
→ Rotate
→ FusionRefill（若有）
→ EnemyAction

RevealFace：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\RevealFaceIntentScriptFactory.cs:80-123

顺序同构。

4. UseItem
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\UseItemIntentScriptFactory.cs:110-148

分支：

击杀：Fill → Rotate → FusionRefill；
非击杀且发生融合：FusionRefill；
非击杀、无融合、盘面有空位：DrainRefill。
决定字段：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\UseItemIntentScriptFactory.cs:208-216

说明目前是否补牌依赖表现剧本对“是否有击杀、是否有融合、是否有空位”的分类。

对应 Drain 独立批测试：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Drain\ResolveDrainRefillBatchCommandTests.cs:24-102

5. 敌方行动

顺序：

Register Resolve/Present：:77-107
每条 Strike 独立 Resolve：:120-164
有伤害走 Counter 通道：:131-151
无伤害走 Board 通道：:152-164
玩家未死且名单未空，继续下一条：:167-195
Finale Resolve/Present：:198-218
Core 收尾才 Fill：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:1753-1765

Core 测试明确保证齐射中不补、Finale 才补：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core.Tests\EnemyActionPhaseTests.cs:145-168

6. Pickup 仍是明显的兼容整拍路径
生产 Pickup Apply：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Commands\ApplyPickupItemCommand.cs:37-68

直接调用：

IPhaseSystem.ApplyPickupItem

而 Core Pickup 内部：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:617-648

在一次调用里执行：

PickupCardAction
→ ResolveInteractiveRotation

ResolveInteractiveRotation 又一次性执行：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:1592-1606

InteractionCount
→ Fill
→ Rotate
→ 整个 EnemyActionPhase

表现层随后只把完整 EventLog 投影为一段 Summary 再 Drain：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\CardHandManagerSingleton.cs:740-752

因此 Pickup 目前并不满足“一批 ack 后 Core 才继续下一拍”的严格 ADR-0001 语义，只是用 ExternalHold 阻止输入并事后顺序播放。

7. Opening 同样是 Core 先完成
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Systems\PhaseSystem.cs:164-178

StartNode 在一次 RunToCompletion 中依次完成：

建池
→ 开局发牌
→ FillEmptySlots
→ NodeStarted
→ InteractionLoop

表现层在 Core 完成后捕获计划并播放：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BattleSessionExecutor.Opening.cs:159-229

这也是为什么开局 Fill 内触发制造的新空位没有第二轮稳定化。

五、Fill / Drain / Fusion 的实际动画排序
1. 有序步骤来自 Core EventLog

投影规则：

CardDealt → Deal：:99-157
CardRemoved/CardKilled → Remove：:162-181
CardMoved + BoardRotated → Rotate：:57-91、:324-349
每个步骤携带 Core sequence/actionId：:120-127、:149-157
所以同一 Core 批内部，表现步骤总体按 EventLog 顺序保留。

2. Board Drain 的执行顺序
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:282-404

有序 steps 按顺序执行：

Rotate/Move/Swap await 运动：:321-347
Deal 启动发牌：:351-355
Remove 执行普通移除或融合：:359-365
Impact 的规则：

在第一个 Remove 前冲刷：:306-310
若没有 Remove，则所有步骤完成后冲刷：:392-395
因此 [Deal, Rotate, Remove] 的可见顺序是：

Deal 启动
→ Rotate/Move 落地
→ Impact
→ Remove/Fusion

这是 ADR-0005/0018 的“先让触发条件可见，再播后果”。

结构测试：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\CardFaceBeatStructuralTests.cs:220-261

明确禁止在同批 [Deal, Rotate] 的 Deal 前抢跑 Impact。

3. 发牌不是逐张完全等待，而是批量起飞、尾部栅栏等待
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:406-546

多张牌按 dealInterval 交错起飞：:425-545
每张成功飞牌只收集 handle：:518-523
DrainDealsAsync 自身不立即等待所有牌落地。
真正的就位栅栏在 Drain 尾部：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:215-240

WaitAllActiveDealFlightsAsync 完成后，Board Present 才能返回，继而 Channel complete、PresentStep ack。

4. Fill 与 Rotate 的两种现状
新导演分批路径
Attack/Explore/Reveal/UseItem 击杀路径：

Fill Resolve
→ Fill Present 等所有飞牌落地
→ ack
→ Rotate Resolve
→ Rotate Present

这是真正的跨 Batch 串行。

Pickup 等旧合并路径
Pickup 一次 Core 调用可生成 [Deal..., Rotate...] 同批步骤。为处理这种旧路径：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\DealVisualTargetResolver.cs:5-40

发牌会跳过连续 Deal，预看后续 Rotate，并直接瞄准旋转后的最终视觉格。

测试：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Cards\DealFlightMathTests.cs:213-243

所以当前项目同时存在：

分批 Fill 落地后再 Rotate；
同批 Deal 飞行与 Rotate 交错/重定向。
这正是 Pickup 未完全迁入导演批次模型的可见后果。

5. Fusion 的顺序
Fusion 不是看到第一个 participant Remove 就立即播放。Board Player 会等待同一 Fusion action 的全部 participant Remove step 到齐：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:1321-1378

之后：

确保结果 View；
播放融合；
Fusion 完成后才尝试调度 FusionRefill。
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:1387-1425

融合动画内部：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\SkeletonDeckPresentationManager.cs:91-170

顺序是：

Vacate 参与者几何占格
→ 双卡撞中点
→ 闪白
→ 隐藏参与者
→ Reveal 结果卡
→ 结果卡飞回牌堆
→ 释放参与者

参与者真正 Vacate：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\SkeletonDeckPresentationManager.cs:294-307

当前 Board Player 传入 onFusionStarted: null，因此不会在 Vacate 时立即补牌；Refill 被推迟到整段融合 Present 结束后。

6. 同批 Deal→Remove 的保护
如果新发出的牌在同批效果中立刻被移除，必须取消飞牌：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\Ground\DealFlightCoordinator.cs:220-278

取消会：

从 active map 移除；
提前关闭 choreo；
防止尾部 WaitAll 永久挂住。
对应结构测试：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Cards\DealFlightRemoveCancelContractTests.cs:9-83

六、Core/表现“双真相”当前真实边界
1. Core 是逻辑权威
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Models\BoardModel.cs:26-103

所有规则占格写入都在 BoardModel：

PlaceCard
ClearSlot
RemoveCard
2. 表现层仍有 _uidBySlot，但其合同已改为几何注册
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\Ground\GroundOccupancyIndex.cs:7-16

同名 _uidBySlot 仍存在，但注释明确：

只是场地几何占格、命中认领；
Core BoardModel 仍是规则真相；
禁止 force-sync 自愈。
冲突不能静默覆盖：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\Ground\GroundOccupancyIndex.cs:106-151

3. Force-sync 已被硬禁止
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\OccupancyForceSyncGuard.cs:27-57

所有请求只记 Anomaly，并返回 false。

真正断言入口：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BattleSessionExecutor.cs:730-740

旧 RequestSyncBoardFromCore 只会触发禁止断言：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BattleSessionExecutor.cs:983-986

4. 允许的修复仅是“从 Core 判断表现幽灵”
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:549-597

Deal 前可清除表现幽灵，但只有在：

当前 blocker 不是预期 UID；
blocker 也不是 Core 在该格的正确卡；
Core 任何格都不再拥有该 blocker。
如果 Core 仍拥有它，只允许 relocate，不能销毁。这符合“派生缓存可修，Core 不反向改”。

七、nullView、FieldVisualGap、ChoreoIncompleteAtBeatClose
1. nullView 的当前行为
CardDeckManagerSingleton 是场景组件：

GameObject：Directors/CardDeckDirector
发牌前若 View/Transform 为空：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\CardDeckManagerSingleton.cs:1183-1192

行为：

告警；
返回 false；
不登记表现占格。
若登记后 View 失效：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\CardDeckManagerSingleton.cs:1240-1257

行为：

清表现占格；
尝试把卡放回表现牌堆；
返回 false。
这些操作不回滚 Core BoardModel。如果 Core 已发牌，结果就是 Core 有卡、表现无卡。

当前高层自愈存在缺口
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:662-716

这里本来会发现僵尸 View、Release 后重 Spawn。但它先做：

如果 deckManager.ContainsUid(uid)，立即 return null

位置 :664-668。

因此“僵尸 ManagedCard 已经位于表现牌堆槽中”时，不会进入后面的 View 检查与重 Spawn，最终仍会在 CardDeckManagerSingleton 命中 nullView。

而发牌失败后的重试只在成功清理幽灵占格时发生：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:503-516

nullView 通常没有幽灵占格可清，因此不会重试。

最终失败只告警并继续：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BoardPresentationPlayer.cs:530-538

随后 QueuedBoardPresentChannel 无论成功还是失败，finally 都将 mComplete=true：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\QueuedBoardPresentChannel.cs:80-94

所以当前存在：

Core CardDealt 成功
→ View 发牌失败
→ Board Present 仍完成
→ PresentStep ack
→ mainline idle

现有 nullView 测试只保证“不留下表现幽灵占格”：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Cards\DealNullViewTests.cs:70-102

没有测试“必须重建 View”或“失败不得 ack”。

2. FieldVisualGap 的实际含义
捕获器：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Diagnostics\DiagFieldVisualCapture.cs:28-126

它分别统计：

CoreCount
表现 OccupiedCount
VisibleCount
空视觉格
hidden/off-mode occupant
但 MissingVisible 的计算只是：

opening baseline visible - current visible

位置 :123-126。

发出 FieldVisualGap 的条件：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Diagnostics\RegistryTraceRecorder.cs:798-837

只看：

baseline > 0
MissingVisible > 0
它没有直接用 coreCount != occupiedCount 判断异常。coreCount 只放在 payload：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Diagnostics\RegistryTraceRecorder.cs:773-795

所以 FieldVisualGap 可能代表三种完全不同的东西：

Core 本身漏补，CoreCount 与表现一致但都少；
Core 有卡、表现没占格；
表现占格有卡，但 Renderer/DisplayMode 不可见。
而且 Opening baseline 是第一次采样的可见数量：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Diagnostics\RegistryTraceRecorder.cs:735-750

如果开局本身就只有 7 张可见卡，baseline 会被定成 7，最初那个逻辑空位不会被 FieldVisualGap 识别。

反过来，牌堆合法耗尽后盘面少于 opening baseline，也可能产生噪声。

3. ChoreoIncompleteAtBeatClose
Perf 检查：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Diagnostics\PerfTraceRecorder.cs:233-250

只在 beatKind == BoardChoreo 时，如果：

OpenChoreoCount > 0
或 OpenMotionCount > 0
发 ChoreoIncompleteAtBeatClose。

Registry 镜像：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Diagnostics\RegistryTraceRecorder.cs:155-178

因此它表示：

某个 BoardChoreo 诊断 beat 关闭时，还有 choreography 或 motion 没有正常收尾。

它不等同于 Core/表现占格分叉，也不必然表示主线仍 busy；可能是：

真异步泄漏；
Beat 边界过早关闭；
取消路径漏 End；
.Forget() 工作跨越了诊断 beat。
项目已经为 Deal→Remove 取消路径增加 eager close：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Cards\Ground\DealFlightCoordinator.cs:220-278

但目前没有找到针对 FieldVisualGap 或 ChoreoIncompleteAtBeatClose 本身的专门契约测试；它们主要还是诊断信号。

八、主线 idle 的精确定义与盲区
当前“主线空闲”只证明
BattleTimeline 当前 Step 为 null；
队列为空；
若有缓冲 intent，则本 Tick 会重新建脚本；
若无缓冲，则清 chainId。
它不直接证明：

Core 不存在可补空位；
IPresentationSyncSystem.ActiveBatchId == 0；
bypass 已结束；
无 open choreo/open motion；
Field/Deck/Hand 局部动画全部 idle；
CoreCount == geometry occupied == visible。
正常导演路径中，PresentStep ack 通常间接保证 ActiveBatch 已关闭；Board Channel 也会等 deal flight。但这是一条“通过正确组合得到”的性质，不是 IsMainlineBusy 自身的判定条件。

完整场景切换前的 idle 判定明显更强：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BattleSession\BattleSessionExecutor.Opening.cs:930-963

它同时检查：

DrainInFlight
Field busy
Hand self busy
Deck busy
Battle presentation busy
Director mainline busy
因此：

MainlineBusy 是输入互斥真相，不是“全表现系统完全静止”的通用真相，更不是“Core 盘面已稳定”的真相。

九、若把补牌做成 Core 不变量，表演层会受到什么影响
1. 必须改变“谁决定要不要补”
应从 Flow 移除或降级以下判断：

DrainRefillScheduler.ShouldRefill
mLastUseNeedsDrainRefill
mLastHitKilledTarget 作为是否补牌的唯一依据
EnemyActionPhaseScheduler.HasParticipatingEnemy 对是否存在 Finale Fill 的间接影响
FusionRefill 的 Flow 层 deck reorder/exclude 决策
表现层仍可以决定：

补牌飞行时长；
多张牌间隔；
镜头、音效；
一批或逐张展示；
Present 何时完成；
ack 何时返回。
但不能再决定：

是否发生 Fill；
Fill 哪些位置；
抽哪几张牌；
Fusion result 是否可立即被抽出；
新 Deal 触发后是否继续补。
2. 不能把整个不动点一次算完塞进一个 Batch
当前 PresentationBatch 每批只有一份批末最终 Snapshot：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Foundation\NineGrid.Core\Presentation\PresentationBatch.cs:27-44

而盘面投影还会读取命令结束后的 live Core Registry 来补 DefId：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\BoardPresentationStepProjector.cs:134-147

如果一个命令里执行：

Fill
→ OnDeal 移除
→ 再 Fill
→ Fusion
→ 再 Fill
→ 最终稳定

然后只生成一个 Batch，表现层在播放早期 Deal 时，Core 已经处于最后状态。会重新引入 ADR-0001 所反对的核先行，也容易发生：

早期卡已经在 Core 被移除，却要临时 Spawn 其旧 View；
融合结果已经再次被抽出；
卡面 Commit 读取最终值而不是当拍值；
原因顺序虽然保留在 EventLog，资源/Registry 却已进入未来状态。
安全粒度应是：

每次稳定化 Fill action 及其直接反应形成一个语义切片；该切片 Present/ack 后，Core 再判断是否需要下一轮 Fill。

3. 必须保留领域检查点，而不是全局“见空就补”
至少要有这些稳定化检查点：

开局稳定化
OpeningDeal
→ Opening Fill
→ 入场触发
→ 若再有空位，继续 Opening Stabilize
→ 达到稳定后才给玩家输入

是否允许开局 Fill 触发捕熊等攻击性机关，必须成为明确 Core 规则，例如 FillReason.InitialLayout，不能由动画层特殊处理。

玩家互动的 Pre-Rotate 稳定化
当前正式规则是 Fill 在 Rotate 前。未来仍须保持：

玩家行为及直接反应
→ PreRotate Stabilize
→ Rotate

不能仅在整个意图最后统一补，否则会变成 Rotate 后 Fill，改变玩法和已有测试合同。

Post-Rotate / Fusion 稳定化
Rotate 触发融合后：

Rotate 逻辑切片
→ Rotate/Fusion Present
→ ack
→ Core 判定下一稳定化切片

但“融合结果本轮不可立即被补回”必须由 Core 的 transaction/cause 规则表达，而不是 Flow 临时重排牌堆。

EnemyAction Finale 稳定化
报名和逐条结算期间必须显式抑制稳定化：

Register
→ Strike 1
→ Strike 2
→ ...
→ Finale Stabilize

不能让通用稳定器在每个 ResolveNextEnemyAction 后发现空位就补。

清关抑制
node cleared 或 clearResidual 过程中必须直接判定“稳定化关闭”。

4. UseItem、Pickup、Attack 的领域差异必须继续保留
Core 不变量不等于把所有行为统一成同一互动链：

UseItem 当前不推进 interaction count，也不跑敌方行动；
Pickup 当前算九宫格互动，会 Fill/Rotate/EnemyAction；
Attack 未击杀也推进 interaction count 和敌方行动；
Enemy Finale Fill 不旋转；
LeaveTrap clear 不 Fill。
稳定器只负责恢复允许阶段的盘面稳定，不能顺便抹平这些领域差异。

十、安全落地边界
边界 A：Core 拥有“必然性”，导演保留“推进节奏”
建议的接口语义应类似：

Core:
    HasPendingSettlement
    ResolveNextSettlementSlice()
 
Presentation:
    Resolve slice
    Present slice
    ack
    再问 Core 是否有下一 slice

Core 决定下一 slice 必须是：

Fill；
Rotate；
Enemy register/strike/finale；
Node complete；
Stable。
Director 不再用 ShouldRefill 或击杀布尔决定是否 Fill，只负责重复推进 Core 已声明的 settlement。

边界 B：用一个主线 SettlementLoopStep 保持 busy
不要把 coreNeedsRefill 并入 MainlineBusy 形成第二个 busy 真相。

安全方式：

Intent Script
→ 初始 Resolve/Present
→ SettlementLoopStep 一直留在 BattleTimeline
   → Core 若有下一 slice，追加 Resolve/Present
   → Core 报 Stable 后 Step 才结束
→ 主线真正 idle

这样 ADR-0004 的“输入互斥只认主线”无需改变。

边界 C：每轮稳定化都必须独立 ack
建议一轮是：

FillEmptySlotsAction
+ 这一轮 Fill 引发的同步反应链

这轮产生的新空位由下一轮稳定化处理，而不是在同一 Dispatcher 命令中继续跑到最终状态。

这样既能补到不动点，又保持：

批次锁步；
可见因果；
chainId 连续；
CardFace beat commit；
玩家能看懂 Deal→触发→移除→再 Deal。
边界 D：Fusion 排除规则必须迁入 Core
当前以下逻辑不能留在表现层：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\FusionRefillScheduler.cs:226-277

安全选择有两类：

Core 的稳定化事务携带 ExcludedDrawUids/CauseTransactionId；
融合结果先进入 Core staging zone，待融合 slice ack 后，由下一 Core slice 转入 DrawPile。
无论哪种，Presentation ack 可以控制下一 Core slice 的时间，但不能由表现层自行改变牌序来决定规则结果。

边界 E：ack 必须升级为“成功就位或成功重建”
在 Core 不变量落地前，应明确：

nullView
placeDenied sticky
无 anchor
View spawn 失败
关键 Sync motion 失败
不能只是 LogWarning 后 mComplete=true。

安全策略应是二选一：

从 Batch Snapshot/Core state 重建目标 View，重建成功才 complete；
返回 Present failure，拒绝 ack，并走明确的错误恢复/中止流程。
不得：

View 失败
→ 清表现占格
→ ack
→ 主线 idle

更不得反向修改 Core 来迎合 View。

边界 F：稳定化必须有终止保护
至少需要：

每个 settlement 的 action budget；
状态无进展检测；
循环状态摘要；
开发期 Assert；
正式环境 Critical Anomaly。
但预算超限不能静默把半稳定盘面当正常 idle。

十一、当前测试与契约护栏的缺口
1. Fusion 测试与生产批次顺序存在静态漂移
生产 Explore 已是：

Click
→ Interaction
→ Fill
→ Rotate

见：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Flow\Presentation\ExploreIntentScriptFactory.cs:81-109

但 Fusion 独立批测试仍把第二批标成 Fill、第三批标成 Rotate：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Fusion\ResolveFusionRefillBatchCommandTests.cs:51-72

按当前源码，第二批应是 Interaction，第三批应是 Fill，Rotate 应是第四批。这是明确的测试/生产合同漂移。本次未执行测试，因此不推断实际测试运行结果，但静态期待值需要重新核对。

2. nullView 测试只保护“不留幽灵”，不保护最终一致
C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\Assets\Scripts\NineGrid.Presentation\Tests\Cards\DealNullViewTests.cs:94-101

只断言：

返回 false；
Ground occupancy 为空。
缺少：

Core 已有该 UID 时必须重建；
失败不得 ack；
Mainline 不得进入正常 idle；
CoreCount/OccupiedCount 最终一致。
3. 没有直接的 Core 稳定态契约测试
当前已有：

Fill 在 Rotate 前；
Enemy Finale 才 Fill；
LeaveTrap clear 禁止 Fill；
Drain/Fusion 是独立 Batch。
但没有找到这样的正式断言：

当一次允许补牌的交互事务准备返回可输入状态时：
若 DrawPile 非空，则 Board 不得有可补空位。

也没有针对“Fill 的 OnDeal/OnEnter 再制造空位”的不动点回归测试。

最终判断
当前架构已经有很好的承重骨架：

PresentationDirector
→ BattleTimeline
→ ResolveBatchStep
→ PresentationSyncBatchGate
→ PresentStep
→ Channel complete
→ FlushBeats / Flip idle
→ FinishBatch ack

真正尚未闭合的是：

谁拥有“盘面必须补到稳定”的规则

目前答案仍是“多个 Flow 剧本和 Scheduler 共同拥有”，所以会持续产生路径遗漏。

最安全的长期形态应是：

Core 持有 settlement 状态机和补牌不变量
→ Core 决定下一语义切片是否必须 Fill
→ Director 用同一主线逐切片 Resolve/Present/ack
→ Core 报 Stable 后主线才允许跑空

同时必须保留五条硬边界：

不在任意 RunToCompletion 尾部自动 Fill；
不在敌方齐射中途 Fill；
不改变 Fill-before-Rotate；
Fusion 排除/牌序策略迁入 Core；
View 发牌失败不得正常 ack，也不得反向修正 Core。
本次仅研究和读取，未修改任何文件。

## 请以 medium 级别研究仓库现有领域词汇、ADR、spec、落地票/issue 文档的目录和格式约定，并阅读 Assets/Notes/补牌问题探查.md 提到的 .opencode/reports 两份报告（若存在）。查找 CONTEXT.md、CONTEXT-MAP.md、docs/adr、specs、tickets/issues/plans 等。只研究不改动。输出应该产出哪些文件、建议路径、可复用模板和相关既有文档引用。

结论
本仓库是单一领域上下文：

存在根级 
未发现 CONTEXT-MAP.md
未发现通用的根级 specs/、tickets/、issues/、plans/
长期文档权威是：
：领域词汇与现行领域事实
：长期决策和行为不变量
：已经落地的代码现状
GitHub Issues：Spec、PRD、父票和实施票
、、 都是过程材料，不应成为最终权威。
本次只读研究，未修改任何文件。

现有目录与格式约定
1. 领域词汇
权威文件：


现有词条格式统一为：

## 领域分区
 
**规范词（可选英文/代码名）**：
一句或一段定义。
_Avoid_: 应避免的近义词、旧称或错误模型

与补牌问题直接相关的既有规范词包括：

解算批次（Batch）
就位回执（ack）
编排时间线（BattleTimeline）
占格权威 vs 几何注册
九宫格互动
盘面冻结
清关
常规机关装填
触发可见因果
目前缺少正式定义的概念：

盘面稳定化
稳定边界
开局铺场
战中补位
补位不动点
补位原因 / 入场原因
因此，这次若最终采纳 Core 稳定化方案， 应补这些词条，而不是继续只在报告中使用。

依据：

2. ADR
目录：


现有约定：

四位数字递增：0001–0033
文件名：NNNN-english-kebab-name.md
正文标题通常使用中文
YAML frontmatter 只有状态
常用状态：proposed、accepted
固定章节：
---
status: proposed
---
 
# 中文决策标题
 
## 决策
 
## 为什么
 
## 考虑过的替代
 
## 后果
 
## 相关

本议题会直接触碰：

其中：

ADR-0001：需要澄清 Core 稳定化怎样拆成可 ack 的批次；不能把 ack 误写成“决定规则是否执行”。
ADR-0012：当前敌方行动收尾明确写着“补牌 + 通关检查”，若改为统一稳定化边界，需要修订这条局部职责。
ADR-0026：必须继续保留“清关当拍禁止补牌”。
ADR-0030：若区分开局铺场与战中补位，需要对开局机关触发语义交叉引用或修订。
3. Spec / PRD
仓库明确规定：Spec 和 PRD 的权威载体是 GitHub Issue，而不是新建一个通用 specs/ 目录。

依据：


已使用过的父 Spec 格式可从以下临时草稿复用：

标准章节为：

## Problem Statement
 
## Solution
 
## User Stories
 
## Implementation Decisions
 
## Testing Decisions
 
## Out of Scope
 
## Further Notes
 
## Child tickets

 目前只存 OpenCode ticket-runner 这类仓库工具设计，不足以证明游戏功能 Spec 也应放在那里：


因此不建议新建根级 specs/，也不建议把补牌 Spec 放入 。

4. 落地票 / Issue
正式实施票也是 GitHub Issue。现有子票正文约定非常稳定：

## Parent
 
Part of #<父 Spec>
 
## What to build
 
明确本票要交付的垂直切片。
 
## Acceptance criteria
 
- [ ] 可观察行为
- [ ] 契约或测试
- [ ] 编译与 Console 门槛
 
## Blocked by
 
- #<前置票>

可复用样本：

Issue 依赖的权威表示优先使用 GitHub native dependencies；不支持时才回退到正文中的 Blocked by:。父子关系优先使用 GitHub sub-issue；不支持时使用 Part of #N 和父票 task list。

 是被 Git 忽略的本地镜像状态，不是票面权威：


5. 实施计划
存在两种计划格式，但都不是长期领域权威。

用于具体实施计划，格式通常为 YAML frontmatter 加批次：

---
name: ...
overview: ...
todos:
  - id: ...
    content: ...
    status: pending
isProject: false
---
# 标题
 
## 已定设计
 
## 实施批次
 
## 验证与完成条件

样本：

用于非权威的拆票、迁移顺序和阶段计划，常在开头明确注明“过程笔记，非权威”，并交叉引用 ADR、CONTEXT 和 code-map。

样本：


绝对路径：

本议题建议让 GitHub 父 Spec 和子票承担实施规划；除非需要离线交给 Agent 执行，否则不必再复制一份 Notes 计划。

两份  的核心结论
两份报告均存在并已阅读：

原始问题说明：


绝对路径：

报告形成的共同事实是：

Core 没有统一补牌不变量。
生产代码中的 FillEmptySlotsAction 主要只有开局、PostKill、Drain/Fusion 三类显式入队入口；没有 pipeline 收尾自动稳定化，也没有 TriggerSystem 的系统级补位触发器。

补牌由表现层剧本逐路径手工排队。
Attack、Explore、Reveal、UseItem、Fusion、EnemyAction 各自存在不同的补牌调度与守卫，新增空槽来源时很容易漏接。

FillEmptySlotsAction 只执行一圈。
发完牌后才处理 OnDeal / OnEnter；如果这些触发再次移除卡牌，本批不会继续补到不动点。

节点三开局空格已定位。
捕熊陷阱在开局 Fill 的 Post 触发中攻击邻牌并自移除；StartNode 只有一次 Fill，没有稳定化收尾，因此留下“空格 + 牌堆仍有牌”。

Attack 和 UseItem 的击杀判定口径不一致。
Attack 只看目标是否死亡，UseItem 看任意卡是否死亡；Attack 批内若效果杀死或移除非目标卡，可能不进入补牌分支。

敌方行动收尾不是可靠兜底。
没有可参加敌方行动的怪物时，调度器会提前返回，因此也不会执行其收尾补牌。

Core 漏补与表现占格分叉是两类问题。
nullView、Core 数量与表现登记数量不一致属于视图重建/占格镜像问题，不应和 Core 稳定化混为一个修复。

这些结论与 
 的建议一致：Core 应拥有补牌的必然性和确定性，表现层只拥有节奏与展示。

建议产出的权威文件与路径
A. 必须产出：新 ADR
建议路径：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\docs\adr\0034-board-stabilization-and-refill-fixed-point.md

建议标题：

盘面稳定化：Core 在稳定边界补位至不动点

建议包含的核心契约：

一次外部意图及其全部派生效果结算完成后，才进入稳定化。
节点未终止、当前相位允许补位、牌堆有牌且存在可补空位时，Core 必须继续补位至不动点。
补出的牌所触发的 OnDeal / OnEnter 仍属于同一次稳定化。
清关、Victory、Defeat 等终止态禁止补位。
开局铺场与战中补位具有明确的 Core 原因字段，而不是都只靠 ActionSource=FillEmptySlots。
稳定化必须有进展检查、状态循环检测和 Action Budget。
表现层不得决定、取消或补造领域发牌事件。
ack 控制批次何时继续播放，不决定领域规则是否成立。
Core 占格仍是唯一真相；表现失败不得反向清除 Core。
初始状态建议为 proposed；Q1–Q3 明确定案后再切为 accepted。

B. 必须产出：父 Spec GitHub Issue
建议标题：

Spec · 盘面稳定化与补牌不变量收口

这是权威 Spec，不建议另建正式仓库 specs/ 文件。

如果需要先离线准备 Issue 正文，可临时使用：

C:\Users\jinji\Documents\GitHub\Ninegrid Gambit\.scratch\refill-stabilization-spec-body.md

父 Spec 至少应锁定：

Problem Statement：补牌规则被分散到表现剧本。
Solution：Core BoardStabilizer / ResolveUntilStable。
User Stories：开局无残留可补洞、任意效果移除后自动收敛、表现失败不改规则。
Implementation Decisions：稳定边界、FillReason、终止条件、批次/ack 模型。
Testing Decisions：固定种子契约、运行时不变量、Action Budget、Console 门槛。
Out of Scope：占格视图重建、通用动画重做、无关效果系统重构。
Child tickets：按依赖顺序列出。
C. 必须产出：子票
建议拆为六张，而不是一张大票直接删除全部 Scheduler：

补牌稳定态探针与失败基线
固定种子覆盖：

StartNode 捕熊陷阱造成空位
Fill 批内 OnDeal/OnEnter 再移除
Attack 批内非目标死亡/移除
idle 时“空格 + 可抽牌”不变量失败
Core BoardStabilizer 与不动点算法
包含：

相位和清关门禁
重复 Fill + 触发链
单调进展
状态循环检测
Action Budget
稳定完成事件
开局铺场与战中补位原因契约
定案捕熊陷阱是否在 InitialLayout 开火；引入 FillReason / EntryReason，避免表现层静默机关。

旧补牌入口统一委托稳定器
ResolvePostKillFill、Drain、Fusion、Enemy Finale 暂保 API，但内部统一调用稳定器；消除判定口径分叉。

表现层去规则化与 Batch/Ack 对齐
表现层只消费 Core 发出的 Deal/Remove/Trigger/Stabilized 事实；删除“是否需要补牌”的判断，但保留播放节奏。

收口旧 Scheduler、结构护栏与文档同步
删除已无调用价值的重复调度器或降为兼容壳；补结构测试、code-map 和最终验证。

子票模板直接复用  的 Parent / What to build / Acceptance criteria / Blocked by 格式。

D. 条件产出：领域词汇更新
建议修改现有文件，而不是新建第二份词汇表：


建议新增：

**盘面稳定化（Board Stabilization）**：
一次外部意图及其派生效果完成后，Core 将盘面结算到可重新开放交互的稳定状态；若节点未终止、当前阶段允许补位且牌堆仍有可发牌，则反复补位并处理入场触发，直到不存在可补空位或牌堆无法继续补位。
_Avoid_: 每条表现剧本手工补牌、任意 Action 尾部立即补牌、表现层发现空格后修改 Core
 
**开局铺场（Initial Layout）**：
节点开始、玩家取得输入前建立初始盘面的入场原因；它与战中补位是否触发攻击性或自毁性入场效果必须由 Core 规则显式区分。
_Avoid_: 把所有 FillEmptySlots 都视为同一种战中补牌
 
**战中补位（Post-interaction Refill）**：
九宫格互动或效果链造成空位后，在稳定化阶段进行的补位；新牌入场触发仍属于当前稳定化事务。
_Avoid_: 击杀后补牌、Drain 补牌、Fusion 补牌各自成为独立规则

只有在 ADR 定案后才应写入，避免把未决术语提前写成权威。

E. 落地后必须更新：Code Map
只有代码落地后更新，不能提前描述目标结构：

应记录：

Core 稳定化入口及其职责。
表现层不再判断是否补牌。
旧 Drain/Fusion/PostKill Fill 的去留。
新契约测试和结构护栏。
ADR-0034 索引。
不要提前写入尚未实现的 BoardStabilizer 目标树。
F. 建议单独处理：占格分叉 / nullView
不要塞进 Core 稳定化父票，先与以下已有计划对照：


若报告中的 nullView 和 27 次真实占格分叉在该计划之后仍可复现，再单开兄弟 Issue：

表现实体创建失败与 Core—Presentation 占格分叉治理

验收重点应是从 Core Snapshot 重建、引用身份保护和开发期 Assert，而不是表现失败后回滚 Core。

推荐 ADR 模板
---
status: proposed
---
 
# 盘面稳定化：Core 在稳定边界补位至不动点
 
## 决策
 
定义稳定边界、允许补位的相位、清关门禁、不动点条件、
开局/战中原因、终止保护，以及表现层职责。
 
## 为什么
 
说明逐剧本 Fill 的漏点机制，以及 Fill 批内入场触发导致一圈算法失效。
 
## 考虑过的替代
 
- PresentationDirector 空闲兜底
- 逐剧本补漏
- 任意 RunToCompletion 尾部自动 Fill
- 只给 StartNode 再补一次
 
## 后果
 
说明批次拆分、事件投影、旧 Scheduler 迁移、测试与诊断成本。
 
## 相关
 
- ADR-0001
- ADR-0012
- ADR-0026
- ADR-0030
- CONTEXT.md
- 两份 .opencode 探查报告