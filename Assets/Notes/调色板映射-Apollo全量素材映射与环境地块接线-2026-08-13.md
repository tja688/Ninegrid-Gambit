# 调色板映射：Apollo 全量素材映射与怪物卡环境背景接线（2026-08-13）

## 做了什么

1. **全量调色板映射**：`Assets/Arts/Images` + `Assets/Resources/ContentArt` 下全部 7786 张 PNG
   就地映射到 Apollo 46 色（`Assets/Arts/配色文件/apollo.gpl`），0 错误，抽查 300+ 张全部合规。
2. **区域色偏**：八张岩层/密林环境地块 + 七套正式卡组的怪物动画帧（209 张）按环境色域加权，
   其余素材中性映射。
3. **八张环境图接线（怪物卡卡面背景）**：纯数据接线——给七套正式卡组的 35 张怪物卡
   JSON 填 `sprites.faceBackground`，表现管线已支持（`CoreCardPresentationMapper` →
   卡面「背景」节点，槽位映射见 `CardFaceSlotNodeMap`）。九宫格场地格子保持原有底框不动。
   八张图 meta 的 PPU 调为 16，与「怪物卡标准模板.prefab」背景槽原切片（47×68@16）对齐。

## 卡组 ↔ 环境 ↔ 色域对照（权威表）

| deckId | 设计主题 | 楼层 | 环境地块 | region |
|--------|---------|------|----------|--------|
| deck.dragon | 基础 | 1 | 岩层_浅岩层 | qianyanceng |
| deck.orc_legion | 旋转 | 1 | 密林_地下丛林 | conglin |
| deck.insect | 召唤 | 2 | 岩层_藏骨堂 | canggutang |
| deck.stone_legion | 烈焰 | 2 | 岩层_熔岩之地 | rongyan |
| deck.void | 决斗 | 2 | 密林_失落遗迹 | yiji |
| deck.smallanimal | 链接 | 3 | 密林_岩洞区 | yandong |
| deck.skeleton_legion | 翻面 | 3 | 密林_血色 | zhaoze |
| （备用变体，暂未接线） | — | — | 岩层_血色 | rongyan |

主题名出处：`docs/feedback-analysis/content-audit-report.md`（deck↔设计案表名对照）。

## 可用脚本（scripts/）

| 脚本 | 用途 |
|------|------|
| `palette-map-apollo.py` | 全量映射主工具。`--dry-run` 核对 / `--pilot` 出对比图 / `--run` 就地覆写；`--rules` 追加区域规则；`--only` 过滤子串；`--report` 出 CSV |
| `gen-region-rules.py` | 从卡 JSON（deckId + 动画目录）生成 `region-rules.json`；共用目录自动回退中性 |
| `region-rules.json` | 生成产物（30 条），供 `--rules` 消费；内容变更后重新生成 |
| `palette-compare.py` | 原图\|映射后 并排对比图，输出 `.scratch/palette-map/compare/` |
| `set-monster-face-backgrounds.py` | 按 deck↔环境表给 35 张怪物卡填 `faceBackground`（幂等，`--clear` 还原，自动镜像 StreamingAssets） |

编辑器内增量映射（`Assets/Editor/PaletteMapFolderWindow.cs`，纯 C# 同源算法，
默认调色板 `Assets/Arts/配色文件/apollo.gpl`）：
- Project 右键文件夹 → **NineGrid 调色板映射（递归子文件夹）**，支持多选，直接映射
- 窗口 **NineGrid/Tools/Palette Map Folder（调色板映射）**：可换调色板、干跑统计
全局经验沉淀为 skill：`~/.claude/skills/unity-palette-mapping/`。

环境：`py -3.12`（装有 numpy + pillow）。标准流程：

```
py -3.12 scripts/gen-region-rules.py
py -3.12 scripts/palette-map-apollo.py --dry-run --rules scripts/region-rules.json
py -3.12 scripts/palette-map-apollo.py --run --rules scripts/region-rules.json --report .scratch/palette-map/run-report.csv
```

## 经验记录

- **Oklab 最近色 + 权重**是关键：明度权重 1.15 保住像素画明度结构；色度权重 1.30 让近中性色
  优先落灰阶，避免头骨/石头出现蓝绿杂点。纯 RGB 距离会把暗部映得很脏。
- **区域加权取温和值（0.8–1.25）只掰"摇摆色"**：太激进会把固有色也拉偏（比如骷髅白骨被
  拉成紫色）。目的是氛围偏重，不是整体调色。
- **共用素材必须回退中性**：同一帧目录被过渡卡组和主题卡组共用时（本次 5 个目录），
  强行套区域色会让同一素材在不同上下文中不一致，回退中性最稳。
- **仅映射可见像素**（alpha>0），透明区 RGB 不动，避免边缘 halo 变化。
- **接线优先走内容数据**：卡面各槽位（背景/卡框/横幅…）已有 JSON 字段 →
  `CardFaceSlotNodeMap` 节点名的完整管线，填 `sprites.faceBackground` 即生效，
  不需要新组件。先确认管线再写代码，能少一个运行时组件。
- **换图先对 PPU 与切片尺寸**：模板「背景」槽原切片 47×68@PPU16（世界 2.94×4.25），
  密林四张 47×68 同规格、岩层四张 53×70 略大；八张图 meta 的 PPU 从 32 调 16 后
  Simple 渲染尺寸即对齐，无需改 prefab。
- **数据驱动查引用**：怪物 id→sprite 没有映射表，全靠卡 JSON 的 `animations.slots[].path`
  显式指路径；查"谁引用了这批帧"要扫 `Assets/Arts/ContentVisual/cards/*.json`。

## 后续（未做，供参考）

- 运行时 tint 收敛（详见本次排查结论）：场景烘焙大面积 tint（MainBG、菜单背景、HUD 图标）、
  卡框稀有度 runtime 乘算（`ContentVisualResolver`→`StandardCardView.SetFrameColor`）、
  战斗信息预览黄框 ×27 —— 源图已入调色板后，这些应改为出图预着色或调色板内预设色。
- 31 色自然系调色板实验：另开分支 `尝试31色调色板`（基础中性映射，不做区域分化）。

## 2026-08-18 还原素材本色

公司要求不再强行吃 Apollo 色卡。从映射提交 `77e49b3c4` 的父节点 `fd3c7878d` 把 **7777** 张 PNG 像素还原回映射前本色（不改 `.meta` / 场景 / JSON）。

映射后像素又改过的 **8** 张已跳过，并备份到 `Backups/pre-palette-restore-specials/`（接线清单见该目录 `MANIFEST.md`）：刺客 Icey idle 五帧、主菜单 `F_UI_MenuIcons_A13`/`A8`、以及已不再接线的主菜单旧图 `6f3a84bd-…edit.png`。

未进入 7785 张清单的后续产物（Venue 场地框变体、后加卡面背景、教学/封面图等）本次未动。编辑器增量映射窗口仍在，未关闭。

映射后已从工程删掉的 14 张（教学图标迁到 `ContentArt`、`密林_地下丛林`/`密林_岩洞区` 被新环境图替换）曾被父提交 checkout 带回来，已再次删除，不保留。
