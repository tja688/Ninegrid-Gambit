# 表演锚点排期与触发脉冲 —— BeatScheduler · Handler 链 · TriggerPulseHub · Cue 声明 · 金币表现

> 权威代码：`Flow/Presentation/` 下锚点排期与脉冲区（共 29 个文件，见文末清单）。
> 关联 ADR：ADR-0005（卡面数值 Commit 时机）、ADR-0016（翻牌门控）、ADR-0018（Impact 冲刷时机 / 触发可见因果）、ADR-0028（有效护甲）、ADR-0035（倒计时投影）、ADR-0036（音频跨会话生命周期）、ADR-0040（VFX 独立型不占 ack）

## 职责综述

Core 每批解算会产出 `PresentationInstruction`（结算指令：UpdateHp / ShowDamage / UpdateFaceUp / TriggerEffect / UpdateGold …）。本区回答「**这些指令在表演的哪个锚点（Beat）、由谁消费**」：`BattleBeatScheduler` 是唯一分发点，把 pending 指令按报点（Impact / Settled）派给注册顺序固定的 `IBattleBeatHandler` 链；装饰类输出（飘字/音效/VFX/金币）经 `TriggerPulseHub` 三通道「发即完成」，不占主线 ack。

`AudioCue` / `VfxCue` 系列文件是**发射语义注册表**：稳定 cue ID + 权威发射点标注，全项目音视效的声明中心。

## 关键类型表

| 类型 | 文件 | 一句话职责 |
|------|------|-----------|
| `BattleBeatScheduler` | `Presentation/BattleBeatScheduler.cs` | 结算指令唯一分发点：pending 队列 + 按 Beat 报点派发 + 隔离区 + 未消费诊断 |
| `BattleBeatHook`（静态类） | `Presentation/BattleBeatHook.cs` | 编排/命中帧报点 → Scheduler 的静态桥（委托由 CompositionRoot 注入） |
| `BattleBeatFlush`（静态类） | `Presentation/BattleBeatFlush.cs` | 冲刷门面：FlushBeats（Impact→Settled）、只冲翻牌、非锁步 EventLog 切片 Present |
| `IBattleBeatHandler` | `Presentation/IBattleBeatHandler.cs` | 锚点处理器契约：`TryApply` 认领即消费（true），否则交后续（false） |
| `PlayerInfoHudBeatHandler` | `Presentation/PlayerInfoHudBeatHandler.cs` | 链① Avatar 血/甲 HUD 旁路刷新（恒 return false 让卡面继续认领） |
| `DamageFloaterBeatHandler` | `Presentation/DamageFloaterBeatHandler.cs` | 链② Impact 装饰：飘字/受击音/受击 VFX（ShowDamage 认领；治疗/加甲旁路后放行） |
| `CardFaceStatHandler` | `Presentation/CardFaceStatHandler.cs` | 链③ 卡面数值唯一 Commit 出口：只从指令绝对值赋值，不读 Core |
| `CardFaceFlipBeatHandler` | `Presentation/CardFaceFlipBeatHandler.cs` | 链④ Settled 消费 UpdateFaceUp：Commit FaceUp + 串行翻牌/Snap |
| `EffectTriggerPulseBeatHandler` | `Presentation/EffectTriggerPulseBeatHandler.cs` | 链⑤ Impact 消费 TriggerEffect：FX/音效/类型化 VFX 脉冲装饰 |
| `GoldGainBeatHandler` | `Presentation/GoldGainBeatHandler.cs` | 链⑥ Impact 消费 UpdateGold：广播飞币/HUD 请求 |
| `TriggerPulseHub`（静态类） | `Presentation/TriggerPulseHub.cs` | FX/音效/VFX 三通道脉冲唯一出口，发即完成，可开关降级 |
| `AudioTriggerPulseSink` + `IAudioCuePulseSink` | `Presentation/AudioTriggerPulseSink.cs` | 音频脉冲落地 `IAudioSystem.RequestCue` |
| `CardEffectTriggerPulseSink` | `Presentation/CardEffectTriggerPulseSink.cs` | 旧 FX 通道：`fx.card.{uid}` → 场上卡 `PlayEffectTriggerPulse`（不在场静默丢弃） |
| `DebouncingTriggerPulseSink` | `Presentation/DebouncingTriggerPulseSink.cs` | 装饰 sink：同 cue ID 窗口内（0.05s）只放行首次 |
| `GatedTriggerPulseSink` | `Presentation/GatedTriggerPulseSink.cs` | 装饰 sink：谓词关闭时丢弃脉冲 |
| `NullTriggerPulseSink` | `Presentation/NullTriggerPulseSink.cs` | 空 sink（关闭通道时的可降级实现） |
| `VfxTriggerPulseSink` + `IVfxCuePulseSink` + `NullVfxCuePulseSink` | `Presentation/VfxTriggerPulseSink.cs` | 类型化 VFX 落地 `IVfxSystem.RequestCue` |
| `TriggerStep` + `ITriggerPulseSink` | `Presentation/TriggerStep.cs` | 时间线内一次性脉冲 Step（首帧 Pulse 即 Finished，卡时序用 DelayStep） |
| `AudioCue` 系列声明类 + `AudioCueAttribute` | `Presentation/AudioCue.cs` | 全项目音频 cue ID 注册表 + 发射点标注 + `DeckEntryAudioSession` |
| `VfxCueAttribute` + `VfxCueContexts` 等 | `Presentation/VfxCue.cs` | VFX cue 元数据声明基础设施（无业务逻辑） |
| `BattleCombatVfxCues` / `CardLifecycleVfxCues` / `CombatOutcomeVfx` / `SkillEffectTrapRelicVfxCues` / `FlowBattleEndVfxCues` | `Presentation/BattleVfxCues.cs` | 战斗/生命周期/结果/技能/胜负 VFX cue 声明（ADR-0040 独立型） |
| `ProjectileVfxCues` | `Presentation/ProjectileVfxCues.cs` | 弹道 VFX：源→靶双坐标，返回命中延迟供编排对齐飘字扣血 |
| `GoldGainVfxCues` | `Presentation/GoldGainVfxCues.cs` | `economy.gold_flight` 金币飞入唯一 VFX cue（#203） |
| `GoldGainPresentationScheduler` | `Presentation/GoldGainPresentationScheduler.cs` | 扫 EventLog `GoldModified` → 广播 `GoldGainPresentationRequested` |
| `GoldGainPresentationRequested`（struct） | `Presentation/GoldGainPresentationRequested.cs` | 金币增减单向表现请求（UI/FX 只消费不回写 PlayerModel） |
| `DamageNumberRequested`（struct）+ `DamageNumberKind` | `Presentation/DamageNumberRequested.cs` | 世界坐标飘字请求（单向 FX） |
| `DescriptionOutputEvents`（三 struct） | `Presentation/DescriptionOutputEvents.cs` | 描述显示/清除请求（HUD 提示，非卡面 Commit 权威） |
| `RelicHudSyncRequestedEvent`（struct） | `Presentation/RelicHudSyncRequestedEvent.cs` | 请求遗物栏从 Core 同步（RelicHudController 消费） |
| `TeardownPresentationDirectorRequested`（struct） | `Presentation/TeardownPresentationDirectorRequested.cs` | 流程壳离战斗请求宿主 HardClear/Teardown 导演 |

## 核心流程

### 1. Handler 链与认领语义

注册顺序（`PresentationCompositionRoot.InstallBattleBeatScheduler` 装配，**顺序即契约**）：

```text
① PlayerInfoHudBeatHandler   Avatar 血/甲 HUD —— 旁路刷新后 return false（指令继续下传）
② DamageFloaterBeatHandler   飘字/受击音/VFX —— ShowDamage return true；治疗 UpdateHp、
                             加甲 UpdateArmor 旁路飘字后 return false
③ CardFaceStatHandler        卡面数值 Commit —— UpdateHp/Armor/ActionCount、Countdown、
                             ModifyBaseStat、KillCard、Spawn/Deal/ShowAvatar、OfferReward 全认领 true
④ CardFaceFlipBeatHandler    UpdateFaceUp —— Commit FaceUp + FlipPlaybackCoordinator 串行播翻
⑤ EffectTriggerPulseBeatHandler  TriggerEffect —— FX/音效/VFX 脉冲，装饰消费 true
⑥ GoldGainBeatHandler        UpdateGold —— 广播 GoldGainPresentationRequested，true
```

`BattleBeatScheduler.TryDispatch` 按序调用，**第一个返回 true 的处理器消费并移出 pending**。关键顺序依赖：**飘字必须在卡面前**——治疗的 `UpdateHp` 需 ② 先飘绿字再 false 放行给 ③ Commit；颠倒则治疗飘字永远收不到。Settled 后仍有 pending → `DiagnoseUnconsumed` 断言（未消费诊断）。

### 2. 报点时序（Beat）

- `OnBatchOpened(batchId)`：新批开门，pending 换代（上一批残留 `DiagnoseDiscardedPending`）。
- `ReportBeat(Impact)`：命中帧——装饰类（飘字/金币/TriggerEffect）在此消费；`FlushImpactExcept`（ADR-0018）支持 Vacate 前先飘字、延迟指定 TriggerEffect。
- `ReportBeat(Settled)`：收束帧——卡面数值/翻牌在此提交。
- `FlushUpdateFaceUp`：Present Begin 前只冲翻牌（hop/移位通道先翻牌，战斗通道传 false 延到命中后，ADR-0016）。
- **隔离区**：`QuarantineImpactWhere` / `ReleaseQuarantined`——神圣决斗惩罚指令隔离至决斗者攻击帧再放行；开新批即丢弃隔离区。
- `PresentStandalone`：非锁步单指令直发（开局引导等）。
- `BattleBeatFlush` 门面：`FlushBeats()`（Impact→Settled 一次冲完，锁步 PresentStep 与非锁步路径同构）；`PresentEventLogSlice*` 系列把 EventLog 切片映射成指令再走同一 Handler 链（非战斗用牌/回收/开局路径）。

### 3. TriggerPulseHub 三通道

| 通道 | 生产装配（`TriggerPulseOutputController.ConfigureProductionDefaults`） | 语义 |
|------|------|------|
| FX | `CardEffectTriggerPulseSink` | `fx.card.{uid}` 旧卡面效果脉冲；卡不在场静默丢弃 |
| Audio | `DebouncingTriggerPulseSink(AudioTriggerPulseSink, 0.05s)` | `AudioCueRequest` → `IAudioSystem`；同 cue 去抖 |
| Vfx | `VfxTriggerPulseSink` | 类型化 `IVfxSystem.RequestCue`，可带空间上下文返回 `PresentationPlan` |

纪律：`SafePulse*` 吞异常不拆主线；通道关闭记 degraded trace；`ResetToNull` 全清 / `ResetFxToNull` 仅清 FX（**音频跨战斗会话**，ADR-0036）；禁止 legacy `sfx.effect.<uid>` 动态绑定。

### 4. 金币表现（#199/#203）

链路：Core `GoldModified` → `GoldGainPresentationScheduler`（扫切片，跳过 Delta=0 与 `unusedHelpCards` 原因）或 `GoldGainBeatHandler`（锁步 Impact）→ 广播 `GoldGainPresentationRequested` → `GoldGainPresentationBinder`（[HUD 篇](09-指针命中、HUD与检视.md)）：花费 SnapGold；增益经 `TriggerPulseHub.PulseVfx(economy.gold_flight)` 拿到首达/末达延迟计划 → 驱动 HUD 数字时间窗。UI/FX 单向消费，不回写 `PlayerModel`。

### 5. Cue 声明纪律

- `AudioCue.cs`：按域分组的静态声明类（交互/UI、卡牌生命周期、战斗、技能效果、Avatar 脚步、房间经济等），每条带 `[AudioCue]` 权威发射点标注；`CombatOutcomeAudio` 与飘字**同缝分诊**（block/absorb/hp/heal/armor 一处判定，音画对齐）；`SkillEffectTrapRelicAudioCues.ResolveCueId` 按 trap./relic./skill. 前缀路由。
- `BattleVfxCues.cs`：与音频同缝同分诊（ADR-0040 独立型 VFX 不占 ack）；甲伤 VFX 偏移 0.28 避免与血溅重叠；统一经内部 `VfxPulseEmit.PulseAt` → Hub。
- `ProjectileVfxCues.PulseFromTo`：返回 `PresentationPlan` 命中延迟，编排据此对齐飘字/扣血时点。

## 对外通信面

- **上游**：`PresentationCompositionRoot`（装配 Handler 链与 Hook 委托）；PresentStep / 编排在锚点调 `BattleBeatHook.ReportBeat`；非锁步路径调 `BattleBeatFlush.PresentEventLogSlice*`。
- **下游**：`ManagedCard.CommitPresentation`（经 `CardFaceStatHandler`）；`FlipPlaybackCoordinator`；`RelicHudHook`（倒计时 Commit，ADR-0035）；`PlayerInfoHudPresenter` / `DamageNumberHook` / `GoldGainPresentationBinder`；`IAudioSystem` / `IVfxSystem`。
- **Event**：`GoldGainPresentationRequested`、`DamageNumberRequested`、`Description*`、`RelicHudSyncRequestedEvent`、`TeardownPresentationDirectorRequested`（QFramework 单向广播）。

## 关联 ADR

ADR-0005（数值只经指令绝对值 Commit）、ADR-0016（FaceUp 冲刷时机）、ADR-0018（Impact 分段冲刷 / 可见因果）、ADR-0028（有效护甲经 `StatArmorUtility`）、ADR-0035（遗物倒计时走 Hook Commit）、ADR-0036（音频 sink 跨会话）、ADR-0040（VFX 独立型）。

## 不变量与坑

- **Handler 顺序是行为契约**：①②在③前是治疗/血甲飘字能工作的前提；装配处调序即无声坏。
- **装饰不占主线 ack**：飘字/金币/脉冲全部发即完成；需要阻塞的表演走 Present 通道或外部租约。
- `CardFaceStatHandler` **只认指令绝对值**，不读 `CardRegistry`/`IStatSystem`——Core 回读会把批间中间态钉进卡面。
- `ModifyBaseStat(Armor)` 只刷 HUD 不刷卡面甲（基础甲与战斗剩余甲分轨）。
- 无效 uid 的 UpdateFaceUp 仍 `return true` 空消费——否则 Settled 未消费断言误报。
- 隔离区在开新批时丢弃：神圣决斗惩罚若跨批未释放即静默失效，属设计内。
- 手牌 HelpCard 的 OnSelfUsed 不在 FX 脉冲列（卡已离场）；道具槽音频不受棋盘门禁。

## 本篇文件清单（29）

`BattleBeatScheduler.cs`、`BattleBeatHook.cs`、`BattleBeatFlush.cs`、`IBattleBeatHandler.cs`、`PlayerInfoHudBeatHandler.cs`、`DamageFloaterBeatHandler.cs`、`CardFaceStatHandler.cs`、`CardFaceFlipBeatHandler.cs`、`EffectTriggerPulseBeatHandler.cs`、`GoldGainBeatHandler.cs`、`TriggerPulseHub.cs`、`AudioTriggerPulseSink.cs`、`CardEffectTriggerPulseSink.cs`、`DebouncingTriggerPulseSink.cs`、`GatedTriggerPulseSink.cs`、`NullTriggerPulseSink.cs`、`VfxTriggerPulseSink.cs`、`TriggerStep.cs`、`AudioCue.cs`、`VfxCue.cs`、`BattleVfxCues.cs`、`ProjectileVfxCues.cs`、`GoldGainVfxCues.cs`、`GoldGainPresentationScheduler.cs`、`GoldGainPresentationRequested.cs`、`DamageNumberRequested.cs`、`DescriptionOutputEvents.cs`、`RelicHudSyncRequestedEvent.cs`、`TeardownPresentationDirectorRequested.cs`（均在 `Flow/Presentation/`）。
