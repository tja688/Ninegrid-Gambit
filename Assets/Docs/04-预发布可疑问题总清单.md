# 预发布可疑问题总清单

> 汇总 2026-08-12 六个区块代理逐文件读码（816 个 .cs）时发现的全部可疑问题，按严重度分五组。每条注明出处文档；条目细节以出处文档与代码为准。
>
> **使用须知**：本清单是「预发布风险台账」，不是修复工单——部分条目是刻意行为（出处文档已标注），修复前先读出处确认口径；真要修时遵循 code-map 验证门槛（recompile 后 Console 无新增 Error）。

## 一、疑似真 bug / 行为风险

| # | 问题 | 出处 |
|---|------|------|
| 1 | **云存档删除无墓碑**：`SteamCloudRunSaveStore.Delete` 仅在 `CloudAvailable` 时删云文件。终局清自动档那一刻若 Steam 离线/云不可用，云端旧档残留，下次 `SyncOnBoot` 会把已删除的自动档从云端拉回本地——已结束的 run 可能「复活」 | [Platform桥接/README](Platform桥接/README.md)（不变量与坑 §6） |
| 2 | **效果倒计时投影 every≤1 永不上屏**：`every<=1` 的触发在 `Matches` 提前 return，`CountdownAdvanced` 永不置位——配了 `projectKey` 也不会产出投影提交。投影键应只配给 every≥2 的节奏 | [Core/09-效果DSL与技能装配](Core/09-效果DSL与技能装配.md)（不变量与坑） |
| 3 | **主菜单阶段 VfxSystem 无 Tick 源**：`PresentationSceneRoot.Update` 在 `mInstalled==false` 时直接 return，而 VfxSystem 注册于主菜单阶段——主菜单期得不到 Tick。当前主菜单无 VFX 消费者故无表症；未来主菜单要播 VFX 必须另找 Tick 源 | [Systems与通信/01-装配与场景绑定](Presentation/Systems与通信/01-装配与场景绑定.md)（§4 与「坑」）、[05-VFX系统](Presentation/Systems与通信/05-VFX系统.md) |
| 4 | **音频冷却与排期时钟不同源**：AudioSystem 冷却统一 `Time.realtimeSinceStartup`（unscaled），但 `UnityAudioCueScheduler` 用 `WaitForSeconds`（scaled）——`timeScale≠1` 时排期被拉伸，与冷却判定不同步 | [Systems与通信/04-音频系统](Presentation/Systems与通信/04-音频系统.md)（不变量与坑） |
| 5 | **经济/节点规则表缺文件静默降级**：`ContentCatalogTableLoader`/经济表/节点规则表缺文件仅 LogWarning 返回空——Player 若漏打 StreamingAssets 表现为空目录而非硬报错，只能靠卫生校验与构建流程兜底 | [Content/01-内容目录与加载链路](Content/01-内容目录与加载链路.md)（不变量与坑）、[Content/README](Content/README.md)（可疑 #3） |
| 6 | **DamageLogPanel 不可达**：未接入 `DevTestSceneInstaller`，无场景常驻实例，F1 伤害日志面板实际打不开（需手动 AddComponent） | [Content/07-DevTest工具箱](Content/07-DevTest工具箱.md)（不变量与坑）、[Content/README](Content/README.md)（可疑 #7） |
| 7 | **`VfxBindingPlayback.ResolveTint` 无法表达全透明**：RGBA 四通道全 0 被解释为「未配置→白色」；想要真全透明黑须给 alpha 一个极小非零值 | [Content/04-音频与特效内容真源](Content/04-音频与特效内容真源.md)（不变量与坑）、[Content/README](Content/README.md)（可疑 #4） |
| 31 | **~~交战内旋转的时序副作用~~（已修，ADR-0044，2026-08-12）**：原问题——「攻击后旋转」经 `OnBattle` 插在「玩家命中」与「怪物反击」之间结算、反击无几何校验，正交怪被转到斜角后仍反击；击杀落地前尸体跟环转一格。**裁决与修复**：交战窗与敌方行动阶段为「位移锁定窗口」，效果 DSL 触发的盘面位移（Rotate/Swap/Move）挂起到收尾锚点（互动计数推进 / 齐射收尾 / 道具链收尾）就地复验后落地——先打再转（[ADR-0044](../../docs/adr/0044-battle-window-deferred-board-motion.md)）。`relic.beyond_dimension` 已复活回 `deck.relic`。交战/齐射路径的尸体跟转随之消除；内核侧「已死未移除卡触发自身技能」等剩余面仍见 [Issue #207](https://github.com/tja688/Ninegrid-Gambit/issues/207) | `BattleScopeSystem.TryDeferBoardMotion`、`PhaseSystem.FlushDeferredBoardMotionsInternal`、`DeferredBoardMotionActions.cs` |
| 32 | **~~遗物选后哑火（实例生效失败）~~（已修四层，ADR-0047 补遗，2026-08-12 晚）**：原问题——corelog-20260812-124012 事故局中生命护符/复合盔甲选取后整局无效果：授予链上任意动作 Apply 异常（当日为反应栈深度误爆）炸穿命令，`AddRelic` 已做而效果实例未挂；且自愈仅遗物级（多装配部分丢失不补）、`kind:Modifier` 空挂死实例（挂载时目标缺位、实例在但零修饰符）永不自愈、全程无 corelog 痕迹。**修复**：① `ResolveAction`/`DispatchTriggers` 对 Apply 与反应构建异常熔断遏制（不再炸穿命令，兄弟动作照常）；② 自愈改装配级 + 空挂死实例检测重挂（`ReactivateMissingRelicEffectsAction`）；③ 挂载审计事实 `RelicEffectMountAudited`（declared/implemented/mounted/modifiers 四数）+ `RelicGranted`/`PipelineFault` 进 corelog；④ 木甲系 `tpl.relic.wood_armor.base` 加 `healOnAttach`——加上限回等量血（与 `ModifyBaseStat` 局内约定对齐，此前 8 个 Modifier 型加上限遗物不回血且 UI 几乎无感），丢弃时当前血钳到新上限。回归 `RelicMountResilienceTests`（9 用例） | `C/Systems/ActionPipelineSystem.cs`、`C/Domain/Actions/ContentActions.cs`、`C/Effects/EffectSystem.ActivateModifier`、`P/Flow/Diagnostics/RhythmFaceFlowTraceBinder.cs`、[Core/08](Core/08-内容装填奖励经济与存档.md)、[Core/09](Core/09-效果DSL与技能装配.md) |

## 二、出包风险

| # | 问题 | 出处 |
|---|------|------|
| 8 | **`NineGrid.TemporaryTest.asmdef` 未限平台且引用 `Unity.Pipeline`**（热重载工具包）：发布构建若裁掉该包或其不支持 Player 编译，出包断链。预发布清理建议整目录删除或 `includePlatforms` 限 Editor | [工具与实验/README](工具与实验/README.md)（三·不变量与坑 #1） |
| 9 | **`CardManagerSingleton.BootstrapPrefabs` 仅 Editor 兜底**：Player 下底盘/机关卡面完全依赖场景序列化字段；场景配置丢失时 Spawn 失败（有 Error 日志，无自动恢复） | [Cards/README](Presentation/Cards/README.md)（可疑 #7）、[Cards/01-底盘与卡面模板](Presentation/Cards/01-底盘与卡面模板.md) |

## 三、废止残留与死代码

| # | 问题 | 出处 |
|---|------|------|
| 10 | **属性房三选二完整路径残留**（ADR-0031 已废止、正式流程不触发）：Core 侧 `PendingChoiceModel.AttributeSelectedDefIds`、`RunModel.AttributePickDefIds`、`OfferAttributePickSessionAction`/`SelectAttributeCandidateAction`、`RewardSystem.AttributePickCount`、PhaseSystem 对应门禁；Flow 侧 `AttributeBoard/` 四件（Presenter/HitProxy/SlotResolver/`AttributePickIndexResolver`——最后一个未按 `AttributeBoard*` 前缀命名，与同目录不一致） | [Core/02-领域状态模型](Core/02-领域状态模型.md)、[Core/08-内容装填奖励经济与存档](Core/08-内容装填奖励经济与存档.md)、[Flow/08-房间流程与场地板](Presentation/Flow/08-房间流程与场地板.md)、[Flow/README](Presentation/Flow/README.md) |
| 11 | **意图缓冲机制残留**：`IntentDisposition.BufferToDirector` 枚举与 `IBufferedIntentLegality`/`BoardBufferedIntentLegality` 已被 strict-drop 取代，仅存 legacy 装配签名——不得据枚举名恢复缓冲行为 | [Flow/04-意图门禁与剧本工厂](Presentation/Flow/04-意图门禁与剧本工厂.md)、[Systems与通信/02-输入门禁与平台守护](Presentation/Systems与通信/02-输入门禁与平台守护.md) |
| 12 | **动态描述管道退役残留**：三条 `RequestDescription*Command`（Show/ShowText/Clear）与 `DescriptionOutputController` 空壳（把 `DescriptionDisplayHook` 四缝置 null）。空壳保留是为 SubsystemRegistration 清 Hook，禁复活也别裸删 | [Systems与通信/06-Commands与Queries](Presentation/Systems与通信/06-Commands与Queries.md)、[07-Controllers](Presentation/Systems与通信/07-Controllers.md) |
| 13 | **`AttackPatternRules.Frequency` 恒返 0**：废止兼容桩，别再当频率表用（频率与计数条款归 ADR-0038） | [Core/06-节奏倒计时与敌方行动](Core/06-节奏倒计时与敌方行动.md) |
| 14 | **`GroundMotionExecutor.IsBusy` 死代码**：getter 内 `FieldBattlePresentationHook.BattleOrNull()` 取值后未使用 | [Cards/README](Presentation/Cards/README.md)（可疑 #2） |
| 15 | **`CardEffectManager.TryConsumeHoverSuppression` 名不副实**：名为 Consume 但只读返回不清位（清位在播放 finally），语义上是 `IsHoverSuppressed` | [Cards/README](Presentation/Cards/README.md)（可疑 #3） |

## 四、文档与口径矛盾

| # | 问题 | 出处 |
|---|------|------|
| 16 | **ADR-0025 决策正文与替代段对「忙时回收」表述矛盾**：决策正文写「回收……主线 busy 时拒收」，替代段却写「忙时拒收回收：否决——改为排队」。现行代码与正文一致（忙时 strict-drop 拒收，ADR-0004 修订后），替代段是过期表述，修 ADR 时以正文为准 | [02-ADR索引与代码对照](02-ADR索引与代码对照.md)（0025 行）、仓库 [docs/adr/0025](../../docs/adr/0025-item-slots-run-persistent-hold.md) |
| 17 | **同步技能检测双口径可能漏报**：`MonsterLoadoutPresentationValidator` 用模板 id 启发式（含 `delivery.move`/`gear_delivery`），投影层 `DetectSyncRhythmSkills` 扫 body `OnCardRhythmFire`——前者口径窄，遭遇校验可能漏报有同步技能的配置 | [Content/05-内容编辑器与卫生工具](Content/05-内容编辑器与卫生工具.md)、[Content/01-内容目录与加载链路](Content/01-内容目录与加载链路.md)、[Content/README](Content/README.md)（可疑 #6） |
| 18 | **`IsNumericArgKey` 白名单双份易漂移**：`EffectAssemblyResolver` 与 `EffectDesignTextParameterizer` 各持一份内容相同的数值键白名单，新增数值键需两处同步 | [Content/README](Content/README.md)（可疑 #1）、[Content/02-效果模板与参数化装配](Content/02-效果模板与参数化装配.md) |
| 19 | **Workbench delta 信封实为全量快照**：`EditorWorkbenchTransport` 的 delta 信封与 snapshot 信封 payload 相同（均为全量），「delta」名不副实——前端不可依赖增量假设 | [Content/README](Content/README.md)（可疑 #8）、[Content/06-编辑器工作台体系](Content/06-编辑器工作台体系.md) |

## 五、代码卫生

| # | 问题 | 出处 |
|---|------|------|
| 20 | **`CardDeckTween.cs` 中文注释乱码**：类注释与 `LaunchFieldExitThenDeckInsert` XML 注释是编码错乱的乱码（如「鐗岀粍鐩稿叧…」）；行为正常，建议按 UTF-8 修复注释 | [Cards/README](Presentation/Cards/README.md)（可疑 #1） |
| 21 | **`effect_templates.json` 早期行 GBK 残留**：`design_text` 存在编码历史遗留，部分工具下显示乱码；仅影响作者备注展示，不进玩家界面 | [Content/README](Content/README.md)（可疑 #2）、[Content/02-效果模板与参数化装配](Content/02-效果模板与参数化装配.md) |
| 22 | **`SkeletonDeckPresentationManager.PresentFusionAsync` XML 注释格式错误**：summary 段出现两个 `</summary>` | [Cards/README](Presentation/Cards/README.md)（可疑 #4） |
| 23 | **`HandCardHitProxy` 双轨命中**：仍写自带 collider 尺寸作回退，主路径已是槽位带 AABB 数学——与 ADR-0023 不冲突（手牌非落格对象），但属可收敛的双轨 | [Cards/README](Presentation/Cards/README.md)（可疑 #5） |
| 24 | **三个 Manager 的 `GameObject.Find("Anchors")` 兜底**：`GroundFieldView`/`CardHandManagerSingleton`/`CardDeckManagerSingleton` 未绑定时走 Find；生产装配应依赖场景序列化字段（#141 装配卫生语境下勿新增依赖） | [Cards/README](Presentation/Cards/README.md)（可疑 #6） |
| 25 | **`CardInspectInlineIconHover` 未走统一指针读口**：仍用 `Input.mousePosition` 而非 `WorldPointerUtility`，迁移时注意 | [Flow/09-指针命中、HUD与检视](Presentation/Flow/09-指针命中、HUD与检视.md)（不变量与坑） |
| 26 | **FxLab 用 `FindFirstObjectByType` 找 `CardManagerSingleton`**：`FxLabTrapHalo`/`FxLabCardFoil` 违反 #141 禁 Find 的字面约定；实验模块属豁免灰区，转正时必须改显式绑定 | [工具与实验/README](工具与实验/README.md)（一·不变量与坑 #4） |
| 27 | **两处隐性顺序契约**：① Beat Handler 链注册顺序即语义（飘字必须在卡面 Commit 前，颠倒则治疗飘字失效）；② `RoutingIntentScriptFactory` 遍历**全部**子工厂而非 first-match——新工厂必须「kind 不匹配立即 return」，否则一意图双剧本。两处均无编译期保护，只靠纪律 | [Systems与通信/01-装配与场景绑定](Presentation/Systems与通信/01-装配与场景绑定.md)、[Flow/05-表演锚点排期与触发脉冲](Presentation/Flow/05-表演锚点排期与触发脉冲.md)、[Flow/04-意图门禁与剧本工厂](Presentation/Flow/04-意图门禁与剧本工厂.md) |
| 28 | **BattleInfoPreview 预览槽右键详述断口**：预览槽右键详述已断开（待动态框选系统），`BattleInfoPreviewSlotView` 暂不接入 `PointerHitRegistry` | [Flow/09-指针命中、HUD与检视](Presentation/Flow/09-指针命中、HUD与检视.md)（不变量与坑） |
| 29 | **`BoardBriefTipCopy.GoUpTip` 未启用文案**：「返回上一层」常量与 `ForContentId` 的 GoUp 分支已备好，但本轮无 GoUp 导航图标消费方（源码注释自证「含本轮未启用的 GoUp」） | [Flow/08-房间流程与场地板](Presentation/Flow/08-房间流程与场地板.md)（BoardBriefTip 节） |
| 30 | **`CardPresentationEditorSession` 纯浏览即标脏**：打开怪物卡面即执行 skillIds 展开并标脏——设计使然，但易误触保存产生非预期改动 | [Content/README](Content/README.md)（可疑 #5）、[Content/05-内容编辑器与卫生工具](Content/05-内容编辑器与卫生工具.md) |
